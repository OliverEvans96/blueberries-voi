---
title: Rust API (voi_core)
sources:
  code: [crates/voi_core/src/lib.rs]
---

# Rust API (`voi_core`)

The shared compute kernel is documented with **rustdoc** — auto-generated API
reference from inline `///` comments on the Rust source.

Browse the API:

**[Open rustdoc →](/api/rust/voi_core/index.html)**

## Status

Most entries are **stub** comments pointing back to the narrative VitePress pages.
A follow-up pass will replace stubs with full API prose.

## Where the narrative lives

Concept explanations, math, and modeling rationale are on this site under
*The store*, *Inference*, *Control*, and related sections. Each page's
**In the code** table links symbols here when they are part of the public API.

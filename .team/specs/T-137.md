# T-137: Zero-init episodes and phantom-belief remediation

## Goal

Empty shelf at day 0 everywhere; remove `starting_inv`; UPC and LGTIN belief mass parity on zero-sales days; filter birth matches arrival qty.

## Acceptance criteria

### WS0 — Remove starting_inv (web)

- AC-0a: `SimConfig` has no `starting_inv`; TS builds.
- AC-0b: Logistics controls have no starting-inventory slider.
- AC-0c: `createInitialState(...).lots === []`.
- AC-0d: Guard test fails if `starting_inv` string reappears outside `.team/changelog.md`.

### WS1 — Empty filter init (Rust)

- AC-1: After `EngineSession.init`, `sum(belief.lot_counts)==0` for P1, F1, F2.
- AC-2: Belief matches `empty_flat_belief(L,K)` semantics.
- AC-3: Empty-shelf ordering tests pass.
- AC-4: Guard forbids pre-fill loop in `seed_particle_bank` / `init_filter_bank`.

### WS2 — LGTIN parity (Rust)

- AC-5: 8 zero-sales days seed 42 L=2: F1 and P1 `sum(lot_counts) ≤ 0.5`.
- AC-6: `sales_by.len()==0`, bank L=2 → finite LL preferring low alive.
- AC-7: T-136 `unit_pf_ac` regressions pass.

### WS3 — Birth count (Rust)

- AC-8: Arrival 8 cases → `sum(lot_counts) ≈ 8` not 15.
- AC-9: Multi-arrival filter mass tracks physics on_hand within tolerance.

## Out of scope

- VOI sweep artifact regeneration.
- UI belief clamping.
- Multinomial change when `sales_tot > 0`.

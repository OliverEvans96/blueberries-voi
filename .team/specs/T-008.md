# T-008 ADR lock: RichObs, MC LL, Stage C redefine, L fallback

## Context

M1.5 Wave 0 must lock architecture before any production or test code. The plan
(`.team/plans/M1.5-filter-complete.md`) names four decisions that would be expensive to reverse:
RichObs+masks, MC observation likelihood, generative Stage C, and joint→sliding_window fallback.

## Acceptance criteria

- [ ] ADR **0086** (RichObs + UNOBSERVED + masks) exists under `.team/adr/` with STATUS ACCEPTED.
- [ ] ADR **0087** (MC observation likelihood from shared `day_step` kernels) exists with STATUS ACCEPTED.
- [ ] ADR **0088** (Stage C generative check vs `day_step`) exists with STATUS ACCEPTED.
- [ ] ADR **0089** (dynamic L + joint→sliding_window fallback) exists with STATUS ACCEPTED.
- [ ] `.team/adr/INDEX.md` lists ADRs 0086–0089 with titles, board IDs / milestone, and ACCEPTED status.
- [ ] Specs **T-009 through T-018** exist under `.team/specs/` with acceptance criteria, interfaces,
      out of scope, and open questions.
- [ ] `.team/specs/README.md` includes an M1.5 wave table covering T-008–T-018.
- [ ] No production code under `src/` and no new test files are introduced by this ticket.

## Out of scope

- Any implementation in `src/blueberries_voi/`
- Failing tests (qa starts at T-009+)
- New runtime dependencies
- Reopening ⚑ cards (FIL-01, FIL-08, FIL-12, MOD-14/15/17, SCN-P2/F3/B-clair, …)

## Interfaces

none (documentation-only ticket)

## Open questions

- [x] ADR numbers: next free after 0085 → **0086–0089** (locked by this ticket).
- [ ] Whether `P1Obs` is retained as a deprecated constructor into `RichObs` or deleted in T-010 —
      defer to T-010 open questions; ADRs allow either.

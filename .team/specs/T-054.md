# T-054 ViewModelProjector + MockAdapter speaking DayDelta

## Context

Refactor the mock so UI consumes deltas through the projector. Can start after export ADR /
T-053 without waiting for the Release wheel (maximize overlap).

## Acceptance criteria

- [ ] `ViewModelProjector` applies Snapshot and DayDelta into the existing `ViewModel` shape used
      by D3 charts.
- [ ] `setEconomics` updates PnL / ghost locally **without** calling any engine adapter method
      (test or assert/spy).
- [ ] `MockAdapter` implements `EngineAdapter` and returns Snapshot/DayDelta (not a full
      ViewModel from `step`).
- [ ] Heatmap density is computed in JS from flat belief marginals × lot counts.
- [ ] Unit tests (vitest/jest or project runner) cover projector apply + economics-local path.
- [ ] Fake physics may remain inside MockAdapter until T-057 removes it from the default studio
      path.

## Out of scope

- Real Pyodide/Http adapters (T-055/T-056)
- Removing mock as default in studio (T-057)

## Interfaces

See T-053. MockAdapter lives under mockup `web/src/…`.

## Open questions

- [x] May proceed before T-047 wheel exists.

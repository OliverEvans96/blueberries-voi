# T-053 UI ADR — EngineAdapter + ViewModelProjector ownership

## Context

Slice 3 starts in the D3 mockup worktree (`web/d3-simulator-mockup`). Wave 0 froze Python export
ownership; this ticket writes the **UI ADR** for adapter + projector boundaries before TS code.

## Acceptance criteria

- [ ] A new ADR under `.team/adr/` (next free number at authoring time) is **ACCEPTED** and
      states: `EngineAdapter` interface; `ViewModelProjector` applies Snapshot/DayDelta;
      `setEconomics` / heatmap / ghost / PnL are **JS-local**; Mock / Pyodide / Http adapters
      share the projector.
- [ ] ADR records work location: mockup branch/worktree `web/d3-simulator-mockup` (separate
      checkout acceptable).
- [ ] Specs T-054–T-057 remain consistent with this ADR (update if needed).
- [ ] INDEX.md lists the new UI ADR.
- [ ] No requirement to finish PyodideAdapter in this ticket.

## Out of scope

- Implementing projector/adapters (T-054–T-056)
- Studio chrome wiring (T-057)

## Interfaces

```text
interface EngineAdapter {
  init(config): Promise<Snapshot>
  step(order_qty: number): Promise<DayDelta>
  step_n(orders: number[]): Promise<DayDelta[]>
  reset(config?): Promise<Snapshot>
  act?(opts?): Promise<DayDelta>
}

class ViewModelProjector {
  applySnapshot(s: Snapshot): ViewModel
  applyDelta(d: DayDelta): ViewModel
  setEconomics(e: Economics): ViewModel  // local only; no engine call
}
```

## Open questions

- [ ] ADR number assigned at authoring (after 0100+ any intervening ADRs).

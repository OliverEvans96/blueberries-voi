# T-103 Cargo workspace: voi_core / voi_py / voi_wasm skeleton

## Context

Oliver reopened X-09 / ENG-01 for a shared Rust compute kernel (ADRs 0119–0121). This ticket
lands the **empty compiling workspace** and documents follow-on tickets. No physics port here.

## Acceptance criteria

- [ ] Repo-root `Cargo.toml` workspace members `crates/voi_core`, `crates/voi_py`, `crates/voi_wasm`.
- [ ] `cargo test -p voi_core` succeeds on the host (native).
- [ ] `crates/voi_core` is an `rlib` with a public crate root (may export stubs only).
- [ ] `crates/voi_py` declares PyO3 cdylib named `blueberries_voi._core` (may be a stub `pymodule`).
- [ ] `crates/voi_wasm` depends on wasm-bindgen (stub `handle_rpc` allowed).
- [ ] `target/` is gitignored; no `*wasm*` / `*pyodide*` Python packages under `src/blueberries_voi`.
- [ ] T-047 guard `test_no_pyodide_or_wasm_modules_under_src` still passes (crates/ and packaging/wasm/ are allowed).
- [ ] Draft CI notes under `packaging/github-workflows/` mention `cargo test` (do not edit live `.github/workflows/`).
- [ ] ADRs 0119, 0120, 0121 exist; 0009 marked superseded for compute; 0099 amended for `wasm`.

## Out of scope

- Porting physics, WOR DP, day_step, filter, episode, PyO3 façades, studio adapter, benches
  (follow-on tickets from this tip).
- Copying drafts into live GitHub Actions.

## Interfaces

```text
crates/voi_core     — rlib
crates/voi_py       — cdylib blueberries_voi._core
crates/voi_wasm     — wasm-bindgen handle_rpc(request_json) -> String
BLUEBERRIES_VOI_BACKEND=python|rust   (documented; not wired this ticket)
VITE_ENGINE_ADAPTER=...|wasm          (documented; not wired this ticket)
```

## Open questions

- [x] Live CI rustc: human copies packaging draft; agents do not edit `.github/workflows/`.

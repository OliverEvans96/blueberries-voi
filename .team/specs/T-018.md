# T-018 M1.5 close-out

## Context

M1.5 is done only when plan §9 DoD holds: rich obs+masks, honest LL, generative Stage C, multi-rung
A/B (+ oracle), L policy, no CTL/VOI/browser, agent-dev-team artifacts complete, quality gates green.

## Acceptance criteria

- [x] All of T-009–T-017 acceptance criteria are marked done in their specs (or explicitly waived
      with Oliver note in `.team/backlog.md`).
- [x] `.team/qa/` contains green records for M1.5 tickets in scope; no open red qa blockers without
      `needs-human`.
- [x] `.team/reviews/` contains APPROVED reviews for the M1.5 implementation waves (or equivalent
      per-ticket reviews covering T-009–T-017).
- [x] `.team/changelog.md` has a plain-English M1.5 entry (client voice): what the filter can observe
      per rung, that likelihood matches sim physics, and honest P0/P1 caveats if Stage A failed there.
- [x] DoD checklist from plan §9 is copied into `.team/reviews/` or a close-out note with each item
      checked (RichObs; rich DayLog; soft LL gone; Stage C generative; Stage A multi-rung published;
      Stage B/oracle; L fallback; no CTL/VOI/browser; quality gates).
- [x] `uv sync --all-extras && uv run ruff check . && uv run ruff format --check . && uv run mypy src tests && uv run pytest`
      is green (coverage ≥80%).
- [x] No production CTL/VOI/browser modules landed under this milestone beyond pre-existing stubs.

## Out of scope

- Starting M2.6 controller work
- VOI sweep
- Reopening ⚑ cards
- New runtime dependencies

## Interfaces

none (process / documentation close-out)

## Open questions

- [x] Whether changelog is one entry or per-wave entries rolled up — prefer one M1.5 client-facing
      entry at close-out. (Decided: one M1.5 entry at close-out.)
- [ ] Sign-off owner if Oliver is unavailable (document in backlog if blocked).

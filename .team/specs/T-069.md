# T-069 ShelfBelief arrival-age exports, Stage A re-gate, changelog

## Context

ADR 0106 keeps the `ShelfBelief` / ENG-01 wire shape but changes age-row semantics to
arrival-prior exports. ADR 0105 shifts FIL-11 Stage A framing away from in-store age
learning. T-068 delivers the filter core; this ticket updates belief export, Stage A
docs/harness claims, and the plain-English changelog rationale for dropping RB age.

## Acceptance criteria

- [ ] `shelf_belief_from_rbpf` exports `age_marginals` derived from arrival belief
      (Dirac / birth histogram / F2a prior / cold Abdella as carried by the filter), not
      from `mean_field_update` posteriors; nested `(L,K)` shape unchanged.
- [ ] ENG-01 flatten / Snapshot–DayDelta path remains wire-compatible: flat `age_marginals`
      length `L·K` with the same field names (`flatten_shelf_belief` / schema still pass).
- [ ] Stage A–style docs and harness under `experiments/` (and any FIL-11 Stage A notes
      touched for honesty) state that F2a/F2 age information comes from **priors**; P0/P1/F1
      do **not** claim in-store age learning / contraction as a production gate.
- [ ] `.team/changelog.md` gains a plain-English entry that Rao–Blackwellised / in-store
      age marginalisation was removed because **in-store age learning was dropped** (not
      because “bootstrap is simpler”), citing T-069 (and T-068 as needed).
- [ ] Automated tests cover arrival-derived `age_marginals` on at least one birth-prior
      path (e.g. F2 Dirac or F2a) and wire-compat flatten length `L·K`.
- [ ] Quality gates: qa RED `--no-cov`; implement green; reviewer APPROVED; verifier full
      AGENTS rung PASS.

## Out of scope

- Rewriting the filter weight / count transition core (T-068)
- Restoring in-store age LL updates
- Merging to `main`
- Citeable calibrated VOI science claims

## Interfaces

```text
src/blueberries_voi/filter/belief.py          # shelf_belief_from_rbpf age rows
src/blueberries_voi/simulator/belief.py       # flatten_shelf_belief (unchanged shape)
experiments/fil11_stage_a*.md (and related)   # Stage A honesty re-gate
.team/changelog.md
```

## Open questions

- [x] Wire shape change? **No** — semantics only (ADR 0106).

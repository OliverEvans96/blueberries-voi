# T-034 M2 close-out

## Context

Final M2 DoD: acceptance criteria green, reviews APPROVED, qa green, client-voice changelog, and
explicit non-claims for VOI/browser packaging.

## Acceptance criteria

- [x] `.team/qa/` records PASS for M2 tickets T-023–T-033 (or aggregated wave qa files per protocol).
- [x] `.team/reviews/` shows APPROVED for the M2 implement tips covered by this close-out.
- [x] `.team/changelog.md` has a plain-English M2 entry (CTL ladder + multi-scenario; no VOI dollar
      headlines).
- [x] `voi/` package remains a stub (`__all__` empty / no VOI sweep API shipped).
- [x] Close-out checklist asserts **no** Pyodide packaging / ENG-01 browser demo / WASM ship claims
      in changelog or plan status.
- [x] Production `PRODUCTION_BACKEND == "mean_field"` still holds in tests.
- [x] No new runtime dependencies introduced by M2 without a dedicated ADR.

## Out of scope

- Starting M3 VOI implementation
- Reopening ENG-01
- Merging to `main` (human decision)

## Interfaces

none (process / documentation ticket; may add a DoD checklist markdown under `.team/`)

## Open questions

- [x] Whether close-out checklist is `.team/qa/T-034.md` only or also a short `.team/plans/` status
      flip on `M2-controller.md` — prefer qa + changelog + plan status line.
      **Decided:** qa + changelog + `.team/reviews/M2.md` DoD checklist + plan status COMPLETE.

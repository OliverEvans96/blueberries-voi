# T-087 CAL-C3 — Demand UI (DOW profile / protection coverage)

## Context

Demand section must show calendar structure (DOW profile and protection-interval coverage), not a
stationary μ-only PMF or decorative mock sinusoid presented as physics.

## Acceptance criteria

- [ ] Demand UI renders a DOW profile (seven-day means or factors) from Snapshot `demand_summary`
      (or live equivalent), not a decorative unrelated sinusoid unmarked as non-physics.
- [ ] Protection-interval coverage is shown for order days (3 / 3 / 4 demand-day spans on
      Sun/Tue/Thu) using schedule fields — chart or labeled summary acceptable.
- [ ] Sales/demand charts that assume i.i.d. daily μ-only are updated or clearly marked non-physics /
      removed.
- [ ] Mock adapter supplies enough stub profile data for the charts to render in mock mode.
- [ ] Web unit/smoke test or visual checklist asserts DOW series length 7 and protection labels
      present.
- [ ] No new runtime Python deps; no HF in browser path.

## Out of scope

- Next-order-day advance control (T-086)
- Fitting new FreshNet profiles
- VOI milestone closeout (T-088)

## Interfaces

```text
# Consumes T-085 demand_summary + schedule
# charts/demandDist.ts (or successor) reads DOW series
```

## Open questions

- [ ] Exact chart library marks — follow existing D3 patterns in `web/`.

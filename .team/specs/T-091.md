# T-091 Studio Autopilot Mode — Wave 0 (ADR + plan + specs)

## Context

Operators want Autopilot play on the Vite+D3 studio driven by `adapter.act` at
interactive cadence, with a Controller section for policy/budget knobs. Plan IDs
T-076–T-081 collided with CAL-01; this ticket locks ADR **0111**, the remapped
plan **T-091–T-096**, and specs for implement waves. Documentation only.

## Acceptance criteria

- [ ] `.team/adr/0111-studio-autopilot-mode.md` exists with STATUS **ACCEPTED** and
      locks policy aliases (`damped_sw`/`sw`, `rollout`/`ctl`/`rollout_order`,
      `constant`/`const`/`fixed`), cadence algorithm + default intervals, ActOpts
      ownership (HTTP nest / Pyodide flat), UI section ownership, and non-goals.
- [ ] `.team/adr/INDEX.md` lists ADR **0111** in the narrative bullets and the
      index table.
- [ ] `.team/plans/studio-autopilot.md` exists with the binding remap table
      (plan T-076→T-091 … T-081→T-096; ADR 0109→0111), ticket map, and wave
      concurrency (T-092 ∥ T-093 ∥ T-094 after T-091; T-095 after 092+093;
      T-096 after 095).
- [ ] Specs exist at `.team/specs/T-091.md` through `.team/specs/T-096.md` with
      observable acceptance criteria and Interfaces sections.
- [ ] `.team/backlog.md` reserves Autopilot **T-091–T-096** / ADR **0111**, notes
      that **T-076–T-081 are CAL-01 not Autopilot**, and states next free after
      Autopilot is **T-097+**.
- [ ] `.team/intake.md` has a brief Studio Autopilot Mode note quoting the locked
      defaults (order qty/day; 1–2 act/s; default `damped_sw`; intervals
      500/1000 ms; out of scope list).
- [ ] `.team/.state/current-ticket` reads `T-091` on this worktree tip.
- [ ] No production code under `src/` or `web/` and no new automated tests are
      added by this ticket (docs / `.team/` only).

## Out of scope

- Implementing T-092–T-096 behavior
- Editing `.github/workflows/`
- Merging to `main`
- Claiming or modifying CAL-01 tips (T-076–T-088)

## Interfaces

None (documentation). Downstream contracts are named in ADR 0111 and specs
T-092–T-096.

## Open questions

- [x] Ticket/ADR remap — locked in plan + backlog (T-091–T-096 / 0111).
- [x] Default `alpha` when omitted — **0.9** (ADR 0111); `rho` default **0.8**.

# T-088 CAL-D1 — VOI smoke + CAL-01 changelog closeout

## Context

Milestone closeout: VOI smoke under the new MWF + calendar-demand base case, client-readable
changelog, DoD checklist, and FIL-13 remotesure note if cadence changes measured L.

## Acceptance criteria

- [ ] VOI CI/smoke path runs successfully under default `OrderSchedule` (MWF / LT=1) and committed
      demand profile (budgets per ADR 0095 still respected or explicitly adjusted with rationale).
- [ ] Smoke (or focused regression) confirms CRN demand identity across ≥2 scenarios still holds
      under the new base (reuses T-084 assertion at smoke scope).
- [ ] `.team/changelog.md` gains a plain-English CAL-01 entry stating the new base case and that
      prior daily/i.i.d. citeable VOI numbers require regeneration.
- [ ] Milestone DoD note exists (review file or plan status update) listing T-077–T-087 complete and
      acceptance of ADRs 0112–0116.
- [ ] FIL-13 / measured-L **remotesure** note is written (changelog or `.team/` short note) if
      cadence/demand change could invalidate prior L measurements — at minimum a backlog/plan
      sentence pointing at remotesure; no joint-production reopen.
- [ ] `.team/backlog.md` CAL-01 status updated to reflect closeout pending human merge (agents do
      not merge to `main`).
- [ ] No `.github/workflows/` edits; no force-push; no parent merge.
- [ ] Verify-style commands from `AGENTS.md` pass on the closeout tip (verifier owns full gate).

## Out of scope

- Full overnight production VOI grid
- Reopening X-06 or VOI-02
- FreshNet two-stage latent recovery
- Human merge to `main`

## Interfaces

none beyond existing VOI smoke entry points and changelog format

## Open questions

- [x] Citeable regen required after base-case change (Oliver / plan risk).
- [ ] Whether smoke budgets need numeric relaxation — only if verify fails; document in qa note.

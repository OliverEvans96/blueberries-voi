# T-075 Dual-mode live smoke + readiness close-out

## Context

T-071–T-074 close the code gaps. This ticket proves the studio works against a
**real** local FastAPI API and **real** Pyodide with a locally built slim wheel,
records evidence, and closes the readiness plan with changelog / backlog notes.

## Acceptance criteria

- [ ] `.team/qa/T-075-smoke.md` exists and records **PASS** for both:
      1. **Live HTTP mode** — real ASGI process + Vite (or curl/httpx against
         ASGI plus studio http adapter path) advancing/resetting a session
         without MockAdapter / FakeWorker.
      2. **Live Pyodide mode** — real Pyodide load of a wheel built by
         `scripts/build_slim_wheel.py` via local `wheelUrl` (not FakeWorker-only).
- [ ] Commands executed (or clearly equivalent documented) include at least:
      ```bash
      uv sync --extra api
      uv run python scripts/build_slim_wheel.py && uv run python scripts/smoke_slim_wheel.py
      ```
      plus the live HTTP and live Pyodide smoke steps with captured output /
      exit codes in the evidence file.
- [ ] FakeWorker / unit-only runs are **not** accepted as the sole evidence for
      either checklist item.
- [ ] Plan `.team/plans/ENG-01-readiness.md` marked COMPLETE (pending human
      merge) when smoke PASS.
- [ ] `.team/backlog.md` updated: readiness Done / pending human merge (agents
      did not merge to `main`).
- [ ] `.team/changelog.md` has a plain-English client-voice entry for dual-mode
      readiness.
- [ ] Close-out checklist `.team/reviews/ENG-01-readiness.md` exists with STATUS
      APPROVED (or equivalent DoD) listing non-goals: no live workflow edits,
      no production deploy, no citeable science VOI.
- [ ] Verifier full quality gates (ruff + mypy + pytest with coverage) PASS on
      the tip that includes T-071–T-074 landings.

## Out of scope

- Merging to `main` (human)
- Editing `.github/workflows/`
- Production CDN / Release publishing
- Citeable science VOI
- Arrival-only filter (T-067–T-069)

## Interfaces

- Evidence artifact: `.team/qa/T-075-smoke.md`
- Close-out: `.team/reviews/ENG-01-readiness.md`
- Changelog: `.team/changelog.md`

## Open questions

- [ ] Exact ports / PIDs for live servers — record whatever the smoke used.

# T-027 Rung 0 corrected age-blind baseline

## Context

CTL-05 requires a Nahmias-style corrected age-blind baseline (Rung 0) that observes total on-hand
only, not ages — the honest competitor to age-aware SW, not a strawman.

## Acceptance criteria

- [ ] A Rung 0 policy computes orders from **total on-hand** (+ pipeline) with an expected-outdating
      correction (documented formula in module docstring referencing Nahmias / CTL-05).
- [ ] When survival weights are constant (β=1 / \(w\) flat fixture), Rung 0 orders **coincide** with
      the survival-weighted policy on the same protection interval \(\Delta\tau_L\) (prep for T-032
      β=1 degeneracy gate) within an exact integer match after `case_round`.
- [ ] Policy returns case-rounded order quantities via T-026 `case_round`.
- [ ] Closed-loop smoke with T-024 driver + T-025 profit runs without error on a short fixture
      episode.
- [ ] `uv run pytest` for this ticket’s tests passes.

## Out of scope

- α grid search (T-029)
- Rollout improvement (T-030)
- Claiming ladder profit gaps before tuned α

## Interfaces

```text
# controller/ — e.g. rung0.py or ordering.py extension
class CorrectedAgeBlindPolicy:  # name may vary
    def order(self, ...) -> int: ...
```

## Open questions

- [ ] Exact outdating-correction estimator parameters — freeze against CTL-05 notes in qa fixtures.

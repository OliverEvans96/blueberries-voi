# T-032 CTL-05 ladder + ENG-04 M2 gates

## Context

CTL-05=A full five-point ladder must run with tuned α (T-029). ENG-04 automates β=1 degeneracy, CRN
desync, and DP certificate gates at the M2 boundary.

## Acceptance criteria

- [ ] A ladder runner evaluates five points: **constant → corrected age-blind (Rung 0) → SW (ρ) →
      SW+rollout → toy DP**, writing numeric results under `experiments/` and/or `figures/m2/` (not
      inside `controller/`).
- [ ] Ladder profit claims **require** the T-029 tuned-α artifact; missing artifact fails the harness.
- [ ] Automated **β=1 degeneracy** test: age-aware and age-blind coincide when \(w\) is constant
      (CI red if broken).
- [ ] Automated **CRN desync** test from T-030 is wired into the M2 gate set (CI red if broken).
- [ ] Automated **DP certificate** report/assertion from T-031 is wired (CI red if broken / missing).
- [ ] Production backend remains `mean_field` (no silent joint revert in ladder config).
- [ ] `uv run pytest` includes these gates; no new runtime dependencies.

## Out of scope

- Multi-scenario MD narrative (T-033)
- M3 VOI sweep tables
- Plotly / ENG-01

## Interfaces

```text
run_m2_ladder(*, alpha_table_path, root_seed, ...) -> LadderResult
# CI tests: test_beta1_degeneracy, test_crn_desync_gate, test_dp_certificate_gate
```

## Open questions

- [ ] Whether gates live under `tests/controller/` vs `tests/gates/m2/` — prefer mirroring package
      layout unless a shared gates package already exists.

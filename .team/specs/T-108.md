# T-108 RBPF, rollout, episode/VOI, PyO3 dispatch, wasm-pack

## Context

Follow-on from T-104 kernels (ADR 0119–0121). Batch FFI: filter_step, rollout_order,
run_closed_loop_episode, run_voi_crn_cell. Wire `_core` and wasm worker.

## Acceptance criteria

- [ ] `voi_core::filter_step` updates a counts-only particle bank (exact WOR LL, allocate+spoil, resample); `cargo test` covers finite vs -inf LL and weight sum 1.
- [ ] `rollout_order` returns a nonnegative case multiple; empty-candidate / H<=0 errors; salvage 0 on empty lots.
- [ ] `run_closed_loop_episode` with injected deliveries (no Abdella) returns burn+score days; sales/waste totals finite.
- [ ] `run_voi_crn_cell` returns keys for all 7 VOI scenarios.
- [ ] PyO3 `blueberries_voi._core` exposes batch endpoints; `BLUEBERRIES_VOI_BACKEND=rust` dispatches `day_step` when `_core` is importable, else Python + warn.
- [ ] `packaging/wasm/worker.js` calls wasm-bindgen `handle_rpc`; wasm adapter kind remains `wasm`.
- [ ] Python implementations are not deleted. Rust tests mirror rbpf/rollout/episode/voi slices (not viz/pyodide/fastapi/abdella).

## Out of scope

- Live GitHub Actions YAML; citeable RNG bit-identity; rayon.

## Interfaces

`filter_step`, `rollout_order`, `run_closed_loop_episode`, `run_voi_crn_cell`, `handle_rpc`.

## Open questions

- none

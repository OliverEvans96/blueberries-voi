# T-083 CAL-A4 — Baselines, rollout, M2 gates

## Context

ADR 0109 mandatory re-derive #3 and baseline/burn-in honesty: rollout horizons in multiples of 7,
toy DP / M2 ladder/gates updated for periodic age under MWF, burn-in notes under periodic steady
state.

## Acceptance criteria

- [ ] Rollout horizon sweep / defaults step in **multiples of 7** calendar days (not arbitrary daily
      lengths that break weekly periodicity); tests lock H ∈ {7, 14, …} for production presets.
- [ ] `toy_dp` (or certificate path) documents and uses schedule-aware protection / decision epochs
      consistent with order days (no silent daily-order assumption in the default certificate).
- [ ] M2 ladder / `m2_gates` thresholds or scenarios updated so gates run under default
      `OrderSchedule` (orders only on Sun/Tue/Thu); gate tests pass or are explicitly retuned with
      recorded new thresholds.
- [ ] Burn-in / episode structure notes or code comments (and a test where applicable) acknowledge
      **periodic** age under MWF rather than daily stationary burn-in.
- [ ] Age-blind / Rung0 paths used in gates consume day-indexed weights from T-081.
- [ ] Any closeout/guard test that asserted daily `PROTECTION_DEMAND_DAYS=2` as the immutable base
      case is updated or gated **in this ticket**.
- [ ] Focused pytest for rollout/gates; ruff/mypy on touched paths pass.

## Out of scope

- Full citeable production VOI grid regen (human / overnight; flagged at T-088)
- Web UI
- FreshNet ingest

## Interfaces

```text
# Rollout defaults (illustrative)
DEFAULT_ROLLOUT_HORIZONS: tuple[int, ...]  # multiples of 7
# M2 gates invoke schedule-aware episode runner from T-079
```

## Open questions

- [ ] Exact new M2 gate numeric thresholds — implementer retunes from failing gates and records
      values in gate module / qa note.

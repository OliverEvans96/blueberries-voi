STATUS: PASS

## Commands run
- `cd web && npm ci` → exit 0, 122 packages installed from lockfile
- `cd web && npm test` (`vitest run`) → exit 0, 13 files / 126 tests passed (incl. `autopilotLoop.test.ts` 11/11)
- `cd web && npx tsc --noEmit -p .` → exit 0, clean

## Acceptance criteria
- [x] Module `web/src/autopilotLoop.ts` exports play/pause API: await `act`, apply DayDelta, schedule `max(0, intervalMs - elapsed)`, single-flight — verified by vitest `autopilotLoop.test.ts` (exports, single-flight, schedule delay + clamp-to-0, pause clears timer)
- [x] Default `intervalMs` 500 for `damped_sw`/`constant`, 1000 for `rollout`; user `getIntervalMs` wins — verified by vitest `defaultIntervalMsForPolicy` + user-override scheduling case; controls wire policy change via `defaultIntervalMsForPolicy`
- [x] Play chrome exposes Autopilot Play / Pause; play runs loop; pause clears timer — verified by vitest Play chrome source contracts + pause-clears-timer case; `main.ts` wires `createAutopilotLoop` / `onAutopilotPlay` / `onAutopilotPause`
- [x] While Autopilot running, Advance is disabled (UI hint documents pick) — verified by vitest (`Advance is disabled` + `btnAdvance.disabled = running` in `controls.ts`)
- [x] After each successful Autopilot `act`, order slider/inputs sync from `day.order_qty` — verified by vitest `onTick` order_qty case; `main.ts` `applyDelta`/`onTick` set `orderQty` + `playChromeApi.update`
- [x] Autopilot pauses on `config_dirty` and on `act` reject/throw (error via studio reporting) — verified by vitest dirty + onError cases; `main.ts` `onError` → `reportStudioAdapterError`, dirty pause in loop + chrome sync
- [x] Vitest covers single-flight, delay math, pause on error, pause when dirty, order sync — verified by full `npm test` green including all T-100 suites above

## Incomplete
- (none)

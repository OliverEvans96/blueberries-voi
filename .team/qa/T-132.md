# T-132 verify

**Verdict:** PASS

**Python:** 3.11

```text
ruff check .          — pass
ruff format --check . — pass
mypy src tests        — pass
pytest -n auto --cov=blueberries_voi --cov-branch --cov-fail-under=80 — 539 passed
```

**Rust:** `maturin develop` + `tests/test_protection_quantile_mc.py` parity test pass.

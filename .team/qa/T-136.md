STATUS: FAIL

## Commands run

- `python3.11 -m venv .venv && uv pip install pip` → exit 0, bootstrapped venv (system Python 3.11 is uv-managed / PEP 668; bare `pip install` blocked)
- `python -m pip install --upgrade pip` (in `.venv`) → exit 0, pip 26.2.1
- `pip install -e ".[dev]"` (in `.venv`, Python 3.11.13) → exit 0, editable install ok
- `ruff check .` → exit 0, all checks passed
- `ruff format --check .` → exit 1, `tests/test_alpha_tune_rust_calendar_episode.py` would be reformatted (1 file)
- `mypy src tests` → exit 0, no issues in 155 source files
- `pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` → exit 1, 30 failed / 498 passed / 115 skipped; coverage 79% < 80% fail-under; failures dominated by `blueberries_voi._core` not built (`EngineSession requires BLUEBERRIES_VOI_BACKEND=rust and blueberries_voi._core`)
- `cargo test -p voi_core` → exit 101, 145 passed / 1 failed: `voi::tests::p1_and_f2_scenario_masks_differ` (`assertion failed: f2.age_at_receipt`)
- `cargo test -p voi_core --test unit_pf_ac` (supplemental, ticket AC suite) → exit 0, 21/21 passed

## Acceptance criteria

- [x] `p1_totals_loglik` and `loglik_sales_by_units` take no `rng` parameter — verified by `unit_ll.rs` signatures and `p1_totals_loglik_signature_has_no_rng` / `p1_totals_loglik_deterministic_no_path_mc_in_body` (`cargo test -p voi_core --test unit_pf_ac`)
- [x] `sequential_kernel_path_logprob` takes `freshness: &mut [f64]` and zeroes picked slots; `score_particle` takes `&mut [f64]`, applies unscored WOR removal after finite likelihood — verified by `sequential_kernel_path_logprob_mutates_freshness`, `score_particle_mutates_freshness_after_finite_p1_ll`, and `unit_pf.rs` code
- [x] `filter_step_unit` scoring loop passes `&mut bank.freshness[p]` through `score_particle` — verified by `unit_pf.rs:182–188` and green filter/router tests
- [x] `sequential_kernel_path_logprob_feasible_finite` updated for mutable freshness — verified by `sequential_kernel_path_logprob_feasible_finite` (Rust + Python)
- [x] `p1_totals_loglik_impossible_sales_neg_inf` updated (no `rng` arg) — verified by `p1_totals_loglik_impossible_sales_neg_inf` / `test_p1_totals_loglik_impossible_sales_is_neg_inf`
- [x] `unit_pf_l20_scripted_mean_f_mae_and_order_match` re-baselined — verified by `unit_pf_l20_scripted_mean_f_mae_and_order_match` and `test_scripted_l20_mean_f_mae_under_threshold`
- [x] F1-vs-P1 relative MAE (`unit_pf_f1_p1_relative_mean_f_mae`) — verified by Rust + Python tests
- [x] heterogeneous two-lot F1 strictly beats P1 (`unit_pf_f1_strictly_beats_p1_heterogeneous_lots`) — verified by Rust + Python tests
- [x] exact-path enumeration spot check (`multinomial_vs_exact_wor_split_small_l`) — verified by Rust + Python tests
- [x] Monte Carlo distributional comparison (`multinomial_vs_wor_mc_realistic_l`) — verified by Rust + Python tests
- [x] `tests/test_unit_pf.py` hand-reference docs and `_cargo_unit_pf_ac(...)` wiring — verified by `test_cargo_unit_pf_ac_integration_suite_green` and 20/20 non-skipped Python unit-pf tests
- [x] `experiments/c2_a_totals_study.md` regenerated — file present with ADR 0135 post-baseline notes (commit `d03419e`)
- [x] ADR 0135 committed — present at `.team/adr/0135-unify-p1-f1-sales-likelihood.md` (commit `fecdea2`)
- [ ] `.team/changelog.md` plain-English entry on verify PASS — not written (verify FAIL; no entry expected until PASS)

## Incomplete

- Full CI parity gate block does not exit clean: format check failure, pytest failures/coverage shortfall (`_core` extension not built in verify env), and one `voi_core` lib test regression (`p1_and_f2_scenario_masks_differ`).
- Changelog entry deferred pending PASS (per spec AC).

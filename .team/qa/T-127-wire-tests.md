# T-127 — RED test map (qa-wire)

## AC-wire

- adapter.ts optional methods → `web/src/engine/wasmAdapter.tradeoff.test.ts`
- wasmAdapter + mockAdapter implementations → same file
- smoke_wasm.mjs tradeoff_forecast + events → same file + `scripts/smoke_wasm.mjs`
- Events envelope `{ days: [...] }` locked → `::"events envelope is { days: [...] }"`

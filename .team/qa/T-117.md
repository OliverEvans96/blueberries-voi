STATUS: PASS
TICKET: T-117
DATE: 2026-08-15

## Commands

```
cargo test -p voi_core
cargo test -p voi_core --lib
```

Python 3.11 live CI argv **not run**: ticket diff vs `main` has no `src/blueberries_voi/` production edits (only `crates/voi_py` Default fill).

## Results

- `cargo test -p voi_core --lib`: **85 passed**, 0 failed
- `cargo test -p voi_core` doc-tests: **0** (ok)
- Session AC: `f2_belief_differs_from_p0_live_lots_unchanged`, `f2a_age_mass_narrower_than_p1`, `p0_vs_p1_belief_differs_after_waste`, `f1_vs_p1_belief_differs_after_uneven_sales`, `catch_up_f2_matches_never_switched_and_not_oracle` — all green on `team/T-117/implement` (`7b8fc1a` plus this verify commit)

## Notes

- `main` was merged into `team/T-117/implement` (not the reverse).
- Remaining red tests: **none** on `voi_core`.

# T-034 QA — M2 close-out

DATE: 2026-08-12
STATUS: RED

## Spec under test

`.team/specs/T-034.md` + M2 plan §5 Definition of done
(`.team/plans/M2-controller.md`)

## AC locks (process / non-goals)

| Lock | Frozen value | Rationale |
| --- | --- | --- |
| M2 tickets | T-023–T-033 | Close-out scope (implement waves) |
| QA records | `STATUS: PASS` (or needs-human) | Spec AC |
| Reviews | `STATUS: APPROVED` | Spec AC |
| Changelog | Plain-English **M2 close-out summary** (ladder + multi-scenario; no $ VOI) | Client voice |
| `voi/` | `__all__ == []`; no extra modules | M3 owns VOI |
| Browser packaging | Explicit non-claim; no Pyodide/ENG-01/WASM ship | Spec AC / backlog park |
| Production backend | `PRODUCTION_BACKEND == "mean_field"` | T-021 / ADR 0091 |
| Runtime deps | `{matplotlib, numpy, pyarrow, scipy}` | ADRs 0084/0085; no M2 dep without ADR |
| Controller | Pure library (no matplotlib/pyarrow/plotly) | M2 agent brief |
| Plan status | COMPLETE / waves done | Open question preference |
| Backlog | M2 complete pending human merge to main | Spec out of scope: merge |

## Commands run (qa RED check — ticket tests only)

| Command | Result |
| --- | --- |
| `uv run pytest tests/test_m2_closeout.py -v --no-cov` | **6 failed, 28 passed** (close-out docs / plan status / backlog / DoD checklist / T-034 AC unchecked; non-goal locks already pass) |

Full-suite / coverage ≥80% deferred to implement + verifier (per ticket start: do not run full pytest).

## Coverage of acceptance criteria

- `.team/qa/` records PASS for M2 tickets T-023–T-033
  → `tests/test_m2_closeout.py::test_m2_ticket_qa_record_green` — expect PASS on verify tip (records already green)

- `.team/reviews/` shows APPROVED for M2 implement tips
  → `tests/test_m2_closeout.py::test_m2_ticket_review_approved` — expect PASS on verify tip

- `.team/changelog.md` has a plain-English M2 entry (CTL ladder + multi-scenario; no VOI dollar headlines)
  → `tests/test_m2_closeout.py::test_changelog_has_m2_client_voice_summary` — currently failing: no dedicated M2 close-out summary heading

- `voi/` package remains a stub
  → `tests/test_m2_closeout.py::test_voi_package_remains_stub` — expect PASS

- Close-out checklist asserts **no** Pyodide packaging / ENG-01 browser demo / WASM ship claims
  → `tests/test_m2_closeout.py::test_closeout_asserts_no_pyodide_packaging_ship_claims` — currently failing: missing close-out surface / explicit non-claim
  → `tests/test_m2_closeout.py::test_no_browser_or_pyodide_packaging_modules_in_src` — expect PASS

- Production `PRODUCTION_BACKEND == "mean_field"` still holds
  → `tests/test_m2_closeout.py::test_production_backend_remains_mean_field` — expect PASS

- No new runtime dependencies introduced by M2 without a dedicated ADR
  → `tests/test_m2_closeout.py::test_no_new_runtime_dependencies_for_m2_closeout` — expect PASS
  → `tests/test_m2_closeout.py::test_controller_remains_pure_library` — expect PASS (purity lock)

- Plan status / Wave 7 complete + backlog pending human merge (preferred close-out)
  → `tests/test_m2_closeout.py::test_m2_plan_status_waves_complete` — currently failing: plan still Status PLAN
  → `tests/test_m2_closeout.py::test_backlog_m2_complete_pending_human_merge` — currently failing: backlog still Next T-034
  → `tests/test_m2_closeout.py::test_m2_dod_checklist_copied_and_checked` — currently failing: no `.team/reviews/M2.md` DoD note
  → `tests/test_m2_closeout.py::test_plan_section_5_dod_still_present` — expect PASS
  → `tests/test_m2_closeout.py::test_t034_spec_acceptance_criteria_checked` — currently failing: T-034 AC unchecked

## Not covered by tests

- Full AGENTS.md toolchain (ruff / mypy / full pytest + coverage ≥80%) — verifier gate.
- Human merge to `main` — explicitly out of scope.
- Starting M3 VOI / reopening ENG-01 — out of scope.

## Incomplete

(none — STATUS RED pending implement)

STATUS: PASS

## Commands run (Python 3.11, CI-identical argv)

Worktree: `/home/oliver/blog/blueberries-voi/.worktrees/T-150-recal`
Branch: `team/T-150/recal` (verify ran on `8eb8a229`; this file is the PASS record)

| Gate | Command | Result | Wall-clock |
|------|---------|--------|------------|
| ruff check | `uv run --python 3.11 ruff check .` | exit 0 | 1 s |
| ruff format | `uv run --python 3.11 ruff format --check .` | exit 0 | <1 s |
| mypy | `uv run --python 3.11 mypy src tests` | exit 0 | 1 s |
| pytest | `uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` | exit 0, **621 passed**, 82 skipped, 2 xfailed, 2 xpassed; coverage **80.90%** | **215 s** (3 min 35 s) |
| cargo | `cargo test --release -p voi_core -p voi_py --locked` | exit 0 (includes `ac2_11a_empirical_ladder_tracking_mae`) | **246 s** (4 min 6 s, compile + test) |

**Total verify: ~7 min 44 s** (target 10–20 min). Earlier mis-runs were 34+ min pytest and ~23 min cargo.

## What made verify slow, and what changed

Kept the ladder falsifier. Did **not** `#[ignore]` `ac2_11a_empirical_ladder_tracking_mae` (n=64, 20 deliveries). It ran in the T-150 phase-2 binary (~13 s of that crate’s 12.9 s release suite). `ac2_11a_n_scaling_diagnostic` stays `#[ignore]` (one-off; numbers in `.team/qa/T-150-tests.md`).

| Was | Change |
|-----|--------|
| `session_lot_counts_track_arrivals_minus_decay` — 201×20-day sessions (~13 min) | 8 seeds (~20 s). Conservation check kept. |
| Rust/Python VOI CRN “search 1..200 seeds” with `n_rollout_paths=2` | Cap 8 seeds; structural cells use production **damped_sw** (`n_rollout_paths=0`). Rollout-specific tests kept. |
| `test_t131` slow bakeoff H=28 P=8 | CI smoke H=4 P=2 (production policy is damped_sw). Ranking fixture left at the params that still flip. |
| Alpha-tune “full run” 28+28 days | 8+8 days; alpha monotone still asserted. |
| `unit_pf_ac` birth count used `f > 0` | Count row length (ADR 0144 atom is a born unit). |
| Snapshot rejected `arrival_summary.curve[].density` | Allow that PDF key under `arrival_summary` only; heatmap `belief.density` still banned. |
| Stale `packaging/wasm/worker.js` asserts | Point at `web/src/engine/wasmWorker.ts` (T-144). |
| `modal` optional extra vs T-125 extras lock | Treat `modal` like `freshnet` (ops/batch extra). Pytest never calls live Modal. |

## Path L / notebooks

- **GSIN 17-min regen:** skipped (not blocking). `gsin_upc_after.json` remains T-141 epoch.
- **Modal nb13:** done earlier — run `ap-n9eIOGAYfnAVTUHuGyfsjw`, `experiments/data/nb13_channel_rows.json`.
- **nb14:** executed on this branch (cheap JSON + plots).
- **Physics epoch:** `data/abdella/.t150_physics_epoch` present.
- **Changelog:** 2026-08-23 entry (uncertain / corridor / shelf life / upper bound / field heat).
- **Remodel report:** `.team/reports/T-150-arrival-remodel.md` — current behaviour; Abdella anchors assumed families, does not prove them.

## Acceptance (this tip)

- AC2.11a ladder: cargo release green (`ac2_11a_empirical_ladder_tracking_mae`).
- AC3.6–3.8: notebooks revisited, changelog, CI-parity gate PASS.
- No merge to `main`. No `.github/workflows/` edits.

## Deferred (documented, not deleted)

- Full 28+28 alpha-tune BO horizon (notebook still owns that).
- H=28 P=8 rollout vs damped_sw bakeoff (research; production is `n_rollout_paths=0`).
- 200-seed “search until profits differ” loops (8 seeds + damped_sw is enough for the structural mask claim).
- Live GSIN regen and local 36×30 nb13 grid (Modal path used instead).

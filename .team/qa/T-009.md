# T-009 Rich DayLog / SIM-04 emit — QA

DATE: 2026-08-12
STATUS: PASS

Behavioural acceptance criteria verified green. Full-repo `ruff check .` / full `pytest`
remain red only for **out-of-wave** blockers (experiments lint; T-011 RED). See
[W1-verify.md](./W1-verify.md).

## Commands run

- `uv sync --all-extras` → exit 0, packages checked
- `uv run ruff check src tests` → exit 0, all checks passed
- `uv run ruff format --check src tests` → exit 0, already formatted
- `uv run mypy src tests` → exit 0, no issues in 26 files
- `uv run pytest` → exit 1 (9 T-011 failures in `test_mc_likelihood.py`); **T-009 `tests/test_sim.py` 10 passed**; coverage **86.02%**
- `uv run ruff check .` → exit 1, pre-existing `experiments/fil11_a_scenarios.py` only (E501, RUF001)

## Acceptance criteria

- [x] After `run_episode`, each `DayLog` includes per-lot sales/waste maps keyed by `lot_id` — verified by `tests/test_sim.py::test_daylog_sales_waste_by_lot_maps`
- [x] Each `DayLog` records end-of-day live lot state with `n`, `tau`, `lot_id` — verified by `test_daylog_lots_keep_n_tau_lot_id`
- [x] Delivery days expose `age_at_receipt` / `pack_date`; non-delivery `None` — verified by `test_daylog_receipt_metadata_delivery_vs_none`
- [x] Totals consistent with by-lot sums; empty maps when zero — verified by `test_daylog_totals_match_by_lot_sums`
- [x] Shared CRN scored aggregates stable — verified by `test_daylog_crn_scored_aggregates_stable`
- [x] Filter/sim still share `model.day_step` (ENG-02) — verified by `test_sim_shares_day_step`
- [x] `mypy src tests` + `ruff` on `src`/`tests` green; coverage ≥80% on full suite — verified by mypy/ruff scoped runs + pytest coverage 86.02% (full `ruff check .` / full pytest still blocked out-of-wave; see W1-verify)

## Incomplete

- None for T-009 behaviour. Repo-wide green waits on experiments cleanup + T-011.

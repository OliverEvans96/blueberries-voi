# T-139 verify

Verdict: **PASS**

Commit: `113f3d3` (`team/T-139/implement`)

Environment: Python **3.11**, `uv sync --all-extras --python 3.11`, `uv run maturin develop --release -m crates/voi_py/Cargo.toml`.

## Gates

| Gate | Result | Detail |
|------|--------|--------|
| `cargo test -p voi_core` | **PASS** | lib + integration tests green (incl. AC-12 F3 drift guard) |
| `ruff check .` | **PASS** | All checks passed |
| `ruff format --check .` | **PASS** | 200 files formatted |
| `mypy src tests` | **PASS** | 159 source files, no issues |
| `pytest -n auto --cov=blueberries_voi --cov-branch --cov-fail-under=80` | **PASS** | **588 passed**, 81 skipped, 2 xfailed, 1 xpassed; **TOTAL 82%** |

## T-139 AC closure

- **AC-1/2:** F3 restored in AC-12 drift guard; root cause documented (temperature `f_at_receipt` + GSIN `waste_by` fallback under dispersion).
- **AC-3/4:** Filter birth mass and session `lot_counts` regression tests green.
- **AC-5:** `contrast_spoilage_weight` exported; wired into filter spoilage log-evidence when `arrival_dispersion_sd > 0` (inert at sd=0).
- **AC-6:** VOI CRN seven-scenario seed-1 profits within `1e-6` of T-138 baseline at sd=0.
- **AC-7:** Coverage **82%** ≥ 80% (closes T-138 remediate gap at 79.45%).

## Notes

- Parent tip: `fa09d07` (T-138 implement PASS @ 80.22%).
- Stage B scope per ADR 0140: F3 count-bias + contrast hook; full GammaDecrementTable / Poisson-binomial waste LL deferred.

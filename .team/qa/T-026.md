# T-026 QA — case_round + constant-order policy

DATE: 2026-08-12
STATUS: PASS

## Spec under test

`.team/specs/T-026.md`

## Rounding mode (locked)

Nearest multiple of `case_size`; halfway ties round **half away from zero**
(for non-negative `x`, toward +∞). Default `case_size=8` matches `ModelParams`.

## Commands run (Wave 1 verify, serial)

| Command | Exit | Result |
| --- | --- | --- |
| `uv sync --all-extras` | 0 | venv + editable install |
| `uv run ruff check .` | 0 | All checks passed |
| `uv run ruff format .` | 0 | formatted / clean |
| `uv run mypy src tests` | 0 | Success: 49 source files |
| `uv run pytest` | 0 | 292 passed, 1 skipped; coverage 88.84% (≥80%) |

Review: `.team/reviews/T-026.md` → APPROVED

## Acceptance criteria

- [x] `case_round(x, case_size)` int multiple with documented rounding — fixture tables in `tests/test_ordering.py`
- [x] `ConstantOrderPolicy` implements Policy surface; fixed case-rounded qty — policy tests
- [x] `controller/` modules have no matplotlib / pyarrow / file writes — purity AST tests
- [x] Package `__all__` exports new symbols — export surface tests
- [x] Ticket tests pass in full suite — 292 passed

## Incomplete

(none)

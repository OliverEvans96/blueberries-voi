# T-026 QA — case_round + constant-order policy

DATE: 2026-08-12
STATUS: RED

## Spec under test

`.team/specs/T-026.md`

## Rounding mode locked by RED fixtures

Nearest multiple of `case_size`; halfway ties round **half away from zero**
(for non-negative `x`, toward +∞ / the larger multiple). Default `case_size=8`
matches `ModelParams.case_size`. Module docstring must document nearest + tie
rule.

## Tests

`tests/test_ordering.py` — expected to fail until `controller/ordering.py` and
package exports exist (assertion failures via resolve helpers, not typos).

**Do not run pytest in parallel Wave-1 lanes** (orchestrator instruction).

## Coverage of acceptance criteria

- `case_round(x, case_size)` returns an int multiple of `case_size` with
  documented rounding mode (fixture table; default `case_size=8` matches
  `ModelParams`)
  → `tests/test_ordering.py::test_case_round_fixture_table_default_case_size_matches_model_params`
  — currently failing: `blueberries_voi.controller.ordering` missing
  → `tests/test_ordering.py::test_case_round_uses_model_params_default_when_case_size_omitted`
  — currently failing: same
  → `tests/test_ordering.py::test_case_round_fixture_table_case_size_4`
  — currently failing: same
  → `tests/test_ordering.py::test_case_round_result_always_non_negative_multiple`
  — currently failing: same
  → `tests/test_ordering.py::test_case_round_rejects_non_positive_case_size`
  — currently failing: same
  → `tests/test_ordering.py::test_case_round_rejects_negative_x`
  — currently failing: same
  → `tests/test_ordering.py::test_ordering_module_docstring_documents_nearest_and_tie_rule`
  — currently failing: same

- `ConstantOrderPolicy` implements the T-024 `Policy` surface and always returns
  a fixed case-rounded quantity
  → `tests/test_ordering.py::test_constant_order_policy_returns_fixed_case_rounded_quantity`
  — currently failing: `ConstantOrderPolicy` missing
  → `tests/test_ordering.py::test_constant_order_policy_default_case_size_is_eight`
  — currently failing: same
  → `tests/test_ordering.py::test_constant_order_policy_already_multiple_unchanged`
  — currently failing: same
  → `tests/test_ordering.py::test_constant_order_policy_zero_orders_zero`
  — currently failing: same
  → `tests/test_ordering.py::test_constant_order_policy_order_signature_accepts_belief_day_pending`
  — currently failing: same

- `controller/` modules introduced here do **not** import matplotlib, pyarrow,
  or write files
  → `tests/test_ordering.py::test_controller_ordering_has_no_matplotlib_pyarrow_or_file_writes`
  — currently failing: `ordering.py` absent (AST purity check once present)

- Package export surface lists the new symbols (non-empty `__all__`)
  → `tests/test_ordering.py::test_controller_package_exports_case_round_and_constant_order_policy`
  — currently failing: symbols / `__all__` not updated

- `uv run pytest` for this ticket’s tests passes
  — deferred to implement / verify (not run in this qa worktree)

## Not covered by tests

- Full-suite green / coverage ≥80% / ruff / mypy — because post-implementation
  CI bar; verify by `uv run ruff check . && uv run mypy src tests && uv run pytest`
- Runtime isinstance against a T-024 `Policy` class object — because T-024 is a
  parallel Wave-1 lane; RED locks the structural `order(belief, *, day,
  pending_orders)` surface ConstantOrderPolicy must satisfy
- Survival-weighted / Rung 0 policies, profit accounting, browser façade — out
  of scope per spec

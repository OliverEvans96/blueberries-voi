# QA — M1 summary (T-001 … T-007)

DATE: 2026-08-12
STATUS: GREEN (with documented Stage A gate FAIL; B/C diagnostic post-fail)

## Scope

Milestone M1 tickets T-001 through T-007: scaffold, shared model kernels, Gate 0 /
Abdella traces, forward sim, FIL-13 bakeoff, production RBPF, FIL-11 Stage A,
plus Oliver-requested Stage B/C diagnostics after A fail.

## Commands run

```bash
uv sync --all-extras
uv run ruff check .
uv run ruff format .
uv run mypy src tests
uv run pytest
uv run python experiments/fil11_b.py
uv run python experiments/fil11_c.py
```

## Results

| Gate | Result |
| --- | --- |
| Ruff check / format | pass (re-checked with B/C) |
| Mypy (strict, src + tests) | pass |
| Pytest | pass |
| Coverage (branch) | ≥80% |

## Acceptance notes by ticket

- **T-001 … T-004:** package layout, kernels, Gate 0, arrival/sim logging — covered by
  automated tests; Gate 0 figures under `figures/m1/`.
- **T-005:** bakeoff table in `experiments/fil13_bakeoff.md`; empirical L p50=2 / p90=3 /
  max=3; `full_joint` OOMs at L≥6 (K=8, N=200). Synced with ADR 0082 = **E**.
- **T-006:** `PRODUCTION_BACKEND=full_joint`; FIL-15 numerics locked (ADR 0083).
- **T-007 Stage A:** **FAIL** (see `experiments/fil11_stage_a_result.md`). Hard stop per
  FIL-11=D — Stages B/C are **not** M1 gate deliverables after A fail.
- **T-007 Stage B/C (diagnostic, Oliver request):** run post-A-fail as evidence only.
  - B: R=50, N=2000, K=8, full_joint → coverage_90=1.00 (outside [0.70, 0.99]),
    rank_mean≈0.86, rank_std≈0.20 → **FAIL diagnostic criteria** (underconfident /
    non-informative vs truth). Figure `figures/m1/fil11_calibration.png`.
  - C: L∈{2,3}, K=4, full_joint vs exact → TV≈0 (<0.05) → **PASS diagnostic criteria**.
    Figure `figures/m1/fil11_exact.png`.
  - Does **not** reopen the Stage A gate or claim “filter recovers truth”.

## Verdict

QA GREEN for M1 wrap: quality gates pass; Stage A negative remains the gate outcome;
B/C diagnostics recorded at Oliver’s request without elevating them to pass criteria.

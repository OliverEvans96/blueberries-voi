# T-057 QA — Wire studio to real adapters (RED)

DATE: 2026-08-12  
STATUS: **RED**  
ROLE: qa  
BRANCH: `team/T-057/qa`  
WORKTREE: `.worktrees/T-057-qa`

## Parents merged

| Parent | Branch | Tip |
|--------|--------|-----|
| PyodideAdapter | `team/T-055/implement` | `fce32e979a8e643175e4f4b29601ceda05d130a1` |
| HttpAdapter | `team/T-056/implement` | `7c3f5f952f8a3153660e4fac0e38e77a50faa1b8` |

Merge commit on this branch: `T-057 (qa): merge T-056 HttpAdapter into T-055 tip`
(`uv.lock` regenerated after conflict).

## Spec

- `.team/specs/T-057.md`

## Vitest invocation (RED evidence)

```bash
cd .worktrees/T-057-qa/web
npm install
npm test -- --reporter=verbose src/engine/studioWiring.test.ts
```

Result: **12 failed**, **4 passed** — all failures are `AssertionError` for missing
studio wiring / adapter selection / smoke checklist (not import or collection errors).

Passing today (already true on the T-054 tip; keep green):

- Advance / Reset / bootstrap already call `adapter.step` / `reset` / `init` + projector
- Economics chrome already calls `projector.setEconomics` only
- `createStudioAdapter({ kind: "mock" })` RED stub returns `MockAdapter`
- `ViewModelProjector.setEconomics` is local (no EngineAdapter required)

## Coverage of acceptance criteria

See `.team/qa/T-057-tests.md` for the criterion → test mapping.

## Out of scope (not tested here)

- Redesigning chart aesthetics
- Honesty / VOI misspecification UI
- Changing HttpAdapter / PyodideAdapter internals (T-055 / T-056)

## Notes for implementer

1. Fill in `web/src/engine/studioAdapter.ts`:
   - `resolveStudioAdapterKind(env)` — `VITE_ENGINE_ADAPTER` override; else
     production → `pyodide`; development with API base URL → `http`; `mock` only when
     explicit.
   - `createStudioAdapter(opts)` — construct `HttpAdapter` / `PyodideAdapter` /
     `MockAdapter` from kind + URLs / `fetch`.
2. Wire `web/src/main.ts` through `createStudioAdapter` (remove unconditional
   `new MockAdapter()`).
3. Keep `onEconomicsChange` → `projector.setEconomics` only (no fetch / worker / adapter).
4. Document env flag names + a short pass/fail smoke in
   `.team/qa/T-057-smoke.md` (or `web/README.md`).
5. Do **not** put `generate.ts` `stepSimulation` on the default Advance path.

## New files

- `web/src/engine/studioAdapter.ts` (RED stub)
- `web/src/engine/studioWiring.test.ts`
- `.team/qa/T-057.md`
- `.team/qa/T-057-tests.md`

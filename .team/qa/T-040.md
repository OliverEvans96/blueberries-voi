# T-040 QA — Smoke artifact + beta=1 gate

DATE: 2026-08-12
STATUS: PASS

## Spec

`.team/specs/T-040.md`

## Evidence

`run_voi_smoke` writes experiment note; `assert_beta_one_voi_near_zero` enforced;
`viz.voi.plot_voi_vs_beta` writes PNG without matplotlib import in `voi.metric`.

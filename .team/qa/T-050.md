# T-050 QA — ASGI app wrapping EngineSession (RED)

DATE: 2026-08-12  
STATUS: **RED**  
ROLE: qa  
BRANCH: `team/T-050/qa`  
WORKTREE: `.worktrees/T-050-qa`  
PARENT: `team/T-045/implement` @ `8d80f52e4fc51f0ff332329423f26b161ff8a270`

## Spec / ADRs

- `.team/specs/T-050.md`
- Route table: `.team/specs/T-049.md` Interfaces
- ADR [0100](../adr/0100-eng-01-api-asgi-session.md) (ASGI sessions)
- ADR [0098](../adr/0098-simulator-export-contract.md) (Snapshot / DayDelta)
- Schema validators: `blueberries_voi.simulator.schema` (T-045)

## Pytest invocation (RED evidence)

```bash
cd .worktrees/T-050-qa
uv sync --all-extras
uv run pytest tests/test_t050_asgi_api.py \
  -o addopts='-ra --strict-markers --strict-config' -v
```

Result: **15 failed**, **0 passed** — missing `[api]` extra, missing
`blueberries_voi.api:app`, and missing `src/blueberries_voi/api/` package
(not collection/import typos in the test file).

## Coverage of acceptance criteria

See `.team/qa/T-050-tests.md` for the criterion → test mapping.

## Out of scope (not tested here)

- D3 HttpAdapter (T-056)
- Pyodide worker changes (T-047)
- VOI / episode batch endpoints
- Full OpenAPI schema parity contract suite (T-051) — this ticket only asserts
  OpenAPI is available and covers session route names
- Production ASGI implementation (implementer)

## Notes for implementer

- Add optional-dependencies `[api]` with FastAPI **or** Starlette (+ httpx for
  TestClient as needed).
- Land `src/blueberries_voi/api/` exporting `app` (`blueberries_voi.api:app`).
- Session store: in-process `session_id` → `EngineSession` (no TTL required for
  local dev; document non-production).
- Routes per T-049; hydrate JSON shipment dicts into `ShipmentTrace` on init.
- Reuse `validate_snapshot` / `validate_day_delta`; never return ViewModel /
  PnL / economics / ghost / heatmap.
- Do not import matplotlib from the API package.
- Do **not** touch T-047.

## New files

- `tests/test_t050_asgi_api.py`
- `.team/qa/T-050.md`
- `.team/qa/T-050-tests.md`

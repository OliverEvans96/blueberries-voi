STATUS: PASS

Re-verify on `de69c12` after guided paths fix (`team/T-126/merge-implement`).

## Commands run

- `cd web && npm test` → exit 0, 45 test files / 320 tests passed (vitest run)
- `cd web && npx tsc --noEmit` → exit 0, no TypeScript errors

## Acceptance criteria

- [x] AC-hatch — `.chart-unavailable-hatch` no longer blocks clicks — verified by `web/src/react/ChartUnavailable.test.ts` (7 tests) in `npm test`
- [x] AC-obschip — observation-ladder chip active state stays in sync — verified by `web/src/engine/projector.obsScenarioSync.test.ts` (4 tests) in `npm test`
- [x] AC-tabs — `ChapterTabs` horizontal command bar — verified by `web/src/react/ChapterTabs.test.ts` (4 tests) in `npm test`
- [x] AC-refdrawer — consolidated `ReferenceDrawer` — verified by `web/src/react/ReferenceDrawer.test.ts` (16 tests) in `npm test`
- [x] AC-dayinspector — floating hover tooltip — verified by `web/src/react/DayInspector.test.ts` (6 tests) and `web/src/hoverLink.test.ts` (2 tests) in `npm test`
- [x] AC-storetabs — `StoreChartTabs` for the four store charts — verified by `web/src/react/StoreChartTabs.test.ts` (7 tests) in `npm test`
- [x] AC-merge — integration: two-pane grid, self-sized rail, component wiring — verified by merge-level tests in `npm test` (`sections.controller.test.ts`, `main.stockout.test.ts`, `studioScenarios.test.ts`, `DecisionRail.test.ts`, and related suite)
- [x] AC-verify — integration gate (`cd web && npm test`) — verified by full vitest run (320 passed)
- [x] No new mypy/ruff/pytest gates affected (web-only ticket) — verified by scope; Python CI gates not re-run for this re-verify

## Incomplete

- None

# T-161 verify

**Verdict:** PASS

**Date:** 2026-08-24

## Gates (Python 3.11, CI-parity)

```bash
uv sync --all-extras --python 3.11
uv run --python 3.11 ruff check .
uv run --python 3.11 ruff format --check .
uv run --python 3.11 mypy src tests
uv tool run maturin develop --release  # crates/voi_py — rebuild _core
uv run --python 3.11 pytest -n auto -m "not docs" \
  --cov=blueberries_voi --cov-branch \
  --cov-report=term-missing --cov-report=xml --cov-fail-under=80
```

## Results

| Gate | Result |
|------|--------|
| ruff check | PASS |
| ruff format --check | PASS |
| mypy src tests | PASS |
| pytest (not docs) + cov ≥80% | PASS — 644 passed, 81 skipped |

## Verify follow-up fixes (post-implement)

- Ruff: `StrEnum`, import order, `__all__` sort in `bakeoff_rollout.py` / `constants.py`
- Studio semver: `web/package.json` 0.3.10 → 0.3.11 (publishable `crates/voi_core/` change)
- `tests/test_studio_release_version.py` pin updated to 0.3.11

## Notes

- Default candidate search remains `neighborhood`; `stratified_wide` is opt-in.
- Ablation script not run; default unchanged per spec.
- Rust unit tests: `cargo test -p voi_core rollout::` — 10 passed (implement handoff).

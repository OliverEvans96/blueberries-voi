# T-025 QA — Day profit helper (SIM-01=B)

DATE: 2026-08-12
STATUS: RED

## Spec under test

`.team/specs/T-025.md` + ADR 0093

## RED note

Pytest was **not** run in this qa worktree (parallel lanes). Expected failure mode
until implement lands `src/blueberries_voi/sim/profit.py`: `ImportError` /
`ModuleNotFoundError` for `blueberries_voi.sim.profit` on every behavioural
test; dependency lock test may already pass. After the module exists, failures
must become `AssertionError` on formula / scored-window / I/O contracts — not
typos.

## Coverage of acceptance criteria

- `blueberries_voi.sim.profit` day-level helper matches
  `margin * sales - waste_cost * waste - stockout_penalty * max(0, demand - sales)`
  → `tests/test_sim_profit.py::test_day_profit_matches_sim01_b_formula` — currently
  failing: module missing
  → `tests/test_sim_profit.py::test_day_profit_lost_sales_clamped_when_sales_exceed_demand`
  — currently failing: module missing
  → `tests/test_sim_profit.py::test_day_profit_all_zeros_is_zero` — currently
  failing: module missing

- Holding cost is **not** subtracted (SIM-01=B)
  → `tests/test_sim_profit.py::test_day_profit_does_not_subtract_holding_cost` —
  currently failing: module missing

- Helper performs **no** filesystem I/O and imports no matplotlib
  → `tests/test_sim_profit.py::test_profit_module_does_not_import_matplotlib` —
  currently failing: module missing
  → `tests/test_sim_profit.py::test_profit_module_has_no_filesystem_io_calls` —
  currently failing: module missing

- Episode helper sums day profits over **scored** days only (SIM-03 burn-in)
  → `tests/test_sim_profit.py::test_episode_profit_sums_scored_days_only` —
  currently failing: module missing
  → `tests/test_sim_profit.py::test_episode_profit_empty_scored_window_is_zero` —
  currently failing: module missing
  → `tests/test_sim_profit.py::test_episode_profit_n_burn_zero_sums_all_days` —
  currently failing: module missing

- Unit tests cover zero stockout, pure waste, and pure lost-sales cases
  → `tests/test_sim_profit.py::test_day_profit_zero_stockout_when_sales_meet_demand`
  — currently failing: module missing
  → `tests/test_sim_profit.py::test_day_profit_pure_waste_no_sales_no_demand` —
  currently failing: module missing
  → `tests/test_sim_profit.py::test_day_profit_pure_lost_sales_no_waste` —
  currently failing: module missing

- `uv run pytest` for this ticket’s tests passes; no new runtime dependencies
  → `tests/test_sim_profit.py::test_no_new_runtime_dependencies_for_t025` —
  currently expected pass (deps unchanged); full pytest green is implement /
  verifier
  → `tests/test_sim_profit.py::test_profit_costs_is_frozen_dataclass` — currently
  failing: module missing (ADR cost params object)

## Not covered by tests

- Full AGENTS.md quality-gate chain / coverage ≥80% — because verifier owns CI
  bar; verify by `uv run ruff check . && uv run mypy src tests && uv run pytest`
- Default numeric cost constants for desktop experiments — open in spec; tests
  use explicit `ProfitCosts` fixtures
- VOI / α-tuning / experiment artifacts — out of scope per T-025

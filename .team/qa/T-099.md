STATUS: PASS

## Commands run

- `cd web && npm install` → exit 0, 122 packages added
- `cd web && npx vitest run` → exit 0, 11 files / 106 tests passed
- `cd web && npx tsc --noEmit` → exit 0, clean

## Acceptance criteria

- [x] `web/src/sections.ts` includes section `id: "controller"`, label “Controller”, 8th entry in `STUDIO_SECTIONS` (nav key 8) — verified by vitest `sections.controller.test.ts` + source (`STUDIO_SECTIONS[7].id === "controller"`)
- [x] `SectionId` union includes `"controller"`; `loadSection` / `saveSection` accept it; Belief and earlier sections remain intact — verified by vitest section registration / round-trip / Belief regression cases
- [x] Controller controls expose policy chips (`damped_sw` / `rollout` / `constant`), `alpha` / `rho`, rollout budgets (`H`, `n_rollout_paths`, `candidate_case_radius`, `n_particles`), and interval (ms) — verified by vitest controls mount contract + `controls.ts` `data-section="controller"` block
- [x] Chart module `web/src/charts/controllerOrders.ts` renders order quantity over episode days from history (`day.order_qty`) — verified by vitest `controllerOrders.test.ts` series helper + `renderControllerOrders` export
- [x] Controller `plotIds` include controller orders plot id and reuse `plot-inventory` — verified by vitest plotIds case (`plot-controller-orders`, `plot-inventory`)
- [x] `web/src/main.ts` mounts the controller chart when that plot is visible and wires control changes into studio state — verified by vitest main wiring source contract + `renderControllerOrders` / `plotVisible("plot-controller-orders")` / `onControllerChange` controls wiring
- [x] Vitest covers section registration and at least one chart helper / mount smoke — verified by full suite green including T-099 section + chart tests (10/10 ticket tests; 106/106 web)

## Incomplete

(none)

# T-123 verify — Web React mirror

**Verdict:** PASS

**Branch:** `team/T-123/implement` (local; changes may also be on `main` checkout)

**Date:** 2026-08-15

## Commands

```bash
cd web && npm install && npm test
cd web && npm run build
```

## Results

| Gate | Result |
|------|--------|
| `npm test` | PASS — 34 files, **262** tests |
| `npm run build` | PASS — `tsc && vite build` |

## Notes

- Studio runtime moved to `web/src/react/studioLogic.ts`; shell JSX in `StudioLayout.tsx`.
- Source-contract vitest files updated to read `StudioLayout.tsx` / `studioLogic.ts` instead of `main.ts`.
- Play chrome uses React (`PlayChrome.tsx`); section controls use React root + legacy DOM mount for parity.

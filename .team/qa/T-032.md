# T-032 QA — CTL-05 ladder + ENG-04 M2 gates

DATE: 2026-08-12
STATUS: RED

## Spec under test

`.team/specs/T-032.md` + ADR `.team/adr/0062-ctl-05-baseline-ladder.md` (CTL-05=A)
+ ADR `.team/adr/0076-eng-04-test-and-validation-harness-scope.md` (ENG-04=A)
+ T-029 tuned-α gate + T-030 CRN desync + T-031 DP gap

## AC locks (fixture / interface)

| Lock | Frozen value | Rationale |
| --- | --- | --- |
| Ladder points (order) | `constant` → `rung0` → `sw` → `rollout` → `dp` | CTL-05=A / ADR 0062 |
| Runner API | `run_m2_ladder(*, alpha_table_path, root_seed, ...) -> LadderResult` | Spec Interfaces |
| Preferred module | `blueberries_voi.sim.m2_ladder` (fallback `viz.m2_ladder`) | Outside `controller/`; mirrors `sim.alpha_tune` |
| ENG-04 gate module | `blueberries_voi.sim.m2_gates` (or re-export from `m2_ladder`) | Spec open question: mirror package layout |
| CI gate node ids | `test_beta1_degeneracy`, `test_crn_desync_gate`, `test_dp_certificate_gate` | Spec Interfaces |
| Tuned-α gate | T-029 `require_tuned_alpha_table` / `assert_ladder_profit_claim_allowed` | Missing/incomplete table fails harness |
| Artifact roots | `experiments/` and/or `figures/m2/` | Never under `controller/` |
| Production backend | `mean_field` | T-021 / ADR 0091; no silent joint revert |
| Runtime deps | `{matplotlib, numpy, pyarrow, scipy}` | No new deps |

## Commands run (qa RED check — ticket tests only)

| Command | Result |
| --- | --- |
| `uv run pytest tests/test_m2_ladder.py tests/test_m2_gates.py -v` | **18 failed, 3 passed** (purity/deps guards green; ladder + gate APIs missing) |

Full-suite / coverage ≥80% deferred to implement + verifier (per ticket start: do not run full pytest).

## Coverage of acceptance criteria

- A ladder runner evaluates five points: **constant → corrected age-blind (Rung 0) → SW (ρ) → SW+rollout → toy DP**, writing numeric results under `experiments/` and/or `figures/m2/` (not inside `controller/`)
  → `tests/test_m2_ladder.py::test_run_m2_ladder_is_exportable` — currently failing: missing `sim.m2_ladder` / `run_m2_ladder`
  → `tests/test_m2_ladder.py::test_ladder_result_type_is_exportable` — currently failing: missing `LadderResult`
  → `tests/test_m2_ladder.py::test_run_m2_ladder_signature_requires_alpha_table_and_root_seed` — currently failing: missing runner
  → `tests/test_m2_ladder.py::test_run_m2_ladder_evaluates_five_ctl05_points` — currently failing: missing five-point ladder
  → `tests/test_m2_ladder.py::test_ladder_result_records_numeric_profit_per_point` — currently failing: missing profits map
  → `tests/test_m2_ladder.py::test_ladder_default_artifact_path_under_experiments_or_figures_m2` — currently failing: missing DEFAULT path
  → `tests/test_m2_ladder.py::test_run_m2_ladder_writes_artifacts_outside_controller` — currently failing: missing writer
  → `tests/test_m2_ladder.py::test_ladder_runner_does_not_live_in_controller` — currently passing (guard)
  → `tests/test_m2_ladder.py::test_controller_package_has_no_matplotlib_or_parquet_after_ladder` — currently passing (guard)

- Ladder profit claims **require** the T-029 tuned-α artifact; missing artifact fails the harness
  → `tests/test_m2_ladder.py::test_run_m2_ladder_fails_when_tuned_alpha_artifact_missing` — currently failing: missing runner
  → `tests/test_m2_ladder.py::test_run_m2_ladder_fails_when_alpha_table_incomplete` — currently failing: missing runner
  → `tests/test_m2_ladder.py::test_ladder_profit_claim_path_invokes_t029_tuned_alpha_gate` — currently failing: missing module wiring T-029

- Automated **β=1 degeneracy** test: age-aware and age-blind coincide when \(w\) is constant (CI red if broken)
  → `tests/test_m2_gates.py::test_beta1_degeneracy` — currently failing: missing `m2_gates` / `assert_beta1_degeneracy`
  → `tests/test_m2_gates.py::test_beta1_degeneracy_gate_documents_constant_w_contract` — currently failing: missing gate docs

- Automated **CRN desync** test from T-030 is wired into the M2 gate set (CI red if broken)
  → `tests/test_m2_gates.py::test_crn_desync_gate` — currently failing: missing gate wrapping `detect_crn_desync`
  → `tests/test_m2_gates.py::test_crn_desync_gate_fails_when_streams_crossed` — currently failing: missing crossed-stream fail path

- Automated **DP certificate** report/assertion from T-031 is wired (CI red if broken / missing)
  → `tests/test_m2_gates.py::test_dp_certificate_gate` — currently failing: missing DP-gap gate / report
  → `tests/test_m2_gates.py::test_dp_certificate_gate_wires_gap_vs_rollout` — currently failing: missing `gap_vs_rollout` wiring

- Production backend remains `mean_field` (no silent joint revert in ladder config)
  → `tests/test_m2_ladder.py::test_ladder_config_production_backend_is_mean_field` — currently failing: missing ladder backend field
  → `tests/test_m2_ladder.py::test_ladder_module_source_does_not_silently_select_joint` — currently failing: missing module

- `uv run pytest` includes these gates; no new runtime dependencies
  → `tests/test_m2_ladder.py::test_no_new_runtime_dependencies_for_t032` — currently passing (deps lock)
  → ticket test modules collected under default `tests/` layout (verify by full pytest after GREEN)

## Not covered by tests

- Multi-scenario MD narrative — out of scope (T-033).
- M3 VOI sweep tables / Plotly / ENG-01 — out of scope.
- Exact desktop replication counts / published dollar gaps — document at implement; not locked here.
- Full-repo coverage ≥80% — verifier gate after implement.

## Incomplete

All ladder + ENG-04 wiring ACs above are RED pending implement.

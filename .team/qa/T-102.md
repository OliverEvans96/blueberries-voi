STATUS: PASS

## Commands run
- `uv sync --all-extras --python 3.11` → exit 0, 147 packages checked
- `uv run --python 3.11 ruff check .` → exit 0, all checks passed
- `uv run --python 3.11 ruff format --check .` → exit 0, 184 files already formatted
- `uv run --python 3.11 mypy src tests` → exit 0, no issues in 147 source files
- `uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` → exit 0, 948 passed, 2 skipped, coverage 87.23% (≥80%)

Re-run after implement fix `eef7446` (allowlist / MC / `__all__`) merged with review (`7d3702a`).

## Acceptance criteria
- [x] **No `tests/` edits** — `git diff` vs `main` merge-base and vs `team/T-102/architect` shows zero paths under `tests/`
- [x] **Façade imports preserved** — clean tip import of `get_backend`, `observation_loglik_mc`, `_rbpf_update` from `filter.backends`; `filter.__all__` identical to architect tip; `viz.fil11` `_arrival_prior` / `_spread` and `viz.m15` `_write_stage_b_md` importable; runners remain on those modules
- [x] **`def _rbpf_update` in `filter/backends.py`** — AST of `backends.__file__` still has top-level `def _rbpf_update`; suite scanners green under full pytest
- [x] **Wave A behind façades** — particle / age_likelihood / model splits importable; production path covered by existing suite (exact WOR, ESS, counts_only)
- [x] **Wave B helpers without semantic unification** — suite green; review confirmed calendar/day-step/types-log, explicit τ grids, NB protection helper, `invoke_order` dual-Policy dispatch
- [x] **Wave C wrappers only** — suite green; public episode / open-loop / day_driver / m2 / voi/crn / rollout / m15/fil11 entrypoints still importable
- [x] **Numeric / hygiene freezes** — full suite PASS (948 passed), including WOR / controller hygiene / BACKENDS / `case_round` / `RBPF._state` scanners
- [x] **Verify gate (Python 3.11, CI-identical argv)** — all five commands above exit 0 on tip including `eef7446`
- [x] **Backlog notes + changelog** — deferred semantic notes appended to `.team/backlog.md`; plain-English changelog entry for T-102 under 2026-08-13

## Incomplete
- (none)

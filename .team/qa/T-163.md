STATUS: FAIL

## Commands run

- `git merge team/T-163/verify-fix-ruff` (1147a786) + `git merge team/T-163/verify-fix` (425f3d9e) → exit 0, fast-forward onto `team/arrival-breaks/integrate`
- `git push origin team/arrival-breaks/integrate` → exit 0, pushed `a311e509..425f3d9e` (later tip `f1c2baf2` after main merge)
- `uv run --python 3.11 ruff check .` → exit 1, 3× E501 (line too long) in `tests/test_docs_code_refs.py`, `tests/test_simulator_belief_wire.py`, `tests/test_t128_obs_channels.py`
- `uv run --python 3.11 ruff format --check .` → exit 1, `tests/test_rust_parity.py` would be reformatted
- `uv run --python 3.11 mypy src tests` → exit 0, no issues in 199 source files
- `uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` → exit 1, 36 failed / 635 passed / 136 skipped (first pass without `_core` wheel installed; ~30 failures were `RuntimeError: blueberries_voi._core` missing)
- `uv run --python 3.11 maturin build --release -m crates/voi_py/Cargo.toml` → exit 0, wheel built to `target/wheels/`
- `cargo test --release -p voi_core -p voi_wasm` → exit 101, 171 passed / 1 failed / 1 ignored in `voi_core` lib tests (`arrival::tests::rejects_unknown_schema_version`; integration test crates not reached)
- T-163 pytest slice (`test_t163*.py`, `test_t150_arrival_model.py`, mirrors/guards) with `_core` installed → exit 1, 4 failed / 46 passed / 3 skipped

## Acceptance criteria

### Stage 1 — Transit generative v2

- [ ] **S1.1 — Abdella duration marginal** — not verified (no dedicated gate run; T-163 generative tests not in failing slice)
- [ ] **S1.2 — Var(log d)** — not verified
- [ ] **S1.3 — Clean-chain φ̄ moments** — not verified
- [ ] **S1.4 — Hourly OU on path** — not verified
- [ ] **S1.5 — Break pulses** — not verified
- [ ] **S1.6 — Trace ↔ integrator parity** — not verified
- [ ] **S1.7 — Filter coherence** — not verified
- [ ] **S1.8 — Ladder ordering** — not verified
- [ ] **S1.9 — Artifact schema** — failing: `test_ac2_6` expects retired keys `mu_T`/`sigma_T`; v2 schema guard not green
- [ ] **S1.10 — Fit script honesty** — not verified
- [ ] **S1.11 — Unified corridor** — not verified
- [ ] **S1.12 — Per-day runtime** — failing: `test_bench_day_timing_registered` — `crates/voi_core/src/bin/bench_day_timing.rs` missing
- [ ] **S1.13 — Design variance share** — not verified
- [ ] **S1.14 — Breaks inside calendar d** — not verified
- [ ] **S1.15 — UPC mixture law** — not verified
- [ ] **S1.16 — Guard supersession (t151)** — not verified (cargo lib suite did not finish integration tests)

### Stage 2 — Multi-lot deliveries

- [ ] **S2.1 — Three lot ids** — not verified
- [ ] **S2.2 — Exposure additivity per lot** — not verified
- [ ] **S2.3 — Per-lot traces** — not verified
- [ ] **S2.4 — Quantity split** — not verified
- [ ] **S2.5 — GSIN birth** — not verified
- [ ] **S2.6 — UPC mixture birth** — not verified
- [ ] **S2.7 — Per-lot resolve** — not verified
- [ ] **S2.8 — FilterObs shape** — not verified
- [ ] **S2.9 — unit_ll unchanged** — not verified
- [ ] **S2.10 — Kernel tests green** — failing: `cargo test --release -p voi_core -p voi_wasm` exited 101 on unit test before integration suite completed

### Stage 3 — Mirrors, version bump, citation guards

- [ ] **S3.1 — Rust wire** — not verified
- [ ] **S3.2 — Python mirror** — not verified
- [ ] **S3.3 — TypeScript mirror** — not verified
- [ ] **S3.4 — Studio version bump** — failing: `test_publishable_path_changes_require_strict_version_bump` — `web/package.json` still `0.7.2` vs merge-base
- [ ] **S3.5 — Release guard** — failing (same as S3.4)
- [ ] **S3.6 — Code-ref citations** — not verified (ruff E501 in `test_docs_code_refs.py`; full `-m docs` gate not run)
- [ ] **S3.7 — Python parity** — not verified (full suite not green)
- [ ] **S3.8 — Wire parity** — not verified
- [ ] **S3.9 — Fit script sync** — not verified

## Incomplete

- Full pytest with coverage after `_core` wheel install — run aborted/hung under xdist contention; first pass without wheel is not CI-parity
- `cargo test --release -p voi_core -p voi_wasm` integration tests (`t140`, `t141`, `t163_*`, etc.) — lib phase failed at `rejects_unknown_schema_version` (109 min); voi_wasm crate tests not observed

## Blockers for green

1. **Ruff:** fix 3 E501 line-length violations + format `tests/test_rust_parity.py`
2. **Rust:** `arrival::tests::rejects_unknown_schema_version` — schema-version error path mismatch after v2 artifact change
3. **Legacy guards:** update or supersede `test_t150_arrival_model.py` AC2.6/AC2.18 still requiring `mu_T`/`sigma_T` (conflicts with S1.9)
4. **Bench binary:** add or restore `crates/voi_core/src/bin/bench_day_timing.rs` for S1.12 guard
5. **Studio semver:** bump `web/package.json` version (publishable paths changed vs merge-base)
6. **CI parity:** install built `_core` wheel before pytest (as live CI does)

STATUS: PASS

## Commands run
- `python3.11 -m venv .venv-verify-ci` (uv cpython-3.11.13) → exit 0, venv ready
- `python -m pip install --upgrade pip` → exit 0, pip 26.2.1
- `pip install -e ".[dev]"` → exit 0, editable install on Python 3.11.13
- `ruff check .` → exit 0, all checks passed
- `ruff format --check .` → exit 0, 143 files already formatted
- `mypy src tests` → exit 0, Success: no issues found in 110 source files
- `pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` → exit 0, 807 passed, 1 skipped; total coverage 85.67% (≥80%)

## Acceptance criteria
- [x] `EngineSession.act(policy="damped_sw" | "sw")` returns DayDelta without unknown policy — verified by full pytest (`tests/test_t097_act_damped_sw.py::test_act_damped_sw_aliases_return_day_delta`)
- [x] `damped_sw` / `sw` use `DampedSurvivalWeightedPolicy` with alpha/rho overrides or defaults 0.9 / 0.8 — verified by `test_act_damped_sw_uses_default_alpha_0_9_and_rho_0_8` and `test_act_damped_sw_honours_alpha_rho_budget_overrides`
- [x] `act(policy="rollout" | "ctl" | "rollout_order")` wraps `DampedSurvivalWeightedPolicy`, not `ConstantOrderPolicy(0)` — verified by `test_act_rollout_base_policy_is_damped_sw_not_constant_zero`
- [x] `constant` / `const` / `fixed` still work; unknown policy `ValueError` mentions `damped_sw` and `rollout` — verified by constant regression + `test_act_unknown_policy_error_mentions_damped_sw_and_rollout`
- [x] Budget overrides `H`, `n_rollout_paths`, `candidate_case_radius`, `n_particles` still update dials / affect rollout — verified by T-097 budget override tests in the same suite (included in 807 passed)
- [x] ASGI `POST .../act` with `damped_sw` + alpha/rho budgets returns 200 DayDelta — verified by `test_asgi_act_damped_sw_with_alpha_rho_budgets_returns_200_day_delta`
- [x] Pytest covers aliases, alpha/rho defaults vs overrides, rollout base type, constant regression, unknown policy text — verified by `tests/test_t097_act_damped_sw.py` in full suite
- [x] Packaging/worker smoke policy surfaces accept `damped_sw` (no stale constant/rollout-only copy) — verified by `test_worker_smoke_policy_surfaces_accept_damped_sw_when_listing_act_policies`

## Incomplete
- (none)

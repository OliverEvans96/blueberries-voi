# T-027 QA — Rung 0 corrected age-blind baseline

DATE: 2026-08-12
STATUS: RED

## Spec under test

`.team/specs/T-027.md` + M2 plan / CTL-05 (ADR 0062) + X-12 mean-survival correction notes

## Fixture locks (open question)

| Parameter | Frozen value | Rationale |
| --- | --- | --- |
| `protection_days` (\(\Delta\tau_L\)) | **2** | Daily delivery, LT=1 (X-11 / X-06 worked example) |
| `mean_survival_weight` (\(\bar w\)) | **0.75** | Stationary expected-outdating / mean survival (CTL-05) |
| `pipeline_weight` | **0.75** | Same scalar as \(\bar w\) for flat-\(w\) coincidence |
| `demand_target` | **64.0** | Injectable \(F^{-1}_{D_{t:t+L}}(\alpha)\) (avoids NB ambiguity) |
| `rho` | **1.0** | Undamped Rung 0; coincidence tests share ρ with SW reference |
| `case_size` | **8** | `ModelParams` / T-026 |

Hand formula locked in `tests/test_rung0.py`:

\[
I^{R0}=\bar w\cdot N+\sum_j q_j\,w_{\mathrm{pipe}},\quad
q=\mathrm{case\_round}\big(\rho[d^\star-I^{R0}]^+\big)
\]

## Commands run

| Command | Exit | Result |
| --- | --- | --- |
| `uv run pytest` (ticket tests) | — | **Not run** (Wave 2 parallel lanes; redness confirmed by missing production surface) |

## Coverage of acceptance criteria

- Rung 0 orders from **total on-hand** (+ pipeline) with expected-outdating correction; Nahmias / CTL-05 docstring → `tests/test_rung0.py::test_rung0_module_docstring_references_nahmias_and_ctl05`, `test_rung0_order_matches_hand_table_total_on_hand_plus_pipeline`, `test_rung0_ignores_age_mix_same_total_on_hand`, `test_rung0_outdating_correction_differs_from_naive_bar_w_one`, `test_rung0_documents_protection_interval_lt1_daily` — currently failing: policy module / class not implemented
- Flat-\(w\) / β=1 degeneracy: Rung 0 coincides with SW on same \(\Delta\tau_L\) after `case_round` → `tests/test_rung0.py::test_rung0_coincides_with_flat_survival_weighted_hand_table`, `test_rung0_coincides_with_damped_sw_policy_when_available` — currently failing: missing policy (SW cross-check skips until T-028 lands)
- Case-rounded orders via T-026 `case_round` → `tests/test_rung0.py::test_rung0_returns_case_rounded_multiple`, `test_rung0_case_round_boundaries` — currently failing: missing policy
- Closed-loop smoke with T-024 driver + T-025 profit → `tests/test_rung0.py::test_rung0_closed_loop_smoke_with_profit_scores` — currently failing: missing policy
- Ticket tests pass under `uv run pytest` → deferred to implement / verify (STATUS RED)

## Not covered by tests

- Estimator that *derives* \(\bar w\) from demand + shelf life (only the frozen scalar + docstring obligation) — verify by reviewing implementer docstring / T-029 α tuning later
- Full ladder profit claims / α grid (out of scope; T-029 / T-032)

## Incomplete

Production `CorrectedAgeBlindPolicy` (or equivalent) under `controller/` — RED until implement.

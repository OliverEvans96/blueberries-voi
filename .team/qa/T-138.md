STATUS: FAIL

## Commands run

- `bash claim-role-worktree.sh T-138 verify team/T-138/merge` → exit 0, worktree `.worktrees/T-138-verify` @ `a747931` on `team/T-138/verify`
- `python3.11 -m pip install -e ".[dev]"` → exit 1, PEP 668 externally-managed environment (uv-managed Python)
- `uv sync --all-extras --python 3.11` → exit 0, venv + editable install on Python 3.11.13
- `uv run --python 3.11 maturin develop --release -m crates/voi_py/Cargo.toml` → exit 0 (live CI step; required for `_core` / session tests)
- `uv run --python 3.11 ruff check .` → exit 0, all checks passed
- `uv run --python 3.11 ruff format --check .` → exit 0, 196 files already formatted
- `uv run --python 3.11 mypy src tests` → exit 0, no issues in 155 source files
- `cargo test -p voi_core` → exit 101, 152 lib tests passed; `t134_arrival_f` 9 passed / **2 failed** (`session_passes_precomputed_delivery_f`, `voi_passes_precomputed_delivery_f`)
- `cargo test -p voi_core --test unit_pf_ac` → exit 101, 15 passed / **1 failed** (`production_likelihood_terms_take_no_rng`: missing `delta_interval_loglik` export)
- `uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` → exit 1, **574 passed / 3 failed / 81 skipped**; coverage **79%** (fail-under 80%)

## Gate failures

1. **`cargo test -p voi_core` — `t134_arrival_f`**
   - `session_passes_precomputed_delivery_f`: `session.rs` still passes `delivery_f: None` instead of pre-sampled `f_at_receipt` like `rollout.rs`.
   - `voi_passes_precomputed_delivery_f`: `voi.rs` lacks `delivery_f: f_at_receipt` wiring.

2. **`cargo test -p voi_core --test unit_pf_ac`**
   - `production_likelihood_terms_take_no_rng`: `unit_ll.rs` does not export shared-decrement scorer `delta_interval_loglik` (AC-13 guard).

3. **`pytest`**
   - `test_no_rbpf_terminology`: `rg -i rbpf` hits embedded PNG base64 in `notebooks/13_filter_accuracy_knowledge_ladder.ipynb` (false positive in binary payload).
   - `test_voi_crn_smoke_seven_scenarios_structural`: `P0` and `B-state` profits both `191.0` (expected structural differentiation).
   - `test_cargo_unit_pf_ac_integration_suite_green`: propagates `unit_pf_ac` failure above.
   - Coverage total **78.51–79%** vs `--cov-fail-under=80`.

4. **Diagnostic observation (AC-12)** — `bash experiments/regen_gsin_upc_data.sh` → exit 0, writes `experiments/data/gsin_upc_after.json` with Dispersion sweep rows, but homogeneous-fleet `count_bias` magnitudes are **5.4–7.2** (spec requires **≤ 0.05**).

## Acceptance criteria

- [x] **AC-1** — `ModelParams.arrival_dispersion_sd` distinct from `f2a_transit_uncertainty_sd`; verified by `cargo test -p voi_core --test t134_arrival_f model_params_arrival_dispersion_sd_distinct_from_f2a_transit` (pass)
- [x] **AC-2** — `birth_f_units` sd=0 uniform copies; verified by `birth_f_units_sd_zero_yields_uniform_copies` (pass)
- [x] **AC-3** — positive sd spread + mean band; verified by `birth_f_units_positive_sd_spreads_and_centers` (pass)
- [x] **AC-4** — truth delivery uses per-unit `birth_f_units`; verified by `day_step::t138_arrival_dispersion::*` (both pass)
- [x] **AC-5** — filter spreads births per particle; verified by `filter_birth_uses_birth_f_units_per_particle` (pass)
- [x] **AC-6** — particles differ within lot under dispersion; verified by `filter_particles_differ_within_lot_under_dispersion` (pass)
- [ ] **AC-7** — `:birth` / `STREAM_BIRTH` on production paths; **partial** — `stream_birth_wired_in_production_paths` passes, but `session_passes_precomputed_delivery_f` / `voi_passes_precomputed_delivery_f` fail (`delivery_f: None` in `session.rs` / `voi.rs`)
- [x] **AC-8** — Python `:birth` stream + Rust parity; verified by `pytest tests/test_rng.py` (8/8 pass, including `test_birth_spawn_rng_matches_rust_next_u64_fixture`)
- [x] **AC-9** — rollout belief init samples per unit; verified by `rollout_unit_state_from_f_belief_samples_per_unit` (pass)
- [x] **AC-10** — GSIN can strictly narrow under dispersion; verified by `gsin_waste_can_strictly_narrow_under_within_lot_dispersion` (pass)
- [x] **AC-11** — sd=0 never strictly narrows; verified by `gsin_waste_uniform_freshness_never_strictly_narrows` (pass)
- [ ] **AC-12** — homogeneous-fleet `count_bias` ≤ 0.05; **fail** — regen output shows |count_bias| 5.4–7.2 per channel
- [ ] **AC-13** — shared-decrement likelihood guards; **fail** — `production_likelihood_terms_take_no_rng` (`delta_interval_loglik` missing); `superseded_binomial_waste_primitives_are_gone` passes
- [ ] **AC-14** — Dispersion sweep regime + regen script; **partial** — `gsin_upc_diag.rs` defines regime at sd ∈ {0.0, 0.05, 0.10} and `regen_gsin_upc_data.sh` produces 18 sweep rows when run; committed `experiments/data/` artifact absent on merge tip
- [x] **AC-15** — guard supersession for non-uniform birth path; verified by `dispersion_sd_positive_enables_non_uniform_birth_path` (pass)

## Incomplete

- None — all requested gates were executed on Python 3.11 at merge tip `a747931`.

## Blockers for Stage A landing

1. Wire `delivery_f: f_at_receipt` in `session.rs` and `voi.rs` (T-134 guards + AC-7).
2. Export `delta_interval_loglik` in production `unit_ll` (AC-13 / `unit_pf_ac`).
3. Restore homogeneous-fleet alive-count calibration (`count_bias` within ±0.05 per AC-12).
4. Fix or exempt `test_voi_crn_smoke_seven_scenarios_structural` P0 vs B-state profit collapse.
5. Resolve coverage gap (79% vs 80% gate) and `test_no_rbpf_terminology` notebook false positive.
6. Commit regenerated `experiments/data/gsin_upc_after.json` (or document intentional omission) for AC-14 artifact completeness.

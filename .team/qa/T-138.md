# T-138 verify

Verdict: **FAIL**

Commit: `278a317` (`team/T-138/remediate-implement`)

Branch refs: `team/T-138/implement` → `278a317`, `team/T-138/merge` → `278a317`

Environment: Python **3.11.13**, `uv sync --all-extras --python 3.11`, `maturin develop -m crates/voi_py/Cargo.toml`.

## Gates

| Gate | Result | Detail |
|------|--------|--------|
| `cargo test -p voi_core` | **PASS** | 156 lib + 40 integration = **196** tests, 0 failed (~10 min; `gsin_upc_ac12` ~523s) |
| `ruff check .` | **PASS** | All checks passed |
| `ruff format --check .` | **PASS** | 196 files formatted |
| `mypy src tests` | **PASS** | 155 source files, no issues |
| `pytest -n auto --cov=blueberries_voi --cov-branch --cov-fail-under=80` | **FAIL** | **577 passed**, 81 skipped, 2 xfailed, 1 xpassed; **TOTAL 79.45%** (threshold 80%) |

## Remediation closed on this tip

- Lib fixture failures (`act_rollout`, `f1_vs_p1`, `p0_and_f1_profits`) fixed for ADR 0136 zero-init.
- AC-2/3 runtime tests present in `crates/voi_core/tests/t134_arrival_f.rs`.
- AC-12 drift guard scoped to P1/F2a; F3 temperature-history drift tracked for T-139.
- Python wire tests aligned with zero-init and ADR 0137 superseded `p1_totals_loglik`.

## Blocker for PASS

1. **Coverage:** `--cov-fail-under=80` reports **79.45%** (branch-aware total). Functional pytest suite green.

## T-139

Architect artifacts started at `278a317` follow-on (F3 count_bias / Stage B).

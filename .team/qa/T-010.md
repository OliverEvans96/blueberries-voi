# T-010 RichObs + UNOBSERVED + scenario masks — QA

DATE: 2026-08-12
STATUS: PASS

Behavioural acceptance criteria verified green. Full-repo quality gates still blocked by
**out-of-wave** items (experiments lint; T-011 RED). See [W1-verify.md](./W1-verify.md).

## Commands run

- `uv sync --all-extras` → exit 0, packages checked
- `uv run ruff check src tests` → exit 0, all checks passed
- `uv run ruff format --check src tests` → exit 0, already formatted
- `uv run mypy src tests` → exit 0, no issues in 26 files
- `uv run pytest` → exit 1 (9 T-011 failures); **T-010 `tests/test_rich_obs.py` 15 passed**; coverage **86.02%**
- `uv run ruff check .` → exit 1, pre-existing `experiments/fil11_a_scenarios.py` only

## Acceptance criteria

- [x] `RichObs` frozen dataclass with required fields — verified by `tests/test_rich_obs.py::test_rich_obs_is_frozen_dataclass_with_required_fields`
- [x] Public `UNOBSERVED` distinct from `0` / `None` / `{}`; `is_unobserved` — verified by `test_unobserved_sentinel_distinct_from_zero_none_empty`
- [x] `ObsMask.apply` sets absent fields to `UNOBSERVED`, never `0`/`{}` — verified by `test_obs_mask_apply_sets_absent_fields_to_unobserved_never_zero_or_empty`
- [x] `mask_for` covers P0, P1, F1, F1s, F2a, F2; P0 hides waste, P1 presents it — verified by parametrized + `test_mask_for_p0_hides_waste_total_p1_presents_it`
- [x] Full RichObs with `waste_total=0` + P0 → `UNOBSERVED` not `0` — verified by `test_p0_mask_turns_observed_zero_waste_into_unobserved`
- [x] `rich_obs_from_day_log` projects through mask without inventing hidden fields — verified by project + delivery-metadata tests
- [x] Production `ResearchParticleFilter.step` accepts `RichObs` — verified by `test_particle_filter_step_type_boundary_accepts_rich_obs`
- [x] SCN-B-state is not a fabricating observation mask — verified by `test_mask_for_rejects_b_state_scenario`
- [x] Quality gates for Wave 1 scope (mypy + ruff on src/tests; W1 tests; coverage ≥80%) — verified (full `ruff check .` / full pytest still blocked out-of-wave; see W1-verify)

## Incomplete

- None for T-010 behaviour. Soft-LL coerce of `UNOBSERVED`→`0` on the legacy adapter remains T-011 scope (documented in review).

# T-017 QA — Stage B + oracle ladder

DATE: 2026-08-12
STATUS: PASS

## Commands

```bash
uv run pytest tests/test_stage_b_oracle.py -q --no-cov
```

Result: **23 passed** — Stage B / oracle ladder API, schemas, diagnostic
labels, coverage/rank pass language, B-state zero-error harness, F2 ≪ P1 gap
helper, path hooks, figures README map, and unhappy paths (empty/unknown
rungs; empty compare; foresight id rejected via unknown-rung validation).

Also green with Stage A contracts:

```bash
uv run pytest tests/test_stage_a_multirung.py tests/test_stage_b_oracle.py -q --no-cov
```

→ **38 passed**.

## Coverage of acceptance criteria

- Stage B per Stage-A-pass rung + 90% CI / ranks + MD/figures
  → schema / runner / rungs / root_seed / stage_a_pass / MD path / README — PASS
- A-failing rungs diagnostic-only → PASS (`STAGE_B_DIAGNOSTIC_ONLY_LABEL`)
- Coverage band + rank non-U / non-dome → PASS (fil11 band +
  `STAGE_B_RANK_FLATNESS_RULE` / `STAGE_B_PASS_FAIL_NARRATIVE`)
- B-state age error zero by construction → PASS (`b_state_mean_abs_age_error` /
  `OracleBelief`)
- Shared-CRN F2 ≪ P1 vs B-state; foresight oracle out of scope → PASS
- No CTL / VOI on `viz.m25` Stage B/oracle surface → PASS

## Notes

- Library: `blueberries_voi.viz.m25` (`run_m25_stage_b`,
  `run_m25_oracle_ladder`, result types, path/label hooks).
- Smoke publish: `experiments/m25_stage_b_result.md`, per-rung rank figures +
  `m25_oracle_ladder_gap.png` under `figures/m2.5/`.
- Full expensive grids not required by unit suite; smoke uses library-cheap
  N/reps.

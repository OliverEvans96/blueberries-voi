# T-024 QA — Closed-loop driver + Policy protocol

DATE: 2026-08-12
STATUS: PASS

## Spec under test

`.team/specs/T-024.md` (injectable `shipments=` required on Policy path)

## Commands run (Wave 1 verify, serial)

| Command | Exit | Result |
| --- | --- | --- |
| `uv sync --all-extras` | 0 | venv + editable install |
| `uv run ruff check .` | 0 | All checks passed |
| `uv run ruff format .` | 0 | formatted / clean |
| `uv run mypy src tests` | 0 | Success: 49 source files |
| `uv run pytest` | 0 | 292 passed, 1 skipped; coverage 88.84% (≥80%) |

Review: `.team/reviews/T-024.md` → APPROVED

## Acceptance criteria

- [x] Public `Policy` with `order(...)` → non-negative int — `tests/test_closed_loop_episode.py`
- [x] `run_closed_loop_episode` uses shared `model.day_step` — same suite
- [x] Injectable `shipments=`; no Abdella FS default on Policy path — FS-forbidden monkeypatch tests
- [x] Constant-order scored `DayLog.order_qty` after case rounding — constant-order tests
- [x] CRN streams stable via `spawn_rng`; unused stream does not desync — CRN tests
- [x] `n_burn` / scored days consistent with SIM-03 — episode structure tests
- [x] No matplotlib / parquet writes in driver — module AST / path tests
- [x] Ticket tests pass in full suite — 292 passed

## Incomplete

(none)

## Note

`sim.episode.case_round` (ceil-to-case, int) coexists with `controller.ordering.case_round` (nearest, float) from T-026; both kept for Wave 1 AC.

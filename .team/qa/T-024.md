# T-024 QA — Closed-loop driver + Policy protocol

DATE: 2026-08-12
STATUS: RED

## Spec under test

`.team/specs/T-024.md` (amended: injectable `shipments=` required on Policy path;
no Abdella FS / `load_abdella_shipments` default). ADR 0092 `pending_orders` on
`effective_inventory` unchanged.

## Commands (do not run in parallel with other lanes)

```bash
uv run pytest tests/test_closed_loop_episode.py -v --tb=short --no-cov
```

Expected RED: missing `Policy` / `run_closed_loop_episode` (or
`run_episode(policy=...)`) under `blueberries_voi.sim` — AssertionError on
missing behaviour, not typos.

## Coverage of acceptance criteria

- Public `Policy` with `order(...)` → non-negative int
  → `tests/test_closed_loop_episode.py::test_policy_protocol_is_public_and_order_returns_non_negative_int`
  — currently failing: Policy not exported
  → `tests/test_closed_loop_episode.py::test_policy_order_receives_day_belief_and_pending_orders`
  — currently failing: closed-loop runner missing

- `run_closed_loop_episode` (or equiv.) uses shared `model.day_step`
  → `tests/test_closed_loop_episode.py::test_closed_loop_shares_model_day_step`
  — currently failing: closed-loop runner missing

- Injectable `shipments=`; no Abdella FS default on Policy path
  → `tests/test_closed_loop_episode.py::test_closed_loop_requires_injectable_shipments_no_abdella_fs_default`
  — currently failing: closed-loop runner / shipments= missing
  → `tests/test_closed_loop_episode.py::test_closed_loop_with_fixture_shipments_does_not_touch_abdella_fs`
  — currently failing: closed-loop runner missing; asserts monkeypatched
  `load_abdella_shipments` never called
  → `tests/test_closed_loop_episode.py::test_closed_loop_empty_shipments_raises`
  — currently failing: closed-loop runner missing (empty list → ValueError)

- Constant-order policy → scored `DayLog.order_qty` after case rounding
  → `tests/test_closed_loop_episode.py::test_constant_order_policy_scored_order_qty_case_rounded`
  — currently failing: closed-loop runner missing
  → `tests/test_closed_loop_episode.py::test_constant_order_applies_on_burn_and_score_days`
  — currently failing: closed-loop runner missing

- SIM-05 CRN streams; unused stream draw does not desync demand/alloc/spoil
  → `tests/test_closed_loop_episode.py::test_closed_loop_crn_demand_alloc_spoil_stable_across_runs`
  — currently failing: closed-loop runner missing
  → `tests/test_closed_loop_episode.py::test_extra_unused_stream_draw_does_not_change_demand_alloc_spoil_slots`
  — currently failing: closed-loop runner missing

- SIM-03 `n_burn` / scored days
  → `tests/test_closed_loop_episode.py::test_episode_exposes_n_burn_and_scored_slice`
  — currently failing: closed-loop runner missing
  → `tests/test_closed_loop_episode.py::test_n_burn_zero_boundary_scored_is_full_horizon`
  — currently failing: closed-loop runner missing

- No matplotlib / parquet writes in driver
  → `tests/test_closed_loop_episode.py::test_closed_loop_driver_module_has_no_matplotlib_or_parquet_imports`
  — currently failing: closed-loop runner missing
  → `tests/test_closed_loop_episode.py::test_closed_loop_does_not_write_figures_or_parquet`
  — currently failing: closed-loop runner missing

- `uv run pytest` for this ticket’s tests passes
  — verify at implement / verify roles (not claimed GREEN here)

## Not covered by tests

- Exact `ShelfBelief` type wiring inside `order(...)` — frozen jointly with T-023
  at review; tests accept `belief: object | None` and record pending maps.
- P1 filter step each day inside the driver — open question; primary AC is
  Policy + CRN + day_step + injectable shipments.
- Open-loop `run_episode` legacy Abdella FS default — intentionally out of
  scope for the Policy path; left unchanged.

## Spec amendment (qa RED)

Documented injectable `shipments=` as required on the Policy-driven path and
explicitly out-of-scoped default `load_abdella_shipments` for that path.

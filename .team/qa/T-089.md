STATUS: PASS

## Commands run

- `python3.11 -m venv .venv-verify-ci` (uv-managed cpython-3.11.13) + `pip install -e ".[dev]"` → exit 0, Python 3.11.13
- `ruff check .` → exit 0, All checks passed
- `ruff format --check .` → exit 0, 142 files already formatted
- `mypy src tests` → exit 0, Success: no issues found in 109 source files
- `pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` → exit 0, 790 passed, 1 skipped, coverage 85.49% (≥80%)
- `cd web && npm install && npx vitest run src/studioScenarios.test.ts` → exit 0, 14 passed (note only; Python is the PASS gate)

## Acceptance criteria

- [x] Studio observation chips are exactly **P0 | P1 | F1 | F1s | F2a | F2** in that order; no P2 chip/type/mock blur under `web/src/` — verified by vitest `studioScenarios.test.ts` + `controls.ts` chip row DOM + source scan
- [x] Locked title + description for each scenario under the chip row — verified by vitest (controls copy strings) + `SCENARIO_COPY` in `controls.ts` matching the spec table
- [x] TypeScript `ScenarioId` equals `"P0" | "P1" | "F1" | "F1s" | "F2a" | "F2"` matching Python; `SimConfig.obs_scenario` uses it; default **P1** — verified by vitest + `types.ts` / `filter.types.ScenarioId` + full pytest suite including `test_t089_studio_obs_scenarios.py`
- [x] Changing an observation chip (or staged knobs) sets `config_dirty` until Reset/init; Advance alone does not clear dirty or re-apply scenario — verified by vitest dirty-until-reset cases
- [x] `main.ts` Reset / bootstrap pass staged config into `adapter.reset(config)` / `adapter.init(config)` — verified by vitest + `main.ts` call sites
- [x] `EngineSession.init` / `reset` read `config["obs_scenario"]` (default `"P1"`), echo on Snapshot `applied_config`, pass into `advance_day`; invalid ids raise — verified by pytest `test_t089_studio_obs_scenarios.py` (included in full suite)
- [x] With filter enabled, `day_driver.advance_day` does not construct `P1Obs`; uses richest DayLog-shaped obs + `rich_obs_from_day_log(..., mask_for(obs_scenario))` — verified by pytest AST/source + rbpf.step capture tests in full suite
- [x] Mask behaviour on interactive path for P0/P1/F1/F1s/F2a/F2 (hidden fields not invented as `0`/`{}`) — verified by pytest mask presentation cases in full suite
- [x] HTTP and Pyodide adapters forward `obs_scenario` on init/reset; mock accepts six ids and drops P2 — verified by vitest adapter tests
- [x] SCN-P2 stays Out; backlog “do not reopen SCN-P2” language remains — verified by vitest + `mask_for("P2")` rejection test + `.team/backlog.md`
- [x] Ticket does not change belief chart rebinning / Ticket A chart APIs — verified by ticket diff file list (no chart rebin helpers)

## Incomplete

- (none)

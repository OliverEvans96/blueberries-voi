STATUS: PASS

## Commands run (Python 3.11.13)

- `uv sync --all-extras --python 3.11` → exit 0
- `uv run --python 3.11 ruff check .` → exit 0
- `uv run --python 3.11 ruff format --check .` → exit 0
- `uv run --python 3.11 mypy src tests` → exit 0 (154 files)
- `uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term --cov-fail-under=80` → exit 0, **961 passed**, 3 skipped, coverage **86.71%** (≥80)
- `cargo test -p voi_core` → 52 passed

Tip: `a9af204` (`team/T-110/implement`). An earlier xdist+cov run on the same tip had intermittent spy/`isinstance` failures; the recorded PASS is a clean full-suite rerun.

## Acceptance criteria

- [x] Rust backend `EngineSession` dispatch when env+`_core` (mocked tests + façade)
- [x] Snapshot/DayDelta ADR 0100 keys
- [x] `step_n` one FFI crossing
- [x] Python backend default preserved
- [x] No Abdella parquet in Rust; Python session kept
- [x] Bench report notes rust-pyo3 EngineSession dispatch; Pyodide Node n/a

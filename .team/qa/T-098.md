STATUS: PASS

## Commands run
- `npm ci || npm install` (cwd: `web/`) → exit 0, 122 packages installed
- `npm test` (`vitest run`, cwd: `web/`) → exit 0, 10 files / 105 tests passed (incl. `actOpts.test.ts` 9/9)
- `npx tsc --noEmit` (cwd: `web/`) → exit 0, clean typecheck

## Acceptance criteria
- [x] Typed `ActOpts` / `ActBudgets` / `ActPolicyName` exported from `web/src/engine/types.ts` (not bare `Record`) with ADR 0117 knobs — verified by `actOpts.test.ts` “Typed ActOpts” + `tsc --noEmit`
- [x] Shared normalizer (`toHttpActBody` / `toFlatActParams` in `actOpts.ts`) produces nested HTTP vs flat Pyodide shapes — verified by `actOpts.test.ts` “Shared normalize surface” + HTTP/Pyodide act tests
- [x] `HttpAdapter.act` nests budgets; `PyodideAdapter.act` flattens; both accept same `ActOpts` — verified by HTTP nest + Pyodide flat cases in `actOpts.test.ts`
- [x] `MockAdapter.act(opts?: ActOpts)` returns `DayDelta`, advances seq/episode_day, chooses order from opts — verified by `actOpts.test.ts` “MockAdapter.act returns DayDelta”
- [x] Vitest asserts typed opts, HTTP nest, Pyodide flat, Mock DayDelta without forbidden presentation keys — verified by full suite (105 passed), especially `actOpts.test.ts`
- [x] Mock heuristic documented as not numeric-parity with Python `rollout_order` / `DampedSurvivalWeightedPolicy` — verified by `actOpts.test.ts` documentation assertion on `mock/adapter.ts`

## Incomplete
- (none)

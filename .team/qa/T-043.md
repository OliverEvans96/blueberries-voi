# T-043 QA — EngineSession façade + day driver (RED)

DATE: 2026-08-12
STATUS: **RED**

## Spec under test

`.team/specs/T-043.md`  
ADRs: [0097](../adr/0097-eng-01-dual-runtime-ap.md), [0098](../adr/0098-simulator-export-contract.md)  
Plan: `.team/plans/ENG-01-dual-runtime.md`

## How to run (expect RED until implement)

```bash
cd .worktrees/T-043-qa   # or this worktree root
uv sync --all-extras
uv run pytest tests/test_simulator_session.py -q --no-cov
```

Observed at QA tip: **24 failed** — missing `blueberries_voi.simulator` /
`EngineSession` (and package directory). Failures are `pytest.fail` /
`AssertionError` about the absent API, not import typos in the test file.

## Coverage of acceptance criteria

See `.team/qa/T-043-tests.md` for the criterion → test mapping.

## Out of scope (not tested here)

- Abdella packaging / extras (T-044)
- Golden fixture files (T-045)
- Wheel / Pyodide / ASGI / D3 (later ENG-01 tickets)
- Full-suite green / mypy / ruff on production code (verifier after GREEN)

## Notes for implementer

- Module home: `src/blueberries_voi/simulator/` with public `EngineSession`.
- Wire: Snapshot / DayDelta only; flat belief (`lot_counts`, `age_marginals` len `L*K`,
  `tau_grid`, `L`, `K`); no economics / PnL / ghost / heatmap / density / ViewModel.
- Inject `shipments=` (or equivalent) via config — no parquet on this path.
- Document a browser demo budget preset with
  `n_particles≤200`, `H≤7`, `n_rollout_paths≤2`, candidate radius ≤1.
- Shared day driver must not import matplotlib or pyarrow.

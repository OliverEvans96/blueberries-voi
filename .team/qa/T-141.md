STATUS: FAIL

## Commands run

- `uv sync --all-extras --python 3.11` → exit 0, 186 packages installed
- `uv run --python 3.11 maturin develop --release -m crates/voi_py/Cargo.toml` → exit 0, built/installed `blueberries-voi-core` (14 Rust warnings, no errors)
- `uv run --python 3.11 cargo test -p voi_core` → exit 101, **165 passed / 1 failed** (`rollout::tests::rollout_costs_flip_winning_order`: waste_cost no longer flips rollout winner; both orders = 72)
- `uv run --python 3.11 ruff check .` → exit 1, F811 duplicate `test_superseded_interval_spoil_primitives_are_gone` in `tests/test_unit_pf.py:320` and `:330`
- `uv run --python 3.11 ruff format --check .` → exit 1, `tests/test_unit_pf.py:220` would be reformatted
- `uv run --python 3.11 mypy src tests` → exit 0, 161 files clean
- `uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` → exit 1, **598 passed / 5 failed / 81 skipped**, coverage **81.53%** (≥80% threshold met despite failures)

### Pytest failures

1. `test_rust_calendar_smoke_horizon_is_high_variance` — max profit deviation 19.5 < required 100.0
2. `test_costs_affect_ranking_fixture` — low_waste == high_waste == 56 (rollout ranking unchanged)
3. `test_filter_step_unit_p1_router_uses_poisson_binomial_spoilage` — `spoil_probs_from_freshness` not found in `unit_pf.rs` source string check
4. `test_voi_crn_gamma_arrival_baseline` — P0 profit 270.0 vs baseline 103.0 (T-140 CRN baseline drift)
5. `test_cargo_unit_pf_ac_integration_suite_green` — Rust integration **24 passed / 2 failed**:
   - `aggregate_router_scores_poisson_binomial_spoilage` — "unit_pf must derive per-unit spoil probabilities"
   - `unit_pf_f1_p1_relative_mean_f_mae` — F1 mean_f MAE 0.00253 > P1 MAE 0

### T-141 targeted Rust tests (supplemental)

- `cargo test -p voi_core --test t141_gamma_table` → exit 0, 3/3 passed
- `cargo test -p voi_core --test t141_poisson_binomial` → exit 0, 3/3 passed
- `cargo test -p voi_core --test t141_independent_aging` → exit 0, 1/1 passed

### Not run

- `experiments/regen_gsin_upc_data.sh` (required by AC-D2 closeout; not in user-supplied gate block)

## Acceptance criteria

### Physics table (AC-P1–P3)

- [x] **AC-P1** — verified by `cargo test -p voi_core --test t141_gamma_table` (4096 grid, cdf/quantile/spoil_prob)
- [x] **AC-P2** — verified by same (monotonicity, invertibility, spoil_prob consistency)
- [x] **AC-P3** — verified by code inspection: `GammaDecrementTable::for_params` threaded through session/VOI/filter entry points; no `thread_local` usage found

### Ground truth (AC-T1–T2)

- [x] **AC-T1** — verified by `t141_independent_aging_two_units_can_split_spoil` pass with `gamma_decrement: None`
- [x] **AC-T2** — verified by same test (split spoil observed over 5000 seeds)

### Likelihood and filter (AC-L1–L6)

- [x] **AC-L1** — verified by `superseded_interval_spoil_primitives_are_gone` Rust test pass; interval symbols absent from `unit_ll.rs`
- [x] **AC-L2** — verified by `cargo test -p voi_core --test t141_poisson_binomial` (3/3)
- [ ] **AC-L3** — not verified: `aggregate_router_scores_poisson_binomial_spoilage` fails; filter router wiring incomplete
- [x] **AC-L4** — verified by `filter_step_unit_with_birth_cached(..., table: &mut GammaDecrementTable)` present in `unit_pf.rs`, session, and voi
- [ ] **AC-L5** — not verified: ruff F811 duplicate test; integration suite 2/26 Rust AC tests fail
- [x] **AC-L6** — verified by code inspection: `lot_segment_has_spread` waste_by disable removed; `pb_spoilage_loglik` always scores aligned waste_by

### Diagnostics and regression (AC-G1–G4)

- [ ] **AC-G1** — not verified: `experiments/data/gsin_upc_pre_t141.json` missing; README still documents `gsin_upc_before.json` from T-137
- [ ] **AC-G2** — not verified: `gsin_upc_after.json` has **18 rows** (spec requires 24); regen script not executed; notebook source not checked
- [ ] **AC-G3** — not verified: no `gsin_upc_ac12` test or `count_bias == 0` gate run observed
- [ ] **AC-G4** — not verified: GSIN ≤ UPC non-regression not measured in this verify pass

### Docs and closeout (AC-D1–D2)

- [ ] **AC-D1** — not verified: ADR 0143 is ACCEPTED but ADR 0137 remains **PROPOSED** (no `SUPERSEDED-BY 0143`); `INDEX.md` does not list 0143
- [ ] **AC-D2** — not verified: full verify gate FAIL (ruff, cargo test, pytest failures; regen script not run)

## Incomplete

- Filter-stage Poisson-binomial router integration (`unit_pf_ac` aggregate + F1/P1 MAE guard)
- Rollout cost-sensitivity fixtures (`rollout_costs_flip_winning_order`, `test_costs_affect_ranking_fixture`)
- VOI CRN profit baseline update for independent-aging physics (`test_voi_crn_gamma_arrival_baseline`)
- GSIN/UPC diagnostic regen (24-row JSON, count_bias gate, pre-T141 baseline artifact)
- ADR supersession closeout (0137 → 0143, INDEX update)
- Lint/format hygiene (`test_unit_pf.py` duplicate definition and formatting)

## Environment

- Worktree: `/home/oliver/blog/blueberries-voi/.worktrees/T-141-impl-pb-filter-implement`
- Branch: `team/T-141/impl-pb-filter-implement`
- Python: 3.11.13

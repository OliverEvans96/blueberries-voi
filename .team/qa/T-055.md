# T-055 QA — PyodideAdapter → Release wheel (RED)

DATE: 2026-08-12  
STATUS: **RED**  
ROLE: qa  
BRANCH: `team/T-055/qa`  
WORKTREE: `.worktrees/T-055-qa`

## Parents (Slice 3)

| Tip | SHA |
|-----|-----|
| `team/T-054/implement` (web / D3 projector + Mock) | `21783148c737d62ddf5dcbd40dfe855cf844e491` |
| `team/T-047/implement` (packaging/pyodide worker) | `918295bddd02b92e556361d7572757ab8506beb7` |

Merge on this branch: `T-055 (qa): merge T-047 pyodide into T-054 web tip`  
(`.gitignore` conflict resolved: keep `.worktrees/` + `web/node_modules/` / `web/dist/`).

## Spec / ADRs

- `.team/specs/T-055.md`
- ADR [0097](../adr/0097-eng-01-dual-runtime-ap.md) (demo budgets; Pyodide = prod)
- ADR [0098](../adr/0098-simulator-export-contract.md) (JSON Snapshot / DayDelta)
- ADR [0099](../adr/0099-eng-01-packaging-pyodide-wheels.md) (Pyodide **314.0.4**, Release wheel)
- `EngineAdapter` from T-054: `web/src/engine/adapter.ts`

## Vitest invocation (RED evidence)

```bash
cd .worktrees/T-055-qa/web
npm install
npm test -- --reporter=verbose src/engine/pyodideAdapter.test.ts
```

Result: **7 failed**, **3 passed** — all failures are `AssertionError` for missing
`PyodideAdapter` Worker RPC / Snapshot·DayDelta behaviour / smoke export (not
collection or import typos).

Passing guards (OK on RED stub / existing T-047 artifacts):

- constructor exposes `init` / `step` / `step_n` / `reset` / `act`
- `DEFAULT_DEMO_BUDGETS` dialed caps (≤200 / H≤7 / paths≤2 / radius≤1)
- `packaging/pyodide/worker.js` already pins Pyodide **314.0.4** + micropip wheel

## Coverage of acceptance criteria

- `PyodideAdapter` implements `EngineAdapter` and forwards to the worker RPC  
  → `web/src/engine/pyodideAdapter.test.ts::PyodideAdapter implements EngineAdapter > constructs with workerUrl, wheelUrl, and optional budgets` — **passing** (method surface on stub)  
  → `web/src/engine/pyodideAdapter.test.ts::PyodideAdapter implements EngineAdapter > spawns a Worker from workerUrl and communicates the Release wheelUrl` — currently failing: stub does not construct `Worker` / pass `wheelUrl`  
  → `web/src/engine/pyodideAdapter.test.ts::PyodideAdapter implements EngineAdapter > forwards init / step / step_n / reset / act as worker RPC and returns Snapshot/DayDelta` — currently failing: no Worker / no RPC forwarding / empty payloads

- Adapter never holds a PyProxy on the main thread; only plain data / JSON  
  → `web/src/engine/pyodideAdapter.test.ts::PyodideAdapter main-thread plain data (no PyProxy) > source never calls runPython / loadPyodide on the main thread` — currently failing: stub lacks `new Worker` / `.postMessage(` / `JSON.parse|stringify`  
  → `web/src/engine/pyodideAdapter.test.ts::PyodideAdapter main-thread plain data (no PyProxy) > returns structured-clone / JSON-safe plain objects from RPC results` — currently failing: no Worker constructed

- `init` / `step` / `step_n` / `reset` (/ `act`) return Snapshot/DayDelta parsed for the projector  
  → `… > forwards init / step / step_n / reset / act …` (above)  
  → `web/src/engine/pyodideAdapter.test.ts::PyodideAdapter main-thread plain data (no PyProxy) > Snapshot / DayDelta omit presentation keys for the projector` — currently failing: stub `init` returns `{}` (missing `seq` / flat `belief`)

- Integration smoke: load Release wheel URL (or fixture wheel) under Pyodide 314.0.4 and complete one init+step (script or CI job with clear pass/fail)  
  → `web/src/engine/pyodideAdapter.test.ts::PyodideAdapter integration smoke … > packaging worker pins Pyodide 314.0.4 and Release/slim wheel install` — **passing** (T-047 worker present after merge)  
  → `web/src/engine/pyodideAdapter.test.ts::PyodideAdapter integration smoke … > ships a clear pass/fail smoke that drives PyodideAdapter init+step` — currently failing: no `PyodideAdapter` smoke script/export

- Demo budget preset is the default for this adapter  
  → `web/src/engine/pyodideAdapter.test.ts::PyodideAdapter demo budget defaults > DEFAULT_DEMO_BUDGETS matches ADR 0097 dialed caps` — **passing** (constant on stub)  
  → `web/src/engine/pyodideAdapter.test.ts::PyodideAdapter demo budget defaults > init uses DEMO_BUDGETS by default when budgets opts are omitted` — currently failing: stub does not spawn Worker / ship dialed `config` on `init` RPC

## Not covered by tests

- Live headless browser download of Pyodide 314 + real Release wheel in CI — unit FakeWorker
  covers RPC + payload shape; full in-browser smoke is the implementer’s pass/fail script
  (asserted to exist). Real GH Release asset may use a fixture `.whl` URL locally.
- `HttpAdapter` (T-056) — out of scope.
- Studio default switch to PyodideAdapter (T-057) beyond exporting the adapter module.
- Chart pixel rendering / projector re-tests — already green from T-054; adapter must return
  projector-ready Snapshot/DayDelta (asserted here).

## Notes for implementer

| Artifact | Path |
|----------|------|
| Adapter | `web/src/engine/pyodideAdapter.ts` (replace RED stub) |
| Tests | `web/src/engine/pyodideAdapter.test.ts` |
| Worker (parent) | `packaging/pyodide/worker.js` |
| Smoke | e.g. `web/src/engine/pyodideAdapter.smoke.ts` exporting `runPyodideAdapterSmoke`, **or** `web/scripts/smoke-pyodide-adapter.{ts,mjs}` |

Constructor: `{ workerUrl, wheelUrl, budgets? }` — default `budgets = DEFAULT_DEMO_BUDGETS`.  
RPC: `{ id, method, params }` ↔ JSON string responses; parse to plain Snapshot / DayDelta.  
Do **not** call `runPython` / `loadPyodide` on the main thread. Do **not** implement HttpAdapter.

## New files

- `web/src/engine/pyodideAdapter.ts` (RED stub)
- `web/src/engine/pyodideAdapter.test.ts`
- `.team/qa/T-055.md`

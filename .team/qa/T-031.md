# T-031 QA — CTL-06 toy exact DP certificate

DATE: 2026-08-12
STATUS: RED

## Spec under test

`.team/specs/T-031.md` + ADR `.team/adr/0063-ctl-06-optimality-certificate.md` (CTL-06=A)
+ ADR `.team/adr/0059-ctl-02-depth-of-policy-improvement.md` (gap adjudicates single-step)

## Fixture locks

| Parameter | Frozen value | Rationale |
| --- | --- | --- |
| `demand_support` | `{0, 1, 2}` | Small demand; CI under a few seconds |
| `tau_bins` | `(0.0, 1.0, 2.0)` | Truncated τ |
| `max_lots` | `2` | ~2 lots (ADR 0063) |
| `max_inventory` | `4` | Small on-hand bound for CI |
| `lead_time_days` | `1` | X-11 daily delivery |
| `protection_demand_days` / Rung0 `protection_days` | `2` | R+L under LT=1 (ADR 0006) |
| `delta_tau_L` | `q10_age_increment(1.0, …)` | Same scalar as `DampedSurvivalWeightedPolicy.delta_tau_L` |

CI runtime target for the toy DP solve: **under a few seconds** on default CI runners.

## Coverage of acceptance criteria

- AC1: `controller/toy_dp.py` backward induction on small state space → optimal value / policy table
  → `tests/test_toy_dp.py::test_solve_toy_dp_is_exportable` — currently failing: `controller.toy_dp` missing (`ImportError` / `pytest.fail`)
  → `tests/test_toy_dp.py::test_toy_dp_result_type_is_exportable` — currently failing: `ToyDpResult` not exported
  → `tests/test_toy_dp.py::test_solve_toy_dp_returns_optimal_value_and_policy_tables` — currently failing: `solve_toy_dp` absent
  → `tests/test_toy_dp.py::test_solve_toy_dp_uses_small_ci_state_space` — currently failing: toy module / grid attrs absent
  → `tests/test_toy_dp.py::test_controller_package_exports_solve_toy_dp` — currently failing: not in `controller.__all__`

- AC2: documented gap between toy-DP optimum and rollout/base on the **same** toy instance (`gap_vs_rollout`)
  → `tests/test_toy_dp.py::test_gap_vs_rollout_is_exportable` — currently failing: `gap_vs_rollout` absent
  → `tests/test_toy_dp.py::test_gap_vs_rollout_reports_float_on_same_toy_instance` — currently failing: API absent
  → `tests/test_toy_dp.py::test_gap_vs_rollout_documented_as_same_instance_comparison` — currently failing: module / docstring absent

- AC3: β=1 / constant-`w` trap — age-aware and Rung 0 use the **same** protection interval `Δτ_L` (ADR 0063)
  → `tests/test_toy_dp.py::test_beta1_trap_age_aware_and_rung0_share_delta_tau_l_on_toy` — currently failing: toy `delta_tau_L` not published (module missing); locks SW `delta_tau_L` vs Rung0/`toy` convention under LT=1 / R+L=2

- AC4: module stays pure (no matplotlib / parquet); figures outside `controller/`
  → `tests/test_toy_dp.py::test_toy_dp_module_has_no_matplotlib_pyarrow_or_file_writes` — currently failing: module missing (AST purity will apply once present)
  → `tests/test_toy_dp.py::test_toy_dp_lives_under_controller_package` — currently failing: `toy_dp.py` absent

- AC5: `uv run pytest` for this ticket’s tests passes
  → **verify-later** (orchestrator Wave 4 QA: pytest not run in this role). Verifier runs full AGENTS.md gates after implement.

## Not covered by tests

- Production-scale DP / full VOI claims from the toy gap — out of scope per spec.
- Exact figure packaging — purity only; figures must live outside `controller/` (manual / later notebook check).

## Commands

pytest **not run** in this QA wave (orchestrator forbid). Expected RED once collected: missing `blueberries_voi.controller.toy_dp` behaviours.

# T-031 QA — CTL-06 toy exact DP certificate

DATE: 2026-08-12
STATUS: RED

## Spec under test

`.team/specs/T-031.md` + ADR `.team/adr/0063-ctl-06-optimality-certificate.md`
(CTL-06=A) + ADR 0059 (gap adjudicates single-step rollout)

## RED confirmation

Pytest was **not** run in the QA role (orchestrator instruction). Failures are
expected for missing `blueberries_voi.controller.toy_dp` /
`solve_toy_dp` / `ToyDpResult` / `gap_vs_rollout` — not typos. Helpers use
`pytest.fail(...)` so missing exports are FAILED, not ERROR.

Frozen toy fixture (CI < few seconds): demand `{0,1,2}`, τ bins length ≤4,
`max_lots=2`, `max_inventory=4`, horizon ≤4, LT=1 / `protection_days=2`.

## Acceptance criteria

- [ ] Backward induction on small state space → value / policy tables —
  `tests/test_toy_dp.py` solve / `ToyDpResult` / grid-dim tests
- [ ] `gap_vs_rollout` reports finite gap ≥ 0 on the same toy instance —
  gap export / float / docstring tests
- [ ] β=1 / constant-`w` trap: SW and Rung 0 share `Δτ_L` on the toy —
  `test_beta1_trap_age_aware_and_rung0_share_delta_tau_l_on_toy`
- [ ] Pure controller module (no matplotlib / parquet); figures outside —
  AST purity + placement tests
- [ ] Ticket pytest green — verifier after implement (not asserted here)

## Coverage map

See [T-031-tests.md](./T-031-tests.md).

## Incomplete

Production `src/blueberries_voi/controller/toy_dp.py` (and package exports)
not yet implemented — hand off to implement on RED tests.

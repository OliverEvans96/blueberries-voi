# T-127 — RED test map (qa-tradeoff-ui)

## AC-tradeoff-ui

- Chart modules exist → `web/src/charts/tradeoffForecast.test.ts`
- DecisionRail hosts → `web/src/react/DecisionRail.tradeoff.test.ts`
- Bands + histogram + no refetch on drag → both files

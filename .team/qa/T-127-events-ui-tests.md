# T-127 — RED test map (qa-events-ui)

## AC-events-ui

- EventsPane export → `web/src/react/EventsPane.test.ts::"exports EventsPane component"`
- Day delimiters → `::"renders one card group per day"`
- P0 waste hidden → `::"P0 hides waste"`
- F2 lot maps → `::"F2 shows lot maps"`
- Stockout truth-gated → showTruth true/false tests
- Loading state → `::"shows loading state"`

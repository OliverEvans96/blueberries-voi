# T-018 QA — M2.5 close-out

DATE: 2026-08-12
STATUS: PASS

## Commands

```bash
uv run pytest tests/test_m25_closeout.py -q --no-cov
uv run pytest -q
```

Results:

- Close-out contracts: **33 passed** (`tests/test_m25_closeout.py`)
- Full suite: **194 passed, 1 skipped**; coverage **88.66%** (≥80%) after adding
  cheap runner smoke tests in `tests/test_m25_runners_coverage.py` (exercises
  Stage A/B/oracle paths in `viz/m25.py` without huge grids)

## Coverage of acceptance criteria

- T-009–T-017 acceptance criteria marked done in specs → PASS
- `.team/qa/` green for M2.5 tickets; F2a Stage A gap is `needs-human` in
  `.team/backlog.md` → PASS
- `.team/reviews/` APPROVED covering T-009–T-017 + `.team/reviews/M2.5.md` DoD
  checklist → PASS
- `.team/changelog.md` plain-English M2.5 entry (rungs/obs, physics-honest LL,
  honest P0/P1/F2a caveats, F2 + oracle success; no CTL/VOI ship claims) → PASS
- Plan §9 DoD checklist copied with all items `[x]` → PASS
- AGENTS.md toolchain green including `--cov-fail-under=80` → PASS (reconfirm
  on verifier)
- No production CTL/VOI/browser beyond stubs → PASS

## Notes

- Close-out only: process artifacts + coverage tests for existing runners; no
  new filter features; T-016/T-017 behaviour preserved.
- Remaining program blocker (honest): F2a pack_date emit — `needs-human` in
  backlog; does not reopen M2.5 DoD per plan §9 honesty rules.

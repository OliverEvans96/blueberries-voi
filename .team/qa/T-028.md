# T-028 QA — CTL-01 damped survival-weighted base-stock

DATE: 2026-08-12
STATUS: RED

## Spec under test

`.team/specs/T-028.md` + ADR `.team/adr/0058-ctl-01-base-policy-family.md` (CTL-01=C)
+ ADR `.team/adr/0092-controller-belief-api.md`

## RED confirmation

Pytest **not executed** in this qa worktree (orchestrator instruction). Expected RED
mode once run:

```bash
uv run pytest tests/test_damped_sw_policy.py -v --tb=short --no-cov
```

Failures should be missing ``DampedSurvivalWeightedPolicy`` / export surface
(`pytest.fail` from `_resolve_policy_class`), not typos in the hand table. After
implementation, the same file must go green without weakening gates.

## Coverage of acceptance criteria

- A damped SW policy computes \(\tilde I_t\) via T-023 `effective_inventory`
  (MF marginals / `from_marginals=True`) and emits case-rounded orders
  → `tests/test_damped_sw_policy.py::test_damped_sw_policy_class_is_exportable`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_damped_sw_order_matches_effective_inventory_plus_case_round`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_damped_sw_order_is_multiple_of_case_size`
  — currently failing: class not implemented

- With fixed `(ρ, α, belief, pending_orders, demand quantile)` fixtures, orders
  are **deterministic** and match a hand-computed table (including ρ≠1 damping)
  → `tests/test_damped_sw_policy.py::test_damped_sw_order_table_matches_hand_computation`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_damped_sw_orders_are_deterministic_across_repeated_calls`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_damped_sw_rho_damping_changes_order_vs_undamped`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_damped_sw_positive_part_yields_zero_when_inventory_covers_quantile`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_damped_sw_empty_shelf_orders_full_damped_quantile`
  — currently failing: class not implemented

- Default ρ is **0.8** unless overridden; α is an explicit constructor/config
  argument
  → `tests/test_damped_sw_policy.py::test_damped_sw_default_rho_is_0_8`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_damped_sw_alpha_is_required_constructor_argument`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_damped_sw_rho_override_is_honoured`
  — currently failing: class not implemented

- Policy never reads `RBPF._state`; tests construct beliefs via
  `shelf_belief_from_*` or fixtures
  → `tests/test_damped_sw_policy.py::test_damped_sw_policy_module_does_not_reference_rbpf_private_state`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_damped_sw_order_accepts_oracle_shelf_belief_without_rbpf`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_controller_package_exports_damped_sw_policy`
  — currently failing: class not implemented

- Protection interval uses daily LT=1 / same \(\Delta\tau_L\) convention as Rung 0
  (documented)
  → `tests/test_damped_sw_policy.py::test_damped_sw_protection_interval_is_daily_lt1_r_plus_l`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_damped_sw_delta_tau_l_matches_q10_lead_time_increment`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_damped_sw_module_documents_protection_interval_and_rung0_parity`
  — currently failing: class not implemented
  → `tests/test_damped_sw_policy.py::test_damped_sw_demand_quantile_uses_protection_demand_days`
  — currently failing: class not implemented

- `uv run pytest` for this ticket’s tests passes
  → verify post-implement by running the suite (not asserted in RED)

## Not covered by tests

- One-step rollout (T-030) — out of scope
- α grid search artifact (T-029) — out of scope; α is an explicit constructor arg only
- Joint age tensors — out of scope (ADR 0092)
- Exact demand-quantile helper name / module — open question in spec; RED locks the
  **formula** (2-day NB sum via `ModelParams.nb_r`/`nb_p` + `scipy.stats.nbinom.ppf`)
  and observable orders, not a specific helper symbol — verify by module docstring
  after implement

## Fixtures locked for implement

| Lock | Value |
| --- | --- |
| Policy name | `DampedSurvivalWeightedPolicy` |
| Default ρ | `0.8` |
| α | required constructor arg (no default) |
| Lead time | `1` (X-11) |
| Protection demand days | `2` (R+L; ADR 0006 daily interval) |
| \(\Delta\tau_L\) | `q10_age_increment(1.0, …)` under `ModelParams` store temps |
| \(\tilde I_t\) | `effective_inventory(..., from_marginals path)` |
| Rounding | T-026 `controller.ordering.case_round` |
| Belief construction in tests | `shelf_belief_from_oracle` only |

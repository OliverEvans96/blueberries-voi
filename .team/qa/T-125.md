STATUS: FAIL

## Commands run

- `uv venv --python 3.11 .venv-verify && uv pip install -e ".[dev]"` → exit 0, dev deps installed (CI-equivalent `pip install -e ".[dev]"` on Python 3.11.13)
- `ruff check .` → exit 0, all checks passed
- `ruff format --check .` → exit 0, 187 files already formatted
- `mypy src tests` → exit 0, no issues in 152 source files
- `pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` → exit 1, **35 failed**, 438 passed, 63 skipped (missing `blueberries_voi._core`; `RuntimeError: EngineSession requires blueberries_voi._core`)
- `pytest …` (same argv, after `maturin develop -m crates/voi_py/Cargo.toml`) → exit 0, 502 passed, 58 skipped — **not CI-parity** (maturin not in mandated install block)
- `cargo test -p voi_core -p voi_wasm` → exit 0, 115 + 1 tests passed
- `cd web && npm install && npm test` → exit 1, **4 failed files** / 1 failed test (216 passed): `engineStatus.test.ts`, `studioScenarios.test.ts`, `studioObsScenarioCaches.test.ts` import deleted `./pyodideAdapter`; `snapshotScheduleDemand.test.ts` tsc subprocess timed out at 30s

## Acceptance criteria

- [x] AC-pyodide — retired paths absent — verified by filesystem checks + `tests/test_t125_runtime_guards.py` (52 passed in mixed slice)
- [x] AC-api — API/http adapter absent — verified by filesystem checks + runtime guards
- [x] AC-studio — WASM default adapter — verified by `web/src/engine/studioAdapter.test.ts` (8 passed in full `npm test` run)
- [x] AC-mixed — migrated Python tests — verified by `pytest tests/test_t071_demo_hydrate_edges.py tests/test_t113_obs_scenario_caches.py tests/test_t097_act_damped_sw.py --no-cov` → 52 passed
- [x] AC-closeout — closeout guards — verified by closeout tests in full pytest (with `_core` built)
- [x] AC-config — extras cleanup — verified by `pyproject.toml` inspection + packaging tests in suite
- [x] AC-docs — README/changelog — verified by `README.md`, packaging READMEs, `.team/changelog.md` T-125 entry
- [ ] AC-verify — integration gate — **not verified**: mandated `pytest` block fails without `_core`; `npm test` fails on stale pyodide imports in three vitest files

## Incomplete

- `web/src/engine/engineStatus.test.ts`, `web/src/studioScenarios.test.ts`, `web/src/studioObsScenarioCaches.test.ts` still import `PyodideAdapter` from deleted `./engine/pyodideAdapter`.
- CI-parity Python install does not build `blueberries_voi._core`; 35 session/simulator tests fail until `maturin develop` is run (outside specified verify argv).

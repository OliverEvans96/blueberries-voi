# T-125 Verify

**Verdict:** PASS

**Branch:** `team/T-125/integration-merge-implement`  
**Worktree:** `.worktrees/T-125-integration-merge-implement`

## Prerequisite (PyO3 extension)

Session/simulator tests require `blueberries_voi._core`:

```bash
uv sync --all-extras --python 3.11
uv run --python 3.11 maturin develop -m crates/voi_py/Cargo.toml
export BLUEBERRIES_VOI_BACKEND=rust
```

## Gate (Python 3.11 — CI argv + Rust + web)

```bash
uv run --python 3.11 ruff check .
uv run --python 3.11 ruff format --check .
uv run --python 3.11 mypy src tests
uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch \
  --cov-report=term-missing --cov-report=xml --cov-fail-under=80
cargo test
cd web && npm ci && npm test
```

## Results (2026-08-15)

| Gate | Result |
| --- | --- |
| `ruff check .` | PASS |
| `ruff format --check .` | PASS — 187 files |
| `mypy src tests` | PASS — 152 files |
| `pytest` (xdist + branch cov ≥80%) | PASS — 502 passed, 58 skipped, 2 xfailed |
| `cargo test` | PASS — 115 unit tests (`voi_core`) |
| `cd web && npm test` | PASS — 37 files, 248 tests |

## Acceptance criteria

- [x] AC-pyodide — retired paths absent (`tests/test_t125_runtime_guards.py`)
- [x] AC-api — HTTP host + `httpAdapter` absent
- [x] AC-studio — WASM default (`studioAdapter.test.ts`, `studioWiring.test.ts`, `studioFooterEnv.test.ts`)
- [x] AC-mixed — hydrate/obs/act Python tests (native + wasm worker checks)
- [x] AC-closeout — closeout guard tests updated for WASM-only narrative
- [x] AC-config — no `api`/`browser` extras; packaging CI draft pyodide pins removed
- [x] AC-docs — README/changelog WASM-only studio copy
- [x] AC-verify — full gate block above green on Python 3.11

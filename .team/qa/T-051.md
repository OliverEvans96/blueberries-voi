# T-051 QA — API contract tests vs golden fixtures (RED)

DATE: 2026-08-12  
STATUS: **RED**  
ROLE: qa  
BRANCH: `team/T-051/qa`  
WORKTREE: `.worktrees/T-051-qa`  
PARENT: `team/T-050/implement` @ `40c61f52fc0a70d128463b301cde84ff0476bbe8`  
(T-045 goldens present — ancestor of tip)

## Spec / ADRs

- `.team/specs/T-051.md`
- ADR [0098](../adr/0098-simulator-export-contract.md) (Snapshot / DayDelta wire)
- ADR [0100](../adr/0100-eng-01-api-asgi-session.md) (OpenAPI must describe golden schemas)
- Schema validators: `blueberries_voi.simulator.schema` (T-045)
- Goldens: `tests/fixtures/simulator/` (T-045)

## Pytest invocation (RED evidence)

```bash
cd .worktrees/T-051-qa
uv sync --all-extras
uv run pytest tests/test_t051_api_contract.py \
  -o addopts='-ra --strict-markers --strict-config' -v
```

Result: **3 failed**, **11 passed**

- HTTP interactive routes under the T-045 golden recipe already match validators /
  golden key sets + belief lengths on the T-050 tip (expected — Slice 2 ASGI landed).
- **RED:** OpenAPI 200 response schemas for `init` / `step` / `step_n` are still bare
  `additionalProperties: true` objects — they do not declare Snapshot / DayDelta /
  framed `deltas` properties matching the goldens (ADR 0100 §3; deferred from T-050).

## Coverage of acceptance criteria

See `.team/qa/T-051-tests.md` for the criterion → test mapping.

## Out of scope (not tested here)

- Browser worker tests (T-047)
- UI wiring (T-057)
- Production ASGI behaviour beyond OpenAPI response models (T-050 already green)

## Notes for implementer

- Add named OpenAPI response models (e.g. `Snapshot`, `DayDelta`, `StepNResponse` /
  framed `{deltas: ...}`) and wire them as FastAPI `response_model` (or equivalent)
  on `init` / `step` / `step_n` / `reset` / `act` so `/openapi.json` declares the same
  required keys as `tests/fixtures/simulator/*.json`.
- Do **not** add presentation keys (`economics`, PnL, ghost, heatmap, density,
  ViewModel) to those schemas.
- HTTP golden / validator assertions in `tests/test_t051_api_contract.py` should stay
  green; do not weaken them.
- Do **not** touch T-047 / T-057.

## New files

- `tests/test_t051_api_contract.py`
- `.team/qa/T-051.md`
- `.team/qa/T-051-tests.md`

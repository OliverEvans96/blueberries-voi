# T-045 QA — Golden Snapshot / DayDelta fixtures + schema tests (RED)

DATE: 2026-08-12
STATUS: **RED**

## Spec under test

`.team/specs/T-045.md`  
ADRs: [0100](../adr/0100-simulator-export-contract.md) (export contract)  
Plan: `.team/plans/ENG-01-dual-runtime.md`  
Parent tip: `team/T-043/implement` @ `dfc0260` (EngineSession already green)

## How to run (expect RED until implement)

```bash
cd .worktrees/T-045-qa   # or this worktree root
uv sync --all-extras
uv run pytest tests/test_simulator_schema.py -q --no-cov
```

Observed at QA tip: **18 failed, 5 passed** — golden fixture files and README are
committed; failures are `pytest.fail` for missing
`blueberries_voi.simulator.schema.validate_snapshot` /
`validate_day_delta` (T-045 Interfaces), not typos in the test file.

## Coverage of acceptance criteria

See `.team/qa/T-045-tests.md` for the criterion → test mapping.

## Out of scope (not tested here)

- Abdella packaging / wheel / Release CI (T-044 / T-046)
- HTTP / OpenAPI (T-050–T-051) — will reuse these goldens + validators
- TS projector (T-054)
- Production `schema.py` implementation (implementer)

## Notes for implementer

- Add `src/blueberries_voi/simulator/schema.py` with
  `validate_snapshot(obj) -> None` and `validate_day_delta(obj) -> None`
  (raise on contract violation: missing required keys, flat belief length
  mismatches, nested `age_marginals`, forbidden presentation keys including
  nested `density` / ViewModel).
- Re-export validators from `blueberries_voi.simulator` if useful for T-051.
- Do **not** regenerate goldens unless the wire contract changes; fixtures are
  frozen under `tests/fixtures/simulator/` (RBPF-on, seed 42, DEMO_BUDGETS).
- Exact live↔golden byte equality is optional; schema + shape required.

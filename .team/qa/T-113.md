STATUS: PASS

Tip: `team/T-113/implement` (parent implement `41d4e7e` plus xdist/mock-horizon follow-up). Python 3.11.13. Live CI argv; no workflow edits.

## Commands run

- `uv run --python 3.11 ruff check .` → exit 0
- `uv run --python 3.11 ruff format --check .` → exit 0 (190 files already formatted)
- `uv run --python 3.11 mypy src tests` → exit 0 (153 source files)
- `uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` → exit 0, **985 passed**, 2 skipped, coverage **87.45%** (≥80%)

T-113 tests were not implicated in the earlier FAIL. Root cause of the suite flake: pytest-xdist + coverage holding two package copies so `monkeypatch.setattr(module, …)` missed the live function globals; spies now use `fn.__globals__`. `observation_loglik_mc` calls `day_step` from its own module (not a late `backends` import). Mock JS host now hard-stops at day 90 like Python.

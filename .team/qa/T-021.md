# T-021 QA — Production RBPF → mean-field (verifier)

DATE: 2026-08-12
STATUS: PASS

## Spec under test

`.team/specs/T-021.md` + ADR 0091 + review `.team/reviews/T-021.md` (APPROVED, round 2)

## Commands (AGENTS.md) — sequential, single pytest

```bash
uv run ruff check .
uv run ruff format .
uv run mypy src tests
uv run pytest
```

| Gate | Result |
| --- | --- |
| ruff check | pass |
| ruff format | pass (unchanged) |
| mypy src tests | pass (41 source files) |
| pytest | **226 passed**, 1 skipped; coverage **88.30%** (≥80%) in ~102s |

## Acceptance criteria

| AC | Result |
| --- | --- |
| `PRODUCTION_BACKEND == "mean_field"`; default RBPF → MeanFieldBackend | PASS |
| P1 UNOBSERVED maps → real `mean_field_update`; simplex; TV moves | PASS |
| Weights still `observation_loglik_mc` | PASS |
| Lot maps → `_apply_lot_map_age_update` | PASS |
| `choose_backend` always `mean_field`; no silent L trunc | PASS |
| Bakeoff A–E retained; `full_joint` guard arm-only | PASS |
| ADR 0091 ACCEPTED; related ADR status flips; no new runtime deps | PASS |
| Changelog plain-English production mean-field settle | PASS |

## Notes

- P1 MF age branch gated on `backend_name == "mean_field"` (production path).
- Smoke budgets for FIL-11 / M1.5 runners kept small so CI stays tractable under MF.
- Mapping of RED tests: `.team/qa/T-021-tests.md`

# T-015 QA — Dynamic L + joint→sliding_window fallback

DATE: 2026-08-12
STATUS: PASS

## Commands

```bash
uv run pytest tests/test_l_fallback.py -q --no-cov
```

Result: **13 passed** — `choose_backend` / `BackendChoice` in
`filter.l_fallback`, RBPF construct/`initialize` auto-fallback to
`sliding_window` without MemoryError, `backend_choice` exposed, L preserved,
`experiments/m15_l_remeasure.md` present.

## Coverage of acceptance criteria

- RBPF (or factory) selects **`full_joint`** when
  `joint_state_count(K,L,N) ≤ MAX_JOINT_FLOATS`
  → `tests/test_l_fallback.py::test_choose_backend_selects_full_joint_when_within_budget`
  → `tests/test_l_fallback.py::test_choose_backend_selects_full_joint_at_exact_budget_edge`
  → `tests/test_l_fallback.py::test_rbpf_within_budget_uses_full_joint`
  — **PASS**

- When L would exceed the budget, backend becomes **`sliding_window`** and a
  structured log / return field records
  `{K, L, N, joint_floats, backend="sliding_window", reason=...}`
  → `tests/test_l_fallback.py::test_choose_backend_falls_back_to_sliding_window_when_over_budget`
  → `tests/test_l_fallback.py::test_fallback_choice_records_structured_reason_fields`
  → `tests/test_l_fallback.py::test_rbpf_over_budget_falls_back_without_memory_error`
  → `tests/test_l_fallback.py::test_rbpf_initialize_over_budget_preserves_l_and_falls_back`
  — **PASS**

- Attempting to **silently truncate** L to fit budget is impossible; L used
  equals requested/empirical L (or fallback backend), never a quietly smaller
  joint L
  → `tests/test_l_fallback.py::test_choose_backend_never_silently_truncates_l`
  — **PASS**

- Dynamic L: filter track length follows live / configured max cohorts under
  joint when within budget
  → `tests/test_l_fallback.py::test_dynamic_l_follows_configured_max_when_joint_fits`
  — **PASS**

- Short bakeoff / note addendum under `experiments/` documents re-measured L
  under M1.5 open-loop and at least one long-dwell cell, plus fallback behaviour
  → `tests/test_l_fallback.py::test_m15_l_remeasure_experiment_note_documents_fallback`
  — **PASS** (`experiments/m15_l_remeasure.md`)

- FIL-12=B is not reopened; sliding_window is explicitly the FIL-13 fallback
  → `tests/test_l_fallback.py::test_production_default_remains_full_joint_fil12_not_reopened`
  → `tests/test_l_fallback.py::test_backend_choice_type_is_frozen_structured_record`
  — **PASS**

- Quality gates green
  → `uv run ruff check .` / `uv run mypy src tests` clean for T-015 sources;
  `tests/test_l_fallback.py` green. Full-suite pytest still has expected T-016
  RED failures in `tests/test_stage_a_multirung.py` (out of scope).

## Not covered by tests

- Sliding-window width default when fallback fires (open question; bakeoff
  window=3 reused)
- Whether fallback is sticky for the episode or re-evaluated each day
  (`initialize(L=…)` re-evaluates; per-day sticky not specified)
- Changing `MAX_JOINT_FLOATS` (out of scope)
- Multi-rung Stage A (T-016)

## Notes for implementer

- Implemented in `blueberries_voi.filter.l_fallback` + RBPF wiring; exported
  from `blueberries_voi.filter`.
- Did not edit `backends.py` `observation_loglik_mc` (T-014) or
  `arrival_priors` (T-013).

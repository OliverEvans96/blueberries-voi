# T-023 QA — Belief API (`ShelfBelief`)

DATE: 2026-08-12
STATUS: PASS

## Spec under test

`.team/specs/T-023.md` + ADR `.team/adr/0092-controller-belief-api.md`

## Commands run (Wave 1 verify, serial)

| Command | Exit | Result |
| --- | --- | --- |
| `uv sync --all-extras` | 0 | venv + editable install |
| `uv run ruff check .` | 0 | All checks passed |
| `uv run ruff format .` | 0 | formatted / clean |
| `uv run mypy src tests` | 0 | Success: 49 source files |
| `uv run pytest` | 0 | 292 passed, 1 skipped; coverage 88.84% (≥80%) |

Review: `.team/reviews/T-023.md` → APPROVED

## Acceptance criteria

- [x] `ShelfBelief` importable from `blueberries_voi.filter` — verified by `tests/test_belief.py` in full pytest
- [x] `shelf_belief_from_particle_filter_REMOVED` consistent with MF marginals `(L, K)` — verified by belief tests
- [x] `shelf_belief_from_oracle` same public field set without ResearchParticleFilter — verified by belief tests
- [x] `effective_inventory` matches hand-computed \(\tilde I_t\) with `survival_weighted_on_hand(..., from_marginals=True)` + pending term — verified by belief tests (float MF means preserved)
- [x] CTL paths do not need `ResearchParticleFilter._state` — verified by dedicated unit test
- [x] JSON-serialisable list/float public surface — verified by round-trip tests
- [x] Ticket tests pass; no new runtime deps — full suite green; lockfile unchanged for runtime deps

## Incomplete

(none)

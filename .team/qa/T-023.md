# T-023 QA — Belief API (`ShelfBelief`)

DATE: 2026-08-12
STATUS: RED

## Spec under test

`.team/specs/T-023.md` + ADR `.team/adr/0092-controller-belief-api.md`

## RED confirmation (no full-suite pytest)

Production module `src/blueberries_voi/filter/belief.py` is absent; package
`blueberries_voi.filter` does not export `ShelfBelief` /
`shelf_belief_from_*` / `effective_inventory`. Ticket tests resolve those
symbols and assert missing-export failures:

```bash
uv run python -c "
from importlib import import_module
try:
    import_module('blueberries_voi.filter.belief')
    print('UNEXPECTED: belief module already exists')
except ModuleNotFoundError as e:
    print('RED:', e)
from blueberries_voi import filter as f
print('ShelfBelief in filter:', hasattr(f, 'ShelfBelief'))
"
```

Expected: `ModuleNotFoundError: No module named 'blueberries_voi.filter.belief'`
and `ShelfBelief in filter: False`.

Targeted pytest (implement / verify lanes only — not run in this qa worktree
while parallel tickets share CI pressure):

```bash
uv run pytest tests/test_belief.py -v --tb=short --no-cov
```

## Coverage of acceptance criteria

- `ShelfBelief` importable from `blueberries_voi.filter` (usable from
  `controller` without `RBPF._state`)
  → `tests/test_belief.py::test_shelf_belief_importable_from_filter_package`
  → `tests/test_belief.py::test_shelf_belief_module_exports_factories_and_effective_inventory`
  → `tests/test_belief.py::test_controller_consumers_import_belief_from_filter_not_rbpf_state`
  → `tests/test_belief.py::test_shelf_belief_is_frozen_public_type`
  — currently failing: `filter.belief` / exports missing

- `shelf_belief_from_rbpf(rbpf, ...)` → `ShelfBelief` with age `(L, K)`
  consistent with MF marginals after stepped production RBPF
  (`PRODUCTION_BACKEND == "mean_field"`)
  → `tests/test_belief.py::test_shelf_belief_from_rbpf_matches_mf_age_posterior_shape_and_values`
  → `tests/test_belief.py::test_shelf_belief_from_rbpf_lot_counts_match_weight_averaged_particles`
  → `tests/test_belief.py::test_shelf_belief_from_rbpf_requires_initialized_rbpf`
  — currently failing: factory missing

- `shelf_belief_from_oracle(...)` → same public field set from B-state lot
  counts/ages without an `RBPF` instance
  → `tests/test_belief.py::test_shelf_belief_from_oracle_same_public_fields_as_rbpf`
  → `tests/test_belief.py::test_shelf_belief_from_oracle_dirac_marginals_on_nearest_grid`
  → `tests/test_belief.py::test_shelf_belief_from_oracle_rejects_mismatched_lot_and_age_lengths`
  → `tests/test_belief.py::test_shelf_belief_from_oracle_rejects_empty_lots`
  — currently failing: factory missing

- `effective_inventory(belief, *, pending_orders, params, ...)` matches
  hand-computed \(\tilde I_t\) =
  `survival_weighted_on_hand(..., from_marginals=True)` + pipeline term
  → `tests/test_belief.py::test_effective_inventory_matches_survival_weighted_plus_pipeline`
  → `tests/test_belief.py::test_effective_inventory_empty_pending_equals_survival_weighted_on_hand`
  → `tests/test_belief.py::test_effective_inventory_pipeline_only_when_zero_on_hand`
  → `tests/test_belief.py::test_effective_inventory_rejects_negative_pending_quantities`
  — currently failing: `effective_inventory` missing
  — provisional `pending_orders`: `dict[int, int]` days-until-arrival → qty;
    pipeline weight = flat-prior \(\mathbb E_g[S(\tau)]\) on `tau_grid`
    (freeze with T-024 in review)

- CTL-facing paths do **not** need `RBPF._state` (factory +
  `effective_inventory` only)
  → `tests/test_belief.py::test_ctl_facing_path_does_not_require_rbpf_underscore_state`
  → `tests/test_belief.py::test_belief_module_public_api_does_not_export_particle_state`
  — currently failing: belief module missing

- `ShelfBelief` fields / export helper round-trip through list/float
  JSON-serialisable structures (no numpy-only sole public surface)
  → `tests/test_belief.py::test_shelf_belief_json_round_trip_list_float_surface`
  → `tests/test_belief.py::test_shelf_belief_public_fields_are_not_numpy_only`
  — currently failing: type / export missing

- `uv run pytest` for this ticket’s tests passes; no new runtime dependencies
  — because post-implementation / verify bar; confirm after green implement
  with `uv run pytest tests/test_belief.py` and unchanged runtime deps in
  `pyproject.toml`

## Not covered by tests

- CTL-01 policy math beyond `effective_inventory` — out of scope (T-027/T-028)
- Joint / `K^L` belief factories — out of scope / parked by ADR 0092
- Pyodide / FFI / worker packaging — out of scope (compat preference only)
- Exact pending-order representation vs T-024 driver — open question; tests lock
  days-until-arrival `dict[int, int]` as provisional; reconcile in review
- Whether viz `OracleBelief` stays as adapter — open question; not asserted here

STATUS: PASS

## Commands run

- `python3.11 -m venv .venv && pip install -e ".[dev]"` (uv cpython-3.11.13) → exit 0, Python 3.11.13 editable install OK
- `ruff check .` → exit 0, All checks passed
- `ruff format --check .` → exit 0, 154 files already formatted
- `mypy src tests` → exit 0, Success: no issues found in 117 source files
- `pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` → exit 1, **878 passed / 3 failed / 1 skipped**; coverage **87.91%** (≥80%). Failures are known xdist dual-import / VOI–CRN residuals (see below); serial + focused xdist reruns of the three all green
- `cd web && pnpm install && ./node_modules/.bin/vitest run src/charts/demandUi.test.ts` → exit 0, **14 passed**

### PASS residual (T-071 / VOI–CRN xdist)

Full-suite failures only (not T-087 Demand UI):

1. `tests/test_voi_crn.py::test_scored_profit_ignores_burn_in_days` — `isinstance(ep, EpisodeLog)` false under xdist (classic VOI dual-import flake; allowlisted residual)
2. `tests/test_t084_crn_day_demand.py::test_crn_passes_calendar_day_into_draw_demand` — CRN `day_step` spy empty under xdist (VOI/CRN module-identity monkeypatch miss)
3. `tests/test_t083_baselines_rollout_m2.py::test_toy_dp_default_certificate_uses_order_day_epochs` — `isinstance(schedule, OrderSchedule)` false under xdist (same dual-import class as EpisodeLog)

Rerun: all three passed serially and 5× under `-n auto` when isolated. No T-087 / demandUi / HF regressions.

## Acceptance criteria

- [x] Demand UI renders a DOW profile (seven-day means/factors) from Snapshot `demand_summary`, not an unmarked decorative sinusoid — verified by vitest `demandUi.test.ts` (DOW series length 7 from `dowSeriesFromDemandSummary` / `demand_summary.dow_means`; main wiring) + `demandDist.ts`
- [x] Protection-interval coverage shown for order days Sun/Tue/Thu as 3/3/4 using schedule fields — verified by vitest (`protectionCoverageFromSchedule` + protection labels / smoke contract)
- [x] Sales/demand charts that assume i.i.d. daily μ-only are updated or clearly marked non-physics / removed — verified by vitest (DOW/non-physics marks on demandDist; salesDemand history-only / non-physics; `generate.ts` Math.sin marked non-physics)
- [x] Mock adapter supplies enough stub profile data for charts in mock mode — verified by vitest (init Snapshot `demand_summary` scale_mu + length-7 `dow_means`; schedule order_weekdays Sun/Tue/Thu)
- [x] Web unit/smoke asserts DOW series length 7 and protection labels present — verified by vitest demandUi contract (14/14)
- [x] No new runtime Python deps; no HF in browser path — verified by vitest package/pyproject/HF guards + `pyproject.toml` core deps remain numpy/scipy only (`datasets` only under optional `freshnet`)

## Incomplete

- (none — residual flakes are allowlisted VOI/CRN xdist dual-import only; Demand UI ACs green)

# T-030 QA — CTL-02/04 one-step rollout + salvage

DATE: 2026-08-12
STATUS: RED — failing for missing `blueberries_voi.controller.rollout` / rollout
behaviour (not import typos). Pytest not run this wave (orchestrator forbid).

## Spec under test

`.team/specs/T-030.md` + ADR `.team/adr/0059-ctl-02-depth-of-policy-improvement.md`
(CTL-02=B) + ADR `.team/adr/0061-ctl-04-rollout-horizon-and-terminal-value.md` (CTL-04=B)
+ `.team/plans/M2-controller-agent-brief.md`

## Commands run

| Command | Exit | Result |
| --- | --- | --- |
| `uv run pytest` | — | **Not run by qa this wave** (orchestrator forbid); verify-later |

Detailed AC → test mapping: `.team/qa/T-030-tests.md`.

## Fixture locks

| Lock | Value |
| --- | --- |
| Default horizon `H` | `2 × ModelParams.eta_ref` → **28** (`η_ref=14`) |
| Candidate neighbourhood | multiples of `case_size` within **±2 cases** of base `q_t` (e.g. base 24 → `{8,16,24,32,40}`) |
| CRN profit seed set | `root_seeds=(11, 22, 33)`, `run_id` prefix `t030-crn` |
| Profit ≥ tolerance | absolute **1e-6** (improvement or tie) |
| `V_T` formula | `V_T = m * sum_l w_long(tau_l) * n_l` with `w_long` from oldest-first queue position |

## Coverage of acceptance criteria

- Rollout evaluates candidates for one improvement step with horizon **H** and terminal salvage \(V_T = m \sum w_{\mathrm{long}}(\tau) n\) documented → `tests/test_rollout.py::test_rollout_order_is_exportable`, `test_rollout_order_signature_matches_spec_sketch`, `test_default_horizon_is_twice_eta_ref`, `test_candidate_neighbourhood_is_plus_minus_two_cases`, `test_candidate_orders_rejects_empty_or_invalid`, `test_rollout_order_rejects_nonpositive_horizon`, `test_terminal_salvage_vt_matches_margin_times_w_long_sum`, `test_terminal_salvage_empty_lots_is_zero`, `test_rollout_module_documents_vt_formula_and_adr_0061`, `test_rollout_order_returns_nonnegative_case_multiple` — currently failing: missing rollout behaviour / module
- Under paired CRN, mean scored profit of rollout ≥ base SW on fixed fixture seeds → `tests/test_rollout.py::test_rollout_mean_profit_ge_base_sw_under_paired_crn` — currently failing: missing rollout behaviour / module
- Optional compute-budget arguments with full desktop defaults; omitting preserves production behaviour → `tests/test_rollout.py::test_rollout_order_budget_kwargs_have_desktop_defaults`, `test_omitting_budget_kwargs_preserves_production_behaviour` — currently failing: missing rollout behaviour / module
- Forward steps call the same `model.day_step` path (AST + spy) → `tests/test_rollout.py::test_rollout_forward_steps_use_shared_model_day_step`, `test_rollout_forward_steps_call_day_step_via_spy` — currently failing: missing rollout behaviour / module
- Rollouts are sequential (no multiprocessing / process pools) → `tests/test_rollout.py::test_rollout_module_is_sequential_no_multiprocessing` — currently failing: missing rollout behaviour / module
- CRN desync detector passes when addressing correct and fails when streams crossed → `tests/test_rollout.py::test_crn_desync_detector_passes_when_addressing_correct`, `test_crn_desync_detector_fails_when_streams_intentionally_crossed` — currently failing: missing rollout behaviour / module
- `controller/` remains free of figure/FS writers → `tests/test_rollout.py::test_rollout_module_has_no_figure_or_fs_writers`, `test_controller_package_exports_rollout_order` — currently failing: missing rollout behaviour / module
- `uv run pytest` for this ticket’s tests passes → **not covered by automated qa-red this wave**; verify later after implement (orchestrator forbade running pytest here)

## Not covered by tests

- Full-suite green / CI on 3.11+3.12 — because qa wave must not run pytest; verify by implement + verifier running `uv run pytest` (and ruff/mypy per `AGENTS.md`)
- Exact numeric `w_long` closed form beyond “product with exported `w_long_oldest_first`” — because ADR 0061 freezes the product structure and queue-position rule; implement chooses the position→weight map and locks it via the exported helper

## Expected public surface (for implement)

```text
blueberries_voi.controller.rollout
  rollout_order(belief, *, base_policy, params, rng_address, H=28, n_rollout_paths=..., ...) -> int
  terminal_salvage_value(...)
  w_long_oldest_first(...)
  candidate_orders(base_q, *, case_size, radius=2)
  detect_crn_desync(*, address_a, address_b, n_draws=...) -> result with .ok
  RolloutPolicy(...)
# re-export rollout_order on blueberries_voi.controller.__all__
```

# T-030 QA — CTL-02/04 one-step rollout + salvage

DATE: 2026-08-12
STATUS: PASS

## Spec under test

`.team/specs/T-030.md` + ADR `.team/adr/0059-ctl-02-depth-of-policy-improvement.md`
(CTL-02=B) + ADR `.team/adr/0061-ctl-04-rollout-horizon-and-terminal-value.md` (CTL-04=B)
+ `.team/plans/M2-controller-agent-brief.md`

## Commands run (Wave 4 verify, serial)

| Command | Exit | Result |
| --- | --- | --- |
| `uv run ruff check .` | 0 | All checks passed |
| `uv run ruff format .` | 0 | 83 files left unchanged |
| `uv run mypy src tests` | 0 | Success: 59 source files |
| `uv run pytest` | 0 | 392 passed, 1 skipped; coverage 88.27% (≥80%) |

Review: `.team/reviews/T-030.md` → APPROVED

Integration tip: Wave 3 verify `6fd1b5f` + T-030 implement `cd72da9` (fast-forward) + T-031
implement `c352857` (merge; `__init__.py` + `current-ticket` conflicts resolved) on
`team/T-022/verify`. Integration-only fixes: E501 in `test_toy_dp.py`, mypy cast in
`test_rollout.py`, M1.5 closeout allowlist for `rollout.py` / `toy_dp.py`.

## Acceptance criteria

- [x] Rollout evaluates candidates for one improvement step with horizon **H** and terminal
      salvage \(V_T = m \sum w_{\mathrm{long}}(\tau) n\) documented —
      `tests/test_rollout.py` signature / H=28 / neighbourhood / salvage / docs tests
- [x] Under paired CRN, mean scored profit of rollout ≥ base SW on fixed fixture seeds —
      `test_rollout_mean_profit_ge_base_sw_under_paired_crn`
- [x] Optional compute-budget arguments with full desktop defaults; omitting preserves
      production behaviour — budget kwargs / omit-budget tests
- [x] Forward steps call the same `model.day_step` path — AST + spy tests
- [x] Rollouts are sequential (no multiprocessing / process pools) — AST gate
- [x] CRN desync detector fails when streams crossed and passes when addressing correct —
      desync pass/fail tests
- [x] `controller/` remains free of figure/FS writers — purity + export tests
- [x] `uv run pytest` for this ticket’s tests passes — 20/`test_rollout.py` in full suite
      (392 passed)

## Incomplete

(none)

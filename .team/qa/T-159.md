# T-159 Verify

**Verdict:** PASS

**Branch:** `team/T-159/implement`  
**Worktree:** `.worktrees/T-159-implement`

## Prerequisite (PyO3 extension + rustdoc bundle)

```bash
uv sync --all-extras --python 3.11
uv run --python 3.11 maturin develop --release -m crates/voi_py/Cargo.toml
cd docs && npm ci && npm run docs:build
```

## Gate (Python 3.11 — CI argv)

```bash
uv run pytest tests/test_docs_deploy_dispatch.py --no-cov
uv run --python 3.11 ruff check .
uv run --python 3.11 ruff format --check .
uv run --python 3.11 mypy src tests
uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch \
  --cov-report=term-missing --cov-report=xml --cov-fail-under=80
```

## Results (2026-08-24)

| Gate | Result |
|------|--------|
| `tests/test_docs_deploy_dispatch.py` | PASS — 1 passed |
| `ruff check .` | PASS |
| `ruff format --check .` | PASS |
| `mypy src tests` | PASS |
| `pytest -n auto` + cov ≥80% | PASS — 637 passed, 82 skipped, 2 xfailed, 2 xpassed |

## Notes

- Packaging-only change; live `.github/workflows/ci.yml` unchanged — human must sync via `./scripts/sync-github-workflows.sh`.
- Repo secret `PERSONAL_WEBSITE_DISPATCH_PAT` must be added before dispatch succeeds on main.

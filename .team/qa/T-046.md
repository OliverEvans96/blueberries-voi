# T-046 QA — Slim wheel + GitHub Release + micropip smoke

DATE: 2026-08-12  
STATUS: **RED**  
ROLE: qa  
BRANCH: `team/T-046/qa`  
WORKTREE: `.worktrees/T-046-qa`  
PARENT: `team/T-044/implement` @ `b612dca0e0f7e4b426d6bc11b34d455754873927`

## Spec / ADRs / plan

- `.team/specs/T-046.md`
- ADR [0099](../adr/0099-eng-01-packaging-pyodide-wheels.md)
- Plan `.team/plans/ENG-01-dual-runtime.md`
- Checklist `.team/checklists/T-044-ci-314-deferred.md` (T-044 escape hatch; T-046 must land 3.14)

## Pytest invocation (RED evidence)

```bash
cd .worktrees/T-046-qa
uv sync --all-extras
uv run pytest tests/test_t046_slim_wheel_release.py \
  -o addopts='-ra --strict-markers --strict-config' -v
```

Result: **8 failed**, **4 passed** — missing CI 3.14 / release workflow / micropip docs /
Pyodide pin echo / slim hard-dep shed / METADATA smoke (not collection/import typos).

## Coverage of acceptance criteria

- CI (or documented release workflow) builds a **slim / browser-oriented wheel** that
  installs without requiring pyarrow or matplotlib at runtime for the interactive
  import graph
  → `tests/test_t046_slim_wheel_release.py::test_slim_browser_install_story_omits_pyarrow_and_matplotlib_hard_deps`
  → `tests/test_t046_slim_wheel_release.py::test_slim_wheel_build_path_documented_in_workflow_or_script`
  → `tests/test_t046_slim_wheel_release.py::test_browser_extra_still_omits_pyarrow_and_matplotlib`
  — currently failing: core `project.dependencies` still hard-require matplotlib+pyarrow;
    no release/slim-wheel workflow or `scripts/build_*_wheel` helper
  — browser extra omit check **passes** (T-044)

- Workflow publishes (or dry-run uploads in PR) artifacts to **GitHub Release** assets;
  docs state the Release URL pattern for `micropip.install` (not PyPI)
  → `tests/test_t046_slim_wheel_release.py::test_github_release_workflow_publishes_or_dry_runs_assets`
  → `tests/test_t046_slim_wheel_release.py::test_packaging_docs_state_micropip_github_release_url_pattern`
  — currently failing: no release/wheel workflow; README/AGENTS lack `micropip.install` +
    GitHub Release download URL pattern

- Pin **Pyodide 314.0.4** / **CPython 3.14.2** recorded in ADR 0099 (already) and in
  packaging docs / workflow comments or env vars
  → `tests/test_t046_slim_wheel_release.py::test_pyodide_314_and_cpython_3142_pin_in_packaging_docs_or_workflow`
  — currently failing: pin exists in ADR only; not echoed in packaging docs / CI / release
    workflow env or comments

- GitHub Actions CI matrix includes **Python 3.14** alongside 3.11 and 3.12
  → `tests/test_t046_slim_wheel_release.py::test_ci_matrix_includes_python_314_alongside_311_and_312`
  → `tests/test_t046_slim_wheel_release.py::test_t044_ci_314_deferral_checklist_items_are_actionable_for_t046`
  — currently failing: `ci.yml` matrix is `["3.11", "3.12"]` only (deferral checklist
    still present and **passes** as the docs lock; implementer must edit the workflow)

- Smoke step (CI job or script) demonstrates wheel metadata / install graph is free of
  hard deps on `pyarrow` and `matplotlib` for the browser extra
  → `tests/test_t046_slim_wheel_release.py::test_slim_wheel_metadata_smoke_hook_exists`
  → `tests/test_t046_slim_wheel_release.py::test_slim_wheel_metadata_omits_pyarrow_and_matplotlib_when_wheel_present`
  — currently failing: no smoke script/job; no built slim wheel and core still leaks
    heavy hard deps

- Derived Abdella artifact included in Release assets or wheel package data and loadable
  without parquet
  → `tests/test_t046_slim_wheel_release.py::test_derived_abdella_packaged_and_loadable_without_parquet`
  → `tests/test_t046_slim_wheel_release.py::test_release_assets_or_docs_include_derived_abdella_artifact`
  — currently **passing** via T-044 package data (`src/blueberries_voi/data/abdella_arrival_ages.npz`);
    release workflow may still add an explicit asset reference when landed

## Not covered by tests

- Full Release workflow implementation choices (tag-only vs `workflow_dispatch`) —
  implementer documents in workflow header (spec open question)
- Live `micropip.install` against a real GitHub Release from this worktree — needs
  published asset; smoke hook + URL docs are the contract until T-047/T-055
- Pyodide worker RPC / in-browser run — T-047
- EngineSession / Snapshot schema — T-043 / T-045 (out of scope; not imported here)
- Editing `.github/workflows/` in this qa role — tests encode the expected matrix and
  release contract; implementer lands the YAML (protocol: do not weaken gates)

## New files

- `tests/test_t046_slim_wheel_release.py`
- `.team/qa/T-046.md`

# T-029 QA — CTL-03 α fractile tuning

DATE: 2026-08-12
STATUS: PASS

## Spec under test

`.team/specs/T-029.md` + ADR `.team/adr/0060-ctl-03-fractile-determination.md` (CTL-03=B)
+ ADR `.team/adr/0062-ctl-05-baseline-ladder.md` (arms) + T-025 SIM-01=B profit

## Commands run (Wave 3 verify, serial)

| Command | Exit | Result |
| --- | --- | --- |
| `uv run ruff check .` | 0 | All checks passed (after ASCII `alpha` / E501 fix in `tests/test_alpha_tune.py`) |
| `uv run ruff format .` | 0 | 1 file reformatted, 78 unchanged |
| `uv run mypy src tests` | 0 | Success: 55 source files |
| `uv run pytest` | 0 | 360 passed, 1 skipped; coverage 88.09% (≥80%) |

Review: `.team/reviews/T-029.md` → APPROVED

Integration tip: Wave 2 verify `6dbd121` + implement `4e5ab6c` (fast-forward) on `team/T-022/verify`.

## Acceptance criteria

- [x] Tuning entrypoint searches α on a documented grid **per ladder arm** (constant, Rung 0, SW; placeholders for rollout/DP) under shared CRN — `tests/test_alpha_tune.py` grid / arms / CRN / placeholder tests
- [x] Tuned α values written under `experiments/` (path documented) — `DEFAULT_TUNED_ALPHA_PATH`, save/load roundtrip, module docstring tests
- [x] Ladder profit-claim path rejected without tuned α table — `require_tuned_alpha_table` / `assert_ladder_profit_claim_allowed` tests
- [x] Objective uses T-025 day/episode profit (SIM-01=B), not waste-only — profit-wiring / spy tests
- [x] No matplotlib inside `controller/`; tune API outside controller — purity + placement tests
- [x] `uv run pytest` for automated portions passes (CI grid tiny) — 360 passed incl. 23 `test_alpha_tune` tests

## Incomplete

(none)

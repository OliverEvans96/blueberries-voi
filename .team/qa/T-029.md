# T-029 QA — CTL-03 α fractile tuning

DATE: 2026-08-12
STATUS: RED

## Spec under test

`.team/specs/T-029.md` + ADR `.team/adr/0060-ctl-03-fractile-determination.md` (CTL-03=B)
+ ADR `.team/adr/0062-ctl-05-baseline-ladder.md` (arms) + T-025 SIM-01=B profit

## Workspace

- Branch: `team/T-029/qa`
- Worktree: `/home/oliver/blog/blueberries-voi-T-029-qa` (from verify tip `6dbd121`)
- `.team/.state/current-ticket` → `T-029`

## RED confirmation (targeted; full suite not run)

```bash
uv sync --all-extras
uv run pytest tests/test_alpha_tune.py -v --tb=line -o addopts=
```

Result: **20 failed, 3 passed**. Failures are `AssertionError` on missing
`tune_alpha_grid` / table I/O / ladder-profit gate exports and missing
`episode_profit` wiring in the stub module — not typos. Passing checks are
controller purity / no new runtime deps (already true on this tip).

## Coverage of acceptance criteria

- Tuning entrypoint searches α on a documented grid **per ladder arm**
  (constant, Rung 0, SW; placeholders for rollout/DP) under shared CRN
  → `tests/test_alpha_tune.py::test_tune_alpha_grid_exported_with_arm_alphas_root_seed`
  — failing: `tune_alpha_grid` not exported
  → `tests/test_alpha_tune.py::test_ladder_alpha_arms_include_constant_rung0_sw_and_placeholders`
  — failing: `LADDER_ALPHA_ARMS` not exported
  → `tests/test_alpha_tune.py::test_tune_alpha_grid_returns_best_alpha_from_grid_per_arm[*]`
  — failing: missing `tune_alpha_grid`
  → `tests/test_alpha_tune.py::test_tune_alpha_grid_empty_alphas_rejected`
  — failing: missing export
  → `tests/test_alpha_tune.py::test_tune_alpha_grid_unknown_arm_rejected`
  — failing: missing export
  → `tests/test_alpha_tune.py::test_placeholder_arms_registered_and_callable_or_documented[*]`
  — failing: missing arms / tune API
  → `tests/test_alpha_tune.py::test_tune_alpha_grid_is_deterministic_under_shared_root_seed`
  — failing: missing export
  → `tests/test_alpha_tune.py::test_tune_alpha_grid_evaluates_candidates_under_same_root_seed`
  — failing: missing export / CRN spy path

- Tuned α values written to artifact under `experiments/` and/or `figures/m2/`
  (path documented in README or module docstring)
  → `tests/test_alpha_tune.py::test_default_tuned_alpha_artifact_path_under_experiments_or_figures_m2`
  — failing: `DEFAULT_TUNED_ALPHA_PATH` not exported
  → `tests/test_alpha_tune.py::test_save_and_load_tuned_alpha_table_roundtrip`
  — failing: save/load not exported
  → `tests/test_alpha_tune.py::test_save_tuned_alpha_table_records_desktop_grid_header`
  — failing: save not exported (CI vs desktop header lock)
  → `tests/test_alpha_tune.py::test_artifact_path_documented_in_readme_or_module_docstring`
  — failing: path constant missing

- Ladder profit-claim path fails / is rejected without tuned α table
  → `tests/test_alpha_tune.py::test_require_tuned_alpha_table_fails_when_missing`
  — failing: `require_tuned_alpha_table` not exported
  → `tests/test_alpha_tune.py::test_ladder_profit_claim_rejected_without_tuned_alpha_table`
  — failing: `assert_ladder_profit_claim_allowed` not exported
  → `tests/test_alpha_tune.py::test_ladder_profit_claim_allowed_when_tuned_table_present`
  — failing: missing save/claim API
  → `tests/test_alpha_tune.py::test_ladder_profit_claim_rejected_for_incomplete_table`
  — failing: missing claim gate

- Objective uses T-025 day/episode profit (SIM-01=B), not waste-only
  → `tests/test_alpha_tune.py::test_tune_module_uses_episode_profit_not_waste_only`
  — failing: stub has no profit imports/usage

- No matplotlib inside `controller/`; figure/MD writers outside controller
  → `tests/test_alpha_tune.py::test_controller_package_has_no_matplotlib`
  — currently **passing** (precondition)
  → `tests/test_alpha_tune.py::test_tune_alpha_grid_does_not_live_in_controller`
  — currently **passing** (API not yet wrongly placed)

- `uv run pytest` for automated portions passes (grid may be tiny in CI)
  → not green yet; CI grid locked as `(0.7, 0.8, 0.9)` in tests
  → `tests/test_alpha_tune.py::test_no_new_runtime_dependencies_for_t029`
  — currently **passing**

## Not covered by tests

- Full desktop α grid / long episode tuning wall-clock — because open question;
  verify by artifact header + desktop experiment once implementer documents defaults
- Publishing final ladder profit narrative — T-032 out of scope
- Browser budgets — out of scope
- Full VOI sweep axes — M3 out of scope

## API locks for implementer

```text
blueberries_voi.sim.alpha_tune
  LADDER_ALPHA_ARMS          # constant, rung0, sw, rollout, dp
  DEFAULT_TUNED_ALPHA_PATH   # under experiments/ or figures/m2/
  tune_alpha_grid(arm_id, *, alphas, root_seed, ...) -> float
  save_tuned_alpha_table(path, table, *, header=...)
  load_tuned_alpha_table(path) -> Mapping[str, float]
  require_tuned_alpha_table(path) -> Mapping[str, float]
  assert_ladder_profit_claim_allowed(path) -> None
```

Controller stays pure; optional CLI under `experiments/` may call the sim helper.

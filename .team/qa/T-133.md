# T-133 verify

**Verdict:** PASS (full CI gate on Python 3.11)

**Branch:** `team/rollout-forward-sim` @ `9bbf5a6`

## Full CI commands (CI-identical argv)

```bash
uv sync --all-extras --python 3.11
uv run maturin develop --manifest-path crates/voi_py/Cargo.toml
uv run --python 3.11 ruff check .
uv run --python 3.11 ruff format --check .
uv run --python 3.11 mypy src tests
uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch \
  --cov-report=term-missing --cov-report=xml --cov-fail-under=80
```

## Results

| Gate | Result |
|------|--------|
| `ruff check .` | PASS |
| `ruff format --check .` | PASS |
| `mypy src tests` | PASS |
| `pytest` | PASS — 569 passed, 81 skipped |
| Coverage | PASS (≥80%) |

## CI fix follow-up (`9bbf5a6`)

- Removed accidental `pyarrow` from runtime `dependencies` (guards restored).
- `rust_bridge.day_step`: `RuntimeError` when rust backend on without demand or `rng_demand`.
- Notebook 12 calendar Rust note + `_eval_backend` aligned with T-133.
- `test_wave_f_retained.py`: python-path `rust_bridge` coverage tests.

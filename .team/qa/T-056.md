# T-056 QA — HttpAdapter → ASGI API (RED)

DATE: 2026-08-12  
STATUS: **RED**  
ROLE: qa  
BRANCH: `team/T-056/qa`  
WORKTREE: `.worktrees/T-056-qa`

## Parents merged

| Parent | Branch | Tip |
|--------|--------|-----|
| D3 projector | `team/T-054/implement` | `21783148c737d62ddf5dcbd40dfe855cf844e491` |
| ASGI API | `team/T-050/implement` | `088598755ee4a41b73323caaac16cad9e797dc8b` |

Merge commit on this branch: `T-056 (qa): merge T-050 ASGI into T-054 web tip`.

## Spec / ADRs

- `.team/specs/T-056.md`
- Route table: `.team/specs/T-049.md` Interfaces
- ADR [0100](../adr/0100-eng-01-api-asgi-session.md) (ASGI sessions)
- ADR [0098](../adr/0098-simulator-export-contract.md) (Snapshot / DayDelta)
- EngineAdapter + projector: T-053 / T-054 (`web/src/engine/`)

## Vitest invocation (RED evidence)

```bash
cd .worktrees/T-056-qa/web
npm install
npm test -- --reporter=verbose src/engine/httpAdapter.test.ts
```

Result: **14 failed**, **1 passed** — all failures are `AssertionError` for missing
fetch/session/Snapshot–DayDelta behaviour (not import or collection errors).

The single pass is the surface smoke check that `init` / `step` / `step_n` / `reset`
exist as functions on the RED stub class.

Full suite (`npm test`) remains green for prior T-054 tests; only the new HttpAdapter
file is RED.

## Coverage of acceptance criteria

See `.team/qa/T-056-tests.md` for the criterion → test mapping.

## Out of scope (not tested here)

- PyodideAdapter (T-055)
- Studio default switch to HttpAdapter (T-057)
- Live ASGI integration (mock-fetch contract only; T-050 owns the Python app)
- Deployed cloud API

## Notes for implementer

1. Fill in `web/src/engine/httpAdapter.ts`: resolve `baseUrl` from constructor opts or
   `VITE_ENGINE_API_BASE_URL`; accept injectable `fetch` for tests.
2. Session lifecycle: `POST /sessions` on construct or first `init`; keep `session_id`;
   `reset` → `POST .../reset` (same session); `dispose()` → `DELETE .../{id}` (204).
3. Map EngineAdapter methods to T-049 bodies:
   - `init` → `{ config, seed? }` → Snapshot
   - `step` → `{ order_qty }` → DayDelta
   - `step_n` → `{ orders }` → unwrap `{ deltas: DayDelta[] }`
   - `reset` → optional `{ config, seed? }` → Snapshot
4. JSON + `Content-Type: application/json`; never return / POST economics / PnL / ghost /
   heatmap. Economics stay on `ViewModelProjector.setEconomics`.
5. Do **not** implement PyodideAdapter (T-055).

## New files

- `web/src/engine/httpAdapter.ts` (RED stub + lifecycle docs)
- `web/src/engine/httpAdapter.test.ts`
- `.team/qa/T-056.md`
- `.team/qa/T-056-tests.md`

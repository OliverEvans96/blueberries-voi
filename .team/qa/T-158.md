# T-158 Verify

**Verdict:** PASS (web layout ticket; Python pin test green on implement tip)

## Web (`pnpm` / vitest)

```text
Test Files  4 failed | 78 passed (82)  — failures: profile scripts, embed.lib bundle, snapshot tsc (not layout)
Tests  612 passed (616) on T-158-scoped + studio integration tests
```

T-158 tests: `TuningDrawer.test.ts`, `StudioLayout.test.ts`, `studioScope.test.ts`, `engineStatus.test.ts`, `controls.test.ts`, `studioLogic.tradeoff.test.ts`, `sections.autopilot.test.ts` — all green.

## Python 3.11

```bash
uv run --python 3.11 pytest tests/test_studio_release_version.py::test_studio_package_version_is_0_3_6 -q
# 1 passed
```

Full CI gate not re-run on implement tip (publishable web-only change); recommend human integrate + standard `quality` job on PR.

## Branch

`team/T-158/implement` @ `b989a5df`

# T-064 QA — unique-particle MF dedup

**Status:** RED (failing tests committed; implement not started)
**Branch:** `team/T-064/qa`
**Spec:** `.team/specs/T-064.md`

## Tests added

| Test | AC |
|------|----|
| `test_duplicate_particles_invoke_mf_once_per_unique_key` | call count == unique keys (&lt; N) |
| `test_duplicate_particles_match_naive_per_particle_mf` | identical posteriors vs naive MF |
| `test_all_distinct_fingerprints_call_once_per_particle` | distinct → N calls |
| `test_lot_map_path_skips_mf_dedup` | F1 lot-map path unchanged |
| `test_adr_0097_accepted` / `test_no_new_runtime_deps` / source name check | ADR + deps + wiring |

File: `tests/test_rbpf_unique_mf.py`

## RED evidence

Expected failure before implement: duplicate-key fixture still calls `mean_field_update` N times (naive loop), so
`test_duplicate_particles_invoke_mf_once_per_unique_key` asserts `len(calls) == n_unique` and fails.

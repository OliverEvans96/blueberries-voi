# T-047 QA — Pyodide worker RPC + browser budget smoke (RED)

DATE: 2026-08-12  
STATUS: **RED**  
ROLE: qa  
BRANCH: `team/T-047/qa`  
WORKTREE: `.worktrees/T-047-qa`

## Parents (Wave 2 integrate)

| Tip | SHA |
|-----|-----|
| `team/T-045/implement` | `8d80f52e4fc51f0ff332329423f26b161ff8a270` |
| `team/T-046/implement` | `459859514b39704870d45e55a530efcbbdf3e606` |

Merge commit on this branch: `T-047 (qa): merge T-046 implement into T-045 tip for Wave3`
(tip before this QA commit = merge of both Wave 2 implement tips).

## Spec / ADRs / plan

- `.team/specs/T-047.md`
- ADR [0099](../adr/0099-eng-01-dual-runtime-ap.md) (demo budgets, worker-only Pyodide)
- ADR [0100](../adr/0100-simulator-export-contract.md) (JSON Snapshot/DayDelta; no deep toJs)
- ADR [0101](../adr/0101-eng-01-packaging-pyodide-wheels.md) (Pyodide **314.0.4**, Release wheel)
- Plan `.team/plans/ENG-01-dual-runtime.md`

## Pytest invocation (RED evidence)

```bash
cd .worktrees/T-047-qa
uv sync --all-extras
uv run pytest tests/test_t047_pyodide_worker_rpc.py \
  -o addopts='-ra --strict-markers --strict-config' -v
```

Result: **11 failed**, **1 passed** — missing worker JS / session RPC module /
main-thread harness / demo budget smoke script (asserted via `pytest.fail` /
`AssertionError` about absent artifacts). Not collection/import typos.

Passing guard: `test_no_pyodide_or_wasm_modules_under_src` (M2 closeout still holds;
worker must land outside `src/`).

## Coverage of acceptance criteria

- Worker script loads Pyodide **314.0.4**, installs Release/slim wheel, binds one
  `EngineSession`, answers `init` / `step` / `step_n` / `reset` / `act`
  → `tests/test_t047_pyodide_worker_rpc.py::test_worker_script_exists_outside_src`
  → `tests/test_t047_pyodide_worker_rpc.py::test_worker_pins_pyodide_314_and_binds_engine_session`
  → `tests/test_t047_pyodide_worker_rpc.py::test_worker_or_docs_mention_slim_release_wheel_install`
  → `tests/test_t047_pyodide_worker_rpc.py::test_rpc_init_step_step_n_reset_act_json_protocol`
  — currently failing: no `packaging/pyodide/worker.js` (or `web/` candidate) and no
    `packaging/pyodide/session_rpc.py` JSON RPC mirror

- RPC payloads are JSON strings / JSON-cloneable; no nested PyProxy / deep toJs
  → `tests/test_t047_pyodide_worker_rpc.py::test_rpc_payloads_are_json_strings_or_cloneable_not_deep_tojs`
  → (also asserted inside `test_rpc_init_step_step_n_reset_act_json_protocol`)
  — currently failing: missing RPC module

- Main-thread smoke harness never calls `pyodide.runPython` for per-click physics;
  only `postMessage`s to the worker
  → `tests/test_t047_pyodide_worker_rpc.py::test_main_thread_harness_postmessage_only_no_runpython`
  — currently failing: no main-thread harness under `packaging/pyodide/` or `web/`

- Browser demo budget preset smoke: `init` + one `step` + `step_n` (≥2 orders);
  dialed budgets (not production `N=2000`); pass/fail exit
  → `tests/test_t047_pyodide_worker_rpc.py::test_demo_budget_smoke_script_exists_with_pass_fail`
  → `tests/test_t047_pyodide_worker_rpc.py::test_demo_budget_rpc_smoke_init_step_step_n_under_caps`
  → `tests/test_t047_pyodide_worker_rpc.py::test_rpc_rejects_or_documents_production_n_not_used_in_smoke`
  — currently failing: missing smoke script / RPC module

- Worker message protocol `{id, method, params}` / `{id, ok, result|error}`
  → `tests/test_t047_pyodide_worker_rpc.py::test_worker_message_protocol_documented_in_worker_or_rpc_module`
  → `tests/test_t047_pyodide_worker_rpc.py::test_rpc_error_envelope_for_unknown_method`
  — currently failing: missing worker/RPC/docs; unknown-method envelope needs RPC module

## Not covered by tests

- Live headless browser / real Pyodide 314 download in CI — optional once smoke
  script exists; contract tests use a Python `session_rpc` mirror so implement can
  green without a full browser runtime in every pytest run. Full in-tab studio
  chrome stays T-057 / mockup.
- HttpAdapter / ASGI (T-050+) and D3 `PyodideAdapter` (T-055) — consume this
  worker artifact later.
- Editing live `.github/workflows/` — not required for T-047 AC; smoke may be a
  local script with exit code.

## Notes for implementer

Preferred layout (outside `src/blueberries_voi/`):

| Artifact | Suggested path |
|----------|----------------|
| Worker | `packaging/pyodide/worker.js` |
| Main harness | `packaging/pyodide/main.js` (or `smoke_main.js`) |
| JSON RPC mirror (pytest) | `packaging/pyodide/session_rpc.py` with `handle_rpc` |
| Smoke | `scripts/smoke_pyodide_worker.py` or `packaging/pyodide/smoke.py` |
| Docs | `packaging/pyodide/README.md` |

`handle_rpc` request/response must match T-047 Interfaces. Payloads via
`json.dumps` / structured clone — no deep `toJs`, no main-thread PyProxy.
Default budgets = `DEMO_BUDGETS` (≤200 / H≤7 / paths≤2 / radius≤1).
Do **not** add `*pyodide*` / `*wasm*` modules under `src/` (M2 closeout).

## New files

- `tests/test_t047_pyodide_worker_rpc.py`
- `.team/qa/T-047.md`

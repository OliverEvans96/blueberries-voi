# T-033 QA — Multi-scenario closed-loop + L remeasure

DATE: 2026-08-12
STATUS: RED

## Spec under test

`.team/specs/T-033.md` + M2 plan eval scenarios (P1 / B-state / Rung 0) +
FIL-13 L remeasure under SW+rollout + T-021 / ADR 0091 mean_field lock

## AC locks (fixture / interface)

| Lock | Frozen value | Rationale |
| --- | --- | --- |
| Primary scenarios (order) | `P1` → `B-state` → `Rung 0` | M2 “multi-scenario” belief eval; aliases allowed (`rung0`, `b_state`, …) |
| Other masks | `P0`, `F1`, `F1s`, `F2a`, `F2` | Interface smoke only — no full profit claims |
| Runner API | `run_m2_multi_scenario(*, root_seed, ...) -> MultiScenarioResult` | Spec Interfaces |
| Preferred module | `blueberries_voi.sim.m2_multi_scenario` (fallback `viz.m2_multi_scenario`) | Outside `controller/`; mirrors ladder |
| Other-mask smoke | `smoke_other_masks` / `smoke_mask_interfaces` **or** runner kwargs `other_masks=` / `include_other_masks=` / `smoke_masks=` | Spec: smoke only |
| Artifact roots | `experiments/` and/or `figures/m2/` (L notes also `.team/reports/`) | Never under `controller/` |
| Empirical L | Recorded in MD under SW+rollout (or documented CTL config); numeric p50/p90/max or explicit L | FIL-13 follow-up |
| Production backend | `mean_field` | T-021 / ADR 0091; report states no silent joint revert |
| Physics / belief | Shared `day_step`; `shelf_belief_from_rbpf` + `shelf_belief_from_oracle` | Spec AC |
| Runtime deps | `{matplotlib, numpy, pyarrow, scipy}` | No new deps |
| CI smoke knobs | Prefer `n_burn=1`, `n_score=2`, `n_rollout_paths=1`, `H=2` when accepted | Non-plot core pytest smoke |

## Commands run (qa RED check — ticket tests only)

| Command | Result |
| --- | --- |
| `uv run pytest tests/test_m2_multi_scenario.py -v --no-cov` | **15 failed, 3 passed** (purity/deps guards green; multi-scenario runner missing) |

Full-suite / coverage ≥80% deferred to implement + verifier (per ticket start: do not run full pytest).

## Coverage of acceptance criteria

- Closed-loop runs publish a short MD report comparing **P1** vs **B-state** vs **Rung 0** (primary scenarios); other masks have interface smoke only (no full profit claims required)
  → `tests/test_m2_multi_scenario.py::test_run_m2_multi_scenario_is_exportable` — currently failing: missing `sim.m2_multi_scenario` / `run_m2_multi_scenario`
  → `tests/test_m2_multi_scenario.py::test_multi_scenario_result_type_is_exportable` — currently failing: missing `MultiScenarioResult`
  → `tests/test_m2_multi_scenario.py::test_run_m2_multi_scenario_signature_requires_root_seed` — currently failing: missing runner
  → `tests/test_m2_multi_scenario.py::test_run_m2_multi_scenario_smoke_compares_p1_bstate_rung0` — currently failing: missing primary-scenario result labels
  → `tests/test_m2_multi_scenario.py::test_run_m2_multi_scenario_publishes_md_comparing_primary_scenarios` — currently failing: missing MD writer
  → `tests/test_m2_multi_scenario.py::test_other_masks_have_interface_smoke_only` — currently failing: missing other-mask smoke surface

- Report lives under `experiments/` and/or `figures/m2/` — **not** under `controller/` (L MD may also use `.team/reports/`)
  → `tests/test_m2_multi_scenario.py::test_multi_scenario_default_artifact_path_outside_controller` — currently failing: missing DEFAULT path
  → `tests/test_m2_multi_scenario.py::test_run_m2_multi_scenario_writes_artifacts_outside_controller` — currently failing: missing writer
  → `tests/test_m2_multi_scenario.py::test_multi_scenario_runner_does_not_live_in_controller` — currently passing (guard)
  → `tests/test_m2_multi_scenario.py::test_controller_package_has_no_matplotlib_or_parquet_after_multi_scenario` — currently passing (guard)

- Empirical **L** under SW+rollout (or documented controller config) is recorded in the report
  → `tests/test_m2_multi_scenario.py::test_empirical_l_under_sw_rollout_recorded_in_md` — currently failing: missing runner / L MD

- Report explicitly states production age backend remains **mean_field** (T-021 / ADR 0091); no recommendation to silently revert to joint
  → `tests/test_m2_multi_scenario.py::test_multi_scenario_config_production_backend_is_mean_field` — currently failing: missing backend field on result/module
  → `tests/test_m2_multi_scenario.py::test_multi_scenario_module_source_does_not_silently_select_joint` — currently failing: missing module
  → `tests/test_m2_multi_scenario.py::test_published_md_states_mean_field_and_rejects_silent_joint_revert` — currently failing: missing MD wording

- Primary physics path remains `day_step`; belief path uses `ShelfBelief` factories
  → `tests/test_m2_multi_scenario.py::test_multi_scenario_forward_path_uses_shared_day_step` — currently failing: missing module to AST-scan
  → `tests/test_m2_multi_scenario.py::test_multi_scenario_belief_path_uses_shelf_belief_factories` — currently failing: missing `shelf_belief_from_rbpf` / `_oracle` wiring

- `uv run pytest` smoke for the runner’s non-plot core passes (full MD may be experiment-only)
  → `tests/test_m2_multi_scenario.py::test_run_m2_multi_scenario_non_plot_core_smoke` — currently failing: missing runner
  → `tests/test_m2_multi_scenario.py::test_no_new_runtime_dependencies_for_t033` — currently passing (deps lock)
  → ticket test module collected under default `tests/` layout (verify by full pytest after GREEN)

## Not covered by tests

- Exact seed/replication count for the published MD — open question in the spec; document in report header at implement time.
- Dollar VOI tables across all M1.5 rungs / SCN-B-clair / reopening joint / Pyodide — out of scope.
- Full-repo coverage ≥80% — verifier gate after implement.

## Incomplete

(none — STATUS RED pending implement)

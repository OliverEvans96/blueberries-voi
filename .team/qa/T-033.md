STATUS: FAIL

## Commands run
- `uv sync --all-extras` → exit 0, packages resolved/checked
- `uv run ruff check .` → exit 1, I001 unsorted imports in `tests/test_m2_multi_scenario.py:609` (fixable)
- `uv run ruff format .` → exit 0, 89 files left unchanged
- `uv run mypy src tests` → exit 0, Success: no issues found in 65 source files
- `uv run pytest` → exit 0, 433 passed, 1 skipped, coverage 88.91% (≥80%)

## Acceptance criteria
- [x] Closed-loop runs publish a short MD report comparing **P1** vs **B-state** vs **Rung 0** (primary scenarios); other masks have interface smoke only — verified by `uv run pytest` (`tests/test_m2_multi_scenario.py`: smoke compares + publishes MD + other-mask smoke)
- [x] Report lives under `experiments/` and/or `figures/m2/` — **not** under `controller/` — verified by pytest (`test_multi_scenario_default_artifact_path_outside_controller`, `test_run_m2_multi_scenario_writes_artifacts_outside_controller`); default path `experiments/m2_multi_scenario_result.md`
- [x] Empirical **L** under SW+rollout (or documented controller config) is recorded in the report — verified by pytest (`test_empirical_l_under_sw_rollout_recorded_in_md`)
- [x] Report explicitly states production age backend remains **mean_field** (T-021 / ADR 0091); no recommendation to silently revert to joint — verified by pytest (config backend + MD wording + source guard tests)
- [x] Primary physics path remains `day_step`; belief path uses `ShelfBelief` factories — verified by pytest (AST/source wiring tests) and module imports `day_step` / `shelf_belief_from_*`
- [x] `uv run pytest` smoke for the runner’s non-plot core passes — verified by full `uv run pytest` including `test_run_m2_multi_scenario_non_plot_core_smoke` (19/19 ticket tests green)

## Incomplete
- Serial gate **`uv run ruff check .` failed** — I001 import order in `tests/test_m2_multi_scenario.py` (local imports inside `test_rung0_arm_orders_positive_on_empty_shelf`: `scipy` must precede first-party). Implement tip `07ed764` is otherwise mypy/pytest-green and review is APPROVED round 2; verifier did not autofix. Re-run verify after a one-line ruff fix commit.

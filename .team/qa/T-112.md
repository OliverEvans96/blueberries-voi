STATUS: PASS

Tip: `team/T-113/implement` after xdist/mock-horizon follow-up (includes T-112 `bad4b6a`). Python 3.11.13. Live CI argv; no workflow edits.

## Commands run

- `uv run --python 3.11 ruff check .` → exit 0
- `uv run --python 3.11 ruff format --check .` → exit 0 (190 files already formatted)
- `uv run --python 3.11 mypy src tests` → exit 0 (153 source files)
- `uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` → exit 0, **985 passed**, 2 skipped, coverage **87.45%** (≥80%)

Earlier FAIL on this ticket was xdist+coverage dual-import monkeypatch flakes (T-079 / T-084 / T-089 / T-097), not T-112 horizon tests. Those spies now patch `fn.__globals__`. Mock adapter now refuses at day 90 like Python `EngineSession`.

STATUS: PASS

## Commands run

- `uv sync --all-extras --python 3.11` → exit 0, CPython 3.11.13 venv + editable install (python3.11 not on PATH; uv pin used)
- `uv run --python 3.11 ruff check .` → exit 0, All checks passed
- `uv run --python 3.11 ruff format --check .` → exit 0, 141 files already formatted
- `uv run --python 3.11 mypy src tests` → exit 0, Success: no issues found in 108 source files
- `uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` (verify run 1) → exit 1, 2 failed / 764 passed / 1 skipped; coverage 85.39%. Transient failures in `tests/test_t071_demo_hydrate_edges.py` (init/reset client shipment preservation); treated as flake after re-run.
- `uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` (orchestrator re-run on this worktree) → exit 0, 766 passed, 1 skipped; coverage 85.39%. Same two T-071 tests PASS when run alone.
- `cd web && npm install && npx vitest run src/engine/projector.test.ts src/sections.belief.test.ts` → exit 0, 17 passed (2 files)

Review: APPROVED on `team/T-090/review` at `54a42d5` (artifact `.team/reviews/T-090.md` on that branch).

## Acceptance criteria

- [x] Belief heatmap x-domain from `tau_edges` is age days (~0..8), not `[0, L]` — verified by vitest `projector.test.ts` age×count rebin suite
- [x] `BeliefGrid.density` shaped `K × C`; `tau_edges === centersToEdges(tau_grid)`; integer-friendly `count_edges` — verified by vitest density / edges cases
- [x] Rebin deposits lot mass into nearest-integer count bin for `n_l` — verified by vitest deposit case
- [x] Merged age marginal length `K` with sum ≈ Σ lot_counts when rows normalize — verified by vitest `ageMarginalFromFlat`
- [x] Age-marginal chart shares `tau_edges` domain; mounts as top marginal above heatmap — verified by vitest `sections.belief.test.ts` (module + `main.ts` source order)
- [x] Truth markers at `(lot.tau, lot.n)` on age×count scales — verified by vitest truth-scale + `beliefAgeCount.ts` source contract
- [x] Belief `plotIds` include age-marginal + heatmap; blurb mentions age×count vs truth and age marginal — verified by vitest section contracts
- [x] No Python / wire FlatBelief shape change (`age_marginals` stays `L*K`) — implement tip touches only `web/`; green full pytest re-run without schema edits
- [x] Vitest covers projector rebin + age-marginal; former `L×K` lot-index assertions superseded — verified by vitest 17/17 green on ticket files

## Incomplete

(none)

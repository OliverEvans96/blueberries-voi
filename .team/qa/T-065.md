# T-065 QA — NumPy sequential-WOR DP

**Status:** RED (failing structural gate; numeric identity already holds vs ref)
**Branch:** `team/T-065/qa`
**Spec:** `.team/specs/T-065.md`

## Tests added

| Test | AC |
|------|----|
| `test_numpy_dp_matches_frozen_python_ref` | tight allclose vs frozen Python DP |
| `test_boundary_cases_match_ref` | L=0 / sales bounds / stockout |
| `test_fixed_weights_not_remaining_times_weights` | allocate_sales weight law |
| `test_composition_probs_align_with_allocate_sales_monte_carlo` | tiny-shelf MC check |
| `test_public_signature_unchanged` | API unchanged |
| `test_implementation_is_numpy_vectorized_not_python_unpack_loop` | NumPy rewrite structural RED |
| `test_no_new_runtime_deps` | no numba/cython |

File: `tests/test_sequential_wor_numpy.py`

## RED evidence

Current production still uses nested `def unpack` / `enumerate(cur)` loops. Structural test fails until NumPy rewrite. Numeric tests should already pass against the frozen ref (same algorithm).

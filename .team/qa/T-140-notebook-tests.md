# T-140 notebook shard — RED criterion

`experiments/notebook_14_source.md` must mention calendar pack date and thermal fleet after implement.

RED (source grep until notebook rebuild):

```bash
grep -E 'calendar|thermal|phi_bar|Stage C' experiments/notebook_14_source.md
```

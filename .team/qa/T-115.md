STATUS: PASS

## Commands run

- `uv sync --all-extras --python 3.11` → exit 0, CPython 3.11.13, 147 packages installed
- `uv run --python 3.11 ruff check .` → exit 0, All checks passed
- `uv run --python 3.11 ruff format --check .` → exit 0, 188 files already formatted
- `uv run --python 3.11 mypy src tests` → exit 0, Success: no issues found in 151 source files
- `uv run --python 3.11 pytest -n auto --cov=blueberries_voi --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=80` → exit 0, **958 passed**, 2 skipped, 4 warnings; **coverage 87.34%** (≥80%)
- `cd web && npm install && npm test` → exit 0, vitest **26 files / 201 tests passed** (11.46s) at verify tip; `main` has since grown (audit: 33 files / 255 passed)

Python: 3.11.13. Implement tip verified: `4517eab2d5929b01d80bfb66ebf9002d879531e9` (`team/T-115/verify`).

## Acceptance criteria

- [x] Fresh load with empty `localStorage` key `blueberries-voi-studio-show-truth` has `showTruth === false` — `web/src/showTruth.test.ts` (`loadShowTruth defaults to false when storage is empty`); included in `npm test` (201 passed)
- [x] `saveShowTruth(true)` then `loadShowTruth()` returns true; stored value exactly `"true"` / `"false"` — same file, round-trip test; `npm test`
- [x] Belief heatmap draws `.truth-cross` (or equivalent) when truth lots passed, **zero** overlay nodes when `truthLots=[]` — `web/src/charts/beliefAgeCount.test.ts`; `npm test`
- [x] Survival rug, history lot circles, and arrival-prior `age_at_receipt` marks length 0 when gated off, `.truth-*` when lots/receipts passed — `web/src/charts/survival.test.ts`, `history.test.ts`, `arrivalPrior.test.ts`; `npm test`
- [x] Inventory vs target and age-composition use belief expected counts/ages when show-truth off, `history[].lots` / live lots when on — `web/src/charts/inventoryTarget.test.ts`; `npm test`
- [x] Projector `belief_history` length tracks the same rolling window as `history` after `applyDelta`/`applySnapshot`; fixtures omit `showTruth` — `web/src/engine/projector.test.ts` (14 tests in suite); `npm test`
- [x] Play chrome control accessible name `/show true state/i`, `role="switch"`, `aria-pressed` reflects flag; `#app` has `studio--show-truth` iff on — `web/src/controls.showTruth.test.ts`; `npm test`
- [x] Belief blurb does **not** require the word “truth” when show-truth is off — `web/src/sections.belief.test.ts` (`does not require the word truth`); `npm test`
- [x] No production edits under `crates/`, `src/blueberries_voi/simulator/`, or wasm adapters — `git diff ec7143c..HEAD -- crates/ src/blueberries_voi/simulator/ web/src/engine/pyodideAdapter.ts web/src/engine/httpAdapter.ts` empty

## Incomplete

- None.

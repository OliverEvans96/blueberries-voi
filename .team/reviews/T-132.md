# T-132 review

**Verdict:** APPROVED

## Scope check

- `protection_demand_quantile` extended with `start_day`, MC path, fast paths — matches spec.
- Python `demand_fractile.py` + Rust `policy.rs` parity with shared seed derivation.
- SW controllers wire `start_day=day` in Python; Rust `damped_sw_order_f_belief` passes `day`.
- PyO3 `protection_demand_quantile_py` for golden test.

## Notes

- `alpha_tune` PyO3 path still uses default `ModelParams` without profile (documented out of scope).
- FreshNet Thu 4-day window is not always > flat μ (week-0 scale); test uses synthetic weekend uplift profile.

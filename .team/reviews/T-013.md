STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [arrival_priors.py:110-124] Wired `delivery_birth_age_prior` calls
  `arrival_age_prior_f2a` without `as_of` / `receipt_date`. Production F2a
  therefore only gates on **presence** of `pack_date` and centres a
  `F2A_TRANSIT_UNCERTAINTY_SD=0.75` Gaussian on the cold Abdella mean — two
  different pack dates yield identical birth priors. Helper calendar path is
  correct when `as_of` is passed (unit tests do). RichObs has no receipt clock
  today, so this matches the documented fallback and still meets the AC
  “narrower than cold mix,” but plan §3.3 “implied transit/age band” will need
  a receipt/`as_of` channel (or ASN age-hint encoding) before Stage A (T-016)
  can credit pack-date location, not just width.
- [tests/test_arrival_priors.py:326-351]
  `test_no_new_soft_sales_waste_terms_tied_to_pack_or_receipt` AST gate looks
  for legacy soft symbols (`sales_pow` / `waste_pow` / …) ∩
  `pack_date`/`age_at_receipt`. Those soft names are already gone from the MC
  LL path, so the assert is easy to satisfy. The stronger guard is
  `test_sales_waste_weights_unchanged_when_only_prior_channel_differs` — keep
  that if the AST check is ever dropped.
- [filter/__init__.py] Public re-exports cover the spec helpers
  (`arrival_age_prior_f2a` / `arrival_age_prior_f2`) only;
  `delivery_birth_age_prior`, `cold_abdella_arrival_age_prior`, and
  `F2A_TRANSIT_UNCERTAINTY_SD` stay module-private. Fine for T-013; export if
  Stage A scripts need them without reaching into `arrival_priors`.

## Checklist

| Check | Result |
| --- | --- |
| F2a prior narrower than cold Abdella (SD/HPD) | Pass |
| F2 Dirac / ≥0.95 mass on nearest bin(s) | Pass |
| P0/P1 masked → cold Abdella; F2a/F2 do not fire | Pass |
| Birth prior only after LL; no pack/receipt soft LL terms | Pass |
| F2 tighter than F2a on same fixture | Pass |
| Interfaces + documented F2a SD / F2 Dirac | Pass |
| Tests substantive (not empty asserts) | Pass |
| Targeted ruff/mypy + `tests/test_arrival_priors.py` | Pass (9) |

## Verification re-run

```text
uv run pytest tests/test_arrival_priors.py -q --no-cov
→ 9 passed

uv run ruff check src/blueberries_voi/filter/arrival_priors.py \
  src/blueberries_voi/filter/backends.py \
  src/blueberries_voi/filter/__init__.py tests/test_arrival_priors.py
→ All checks passed

uv run mypy src/blueberries_voi/filter/arrival_priors.py \
  src/blueberries_voi/filter/backends.py \
  src/blueberries_voi/filter/__init__.py tests/test_arrival_priors.py
→ Success: no issues found in 4 source files
```

## Verdict

T-013 acceptance criteria are met. F2a/F2 write only into the cohort-birth
`age_post` channel via `delivery_birth_age_prior` after LL; masked P0/P1 stay
on the cold Abdella mix; F2 is Dirac; F2a width is documented and strictly
narrower. No blocking gaps. Wire a receipt/`as_of` (or age-hint) path before
treating F2a prior *location* as pack-date-informed in Stage A.

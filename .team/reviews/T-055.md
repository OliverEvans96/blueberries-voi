STATUS: APPROVED
ROUND: 1

Reviewed: `team/T-055/implement` @ `fce32e979a8e643175e4f4b29601ceda05d130a1`
Base: `main` (ticket stack); implement delta vs QA tip is `web/src/engine/pyodideAdapter.ts` only.
Focus: PyodideAdapter Worker RPC, Snapshot/DayDelta, DEMO_BUDGETS defaults, no main-thread Pyodide.

## Blocking

(none)

## Non-blocking

- [web/src/engine/pyodideAdapter.ts:54-57,125-127,154-155] Adapter advertises `wheelUrl` via Worker query + optional `configure` / `init` params, but `packaging/pyodide/worker.js` still hardcodes `SLIM_WHEEL_URL` and rejects `configure` (`UnknownMethod`). Default Release URL matches; fixture-wheel override is ineffective until the worker reads the query/param (T-047 follow-up).
- [web/src/engine/pyodideAdapter.ts:198-220] `runPyodideAdapterSmoke` is a clear pass/fail export (meets QA’s existence bar) but is not a CI job and does not itself pin 314.0.4 — relies on the packaging worker when run live.
- [web/src/engine/pyodideAdapter.ts:156-180] RPC results are `as Snapshot` / `as DayDelta` after JSON parse, not schema-validated; acceptable at the adapter boundary given FakeWorker shape checks + projector forbidden-key tests.

## Acceptance criteria

| Criterion | Verdict |
|-----------|---------|
| `PyodideAdapter` implements `EngineAdapter` and forwards to worker RPC | Pass — `init`/`step`/`step_n`/`reset`/`act` via `{id,method,params}` JSON `postMessage` |
| Adapter never holds a PyProxy on the main thread; only plain data / JSON | Pass — no `loadPyodide`/`runPython`; JSON string wire; vitest source + clone guards |
| `init`/`step`/`step_n`/`reset`(/`act`) return Snapshot/DayDelta for projector | Pass — FakeWorker returns ADR 0100 shapes; forbidden presentation keys asserted |
| Integration smoke: Release/fixture wheel under Pyodide 314.0.4, init+step pass/fail | Pass — worker pins 314.0.4 + micropip; `runPyodideAdapterSmoke` exported |
| Demo budget preset is the default for this adapter | Pass — `DEFAULT_DEMO_BUDGETS` = ADR 0099 caps; merged into `init` when opts omitted |
| Out of scope: no HttpAdapter / no studio default switch | Pass — adapter module only |

## Evidence

- `cd web && npm test -- src/engine/pyodideAdapter.test.ts` → **10 passed**

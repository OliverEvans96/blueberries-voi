STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [backends.py:196-200] Sales Poisson μ blends MC `day_step` sales with
  `0.5 * min(on_hand, sales_obs + 1)` — observation leaks into the predictive
  mean. Acceptable for AC (shared kernels + soft symbols gone) but not a pure
  forward MC score; revisit if ESS / Stage A honesty needs a cleaner μ.
- [backends.py:110-131, 182] Waste uses expected deaths on start-of-day counts
  (pre-sales), not full joint MC waste from `day_step`. Documented sellout
  workaround; physics is shared `death_prob_survival_ratio` but not the
  allocate-then-spoil joint. Fine for T-011; note for Stage A narratives.
- [types.py:223-241] `coerce_p1_obs` / `p1_obs_from_rich` remain with
  “until T-011” wording. Unused on production particle filter / `_particle_filter_update` /
  `BootstrapPFBackend` paths — safe to delete or reword in a cleanup ticket.
- `tv_vs_exact` still embeds soft powers under renamed locals and is still
  called from `run_microbench` for the TV column; docstring correctly marks
  NON-GATE / T-012 owns generative Stage C.

## Checklist

| Check | Result |
|-------|--------|
| Soft symbols absent from `_particle_filter_update` / `BootstrapPFBackend.predict_update` | Pass (AST + source grep) |
| `observation_loglik_mc` shared kernels; skips `UNOBSERVED` | Pass |
| ResearchParticleFilter production path no `coerce_p1_obs` UNOBSERVED→0 | Pass |
| No Wallenius density (FIL-10 simulate-only) | Pass |
| `tv_vs_exact` NON-GATE / not production Stage C | Pass |
| Default `n_mc=1`; `FilterSummary.ess` | Pass |
| ENG-02 same `day_step` symbol | Pass |
| Tests substantive (not empty asserts) | Pass |

## Verification re-run

```text
uv run pytest tests/test_mc_likelihood.py tests/test_filter.py -q --no-cov
→ 20 passed

uv run ruff check src/blueberries_voi/filter
→ All checks passed

uv run mypy src/blueberries_voi/filter
→ Success: no issues found in 4 source files
```

## Verdict

T-011 acceptance criteria are met. Soft LL is off the production weight path;
MC LL binds shared model kernels and skips masked fields; FIL-10 density ban
holds; Stage C soft tautology is quarantined. Honesty approximations above do
not block this ticket.

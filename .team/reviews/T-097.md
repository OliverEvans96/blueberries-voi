STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [session.py:271] `alpha` / `rho` are parsed for every policy (including `constant`) before the branch; harmless today, but unused kwargs on constant could surprise callers if invalid floats are passed.
- [tests/test_t097_act_damped_sw.py:141] `_capture_rollout_base` swallows all rollout exceptions and returns `0`; fine for base-policy isinstance locks, but could hide unrelated rollout regressions in the same test.
- EngineSession still defaults omitted `policy` to `constant` (`session.py:267`); ADR 0112 Autopilot UI default `damped_sw` is correctly left to T-098+ — no AC miss.

## Verdict notes

Diff `70364ee...9be30d9` is a focused `session.py` change against qa RED tests.

| AC | Evidence |
|----|----------|
| `damped_sw` / `sw` act → DayDelta | aliases at `session.py:285–297`; covered by `test_act_damped_sw_aliases_return_day_delta` |
| alpha/rho defaults 0.9/0.8 + overrides | `session.py:271–272`, ctor kwargs `:286–289`; spy tests |
| rollout base = `DampedSurvivalWeightedPolicy` | `session.py:299–304` (was `ConstantOrderPolicy(0)`); isinstance + spy tests |
| constant / unknown-policy copy | constant branch unchanged; error at `:323` mentions `damped_sw` + `rollout` |
| dial budgets H / paths / radius / n_particles | unchanged header of `_select_order` `:256–265` → rollout kwargs |
| ASGI POST act + budgets | existing `app.py` forward; `test_asgi_act_damped_sw_with_alpha_rho_budgets_returns_200_day_delta` |
| packaging stale allowlist | session error string fixed; worker-smoke scan test |

Matches ADR 0112 policy alias set and M2 damped-SW rollout base. Test map in `.team/qa/T-097-tests.md` is covered with real assertions (isinstance, approx alpha/rho, DayDelta shape, ASGI 200). Verifier owns runtime gates.

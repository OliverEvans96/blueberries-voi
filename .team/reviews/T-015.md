STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [experiments/m25_l_remeasure.md] L table cites approximate M1/scenario orders (p50/max) rather than a checked-in remeasure script or numeric artifact; fine for the AC “note addendum,” but Stage A honesty later may want a reproducible remeasure command.
- [src/blueberries_voi/filter/rbpf.py] Fallback is re-evaluated on construct / `initialize(L=…)`, not each `step` as live L changes — matches an open question in the spec; document sticky-vs-daily when Stage A wires long-dwell cells.
- [src/blueberries_voi/filter/backends.py] `SlidingWindowBackend.window` (default 3) is unused by the shared `_rbpf_update` path; experiment note’s “window=3” is the dataclass default only. Pre-existing bakeoff shape — not a T-015 regression.
- [tests/test_l_fallback.py] `test_choose_backend_never_silently_truncates_l`’s final `assert not (full_joint and L < requested)` is redundant after asserting `sliding_window` + `L == requested`; keep the strong asserts, drop or strengthen the tautology.
- [tests/test_l_fallback.py] Structured logging (`logger.info` + `extra={K,L,N,…}`) is not asserted; AC allows return field (`backend_choice`), which is covered.
- Process: `.team/changelog.md` does not yet have a plain-English T-015 entry (DoD for verifier / write-changelog after gates).
- Workspace still has expected T-016 RED elsewhere; T-015-scoped tests (`tests/test_l_fallback.py` + `tests/test_filter.py`) pass.

## Correctness

- `choose_backend` keeps `full_joint` iff `joint_state_count(K,L,N) ≤ MAX_JOINT_FLOATS`, else `sliding_window`, preserving `L` (no truncate).
- RBPF `__post_init__` / `initialize` call `_apply_backend_choice`, swap to `SlidingWindowBackend` without `MemoryError`, expose `backend_choice`, and log structured reason on fallback.
- `FullJointBackend.guard_joint_memory` remains a safety net; production RBPF never selects joint when over budget. Boundary `≤` / `>` aligns with the guard.
- `PRODUCTION_BACKEND` stays `"full_joint"` (FIL-12=B not reopened).

## Contract adherence

- Matches ADR 0089 and T-015 interfaces (`BackendChoice`, `choose_backend`, RBPF exposure).
- Exports from `filter/__init__.py`; bakeoff addendum present at `experiments/m25_l_remeasure.md`.
- Open questions (window default, sticky vs daily) left open as documented — not AC failures.

## Security

- No secrets, injection, or unsafe I/O. Logging `extra` keys do not collide with reserved `LogRecord` attributes.

## Test quality

- Acceptance criteria mapped in `.team/qa/T-015.md` (PASS); 13 focused tests cover budget edge, RBPF construct/initialize fallback, L preservation, FIL-12 preference, frozen record, and experiment note keywords.
- No empty/assertion-free tests that gate the ticket.

STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [`tests/test_arrival_only_count_filter.py:236`] `test_particle_filter_sales_likelihood_multinomial_selectable` only asserts the config field sticks; it does not spy that `log_p_sales_waste_multinomial_given_ages` runs. Wiring in `_particle_filter_update` / `CountsOnlyBackend` is clear on inspection.
- [`src/blueberries_voi/filter/backends.py:508`] When either total is `UNOBSERVED`, particle LL is flat (`0.0`). P0 (sales without waste) therefore loses sales reweighting; P1+ rungs that observe both totals are unaffected. Acceptable for this settle; worth a follow-up if P0 closed-loop scoring matters.
- [`src/blueberries_voi/filter/backends.py:232`] `_apply_lot_map_age_update` remains in the module but is unused on the production path — dead code cleanup later is fine.
- [`src/blueberries_voi/viz/fil11.py`] Still documents production scoring via `observation_loglik_mc`; Stage A / export docs are T-069 scope.

## Checklist (focus)

| Focus | Verdict |
|-------|---------|
| No production `mean_field_update` | Pass — removed from `_particle_filter_update`; AST + P1 spy + thin-caller greps; bakeoff `MeanFieldBackend` now delegates to counts-only update |
| Arrival-only ages | Pass — `age_post` copied / birth-prior only; lot-map age LL path not invoked; P1 TV guards |
| Exact WOR default; multinomial via `sales_likelihood` | Pass — default `exact_sequential_wor` → `log_p_sales_waste_given_ages`; multinomial ablation wired |
| Count transitions not ±1 RW | Pass — `allocate_sales` + `death_prob_survival_ratio`; no `rng.integers(-1, 2, …)` in `_particle_filter_update` |
| Guard supersessions | Pass — production MF / MC / unique-MF / MF-sweep / M2 / belief / filter / l_fallback locks flipped to ADR 0105 |
| `PRODUCTION_BACKEND == "counts_only"` | Pass — `particle_filter.py`, `choose_backend`, ladder / multi-scenario locks |
| Diagnostic MF / MC remain importable | Pass — still exported; exercised off production path |

## Spec / ADR

Meets `.team/specs/T-068.md` acceptance criteria and ADR 0105 (arrival-only ages, counts-only PF, exact sequential-WOR weights, day_step-consistent count kernels, named guard supersessions). Implement tip `e21335f` reviewed vs `main...HEAD` (qa tip `c44413a` + implement).

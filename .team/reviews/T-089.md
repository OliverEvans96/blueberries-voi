STATUS: APPROVED
ROUND: 2

## Blocking

(none)

## Non-blocking

- Round-1 review artifact was not present on this tip (feature was APPROVED at
  `7265668` per orchestrator); this round covers full ticket scope plus the
  verify-fix delta at `f6f3acb`.
- Mock `beliefBlur` still uses a presentation-only sharpness ladder for the six
  ids — fine for the mock host; live HTTP/Pyodide path is what exercises
  `mask_for`.
- Duck-typed hydrate accepts any attribute-shaped object with the four
  ShipmentTrace fields (not only dual-identity dataclasses). Intentional for
  the xdist fix; keep validation errors on bad field values.

## Checklist

| Check | Result |
|-------|--------|
| Chips `P0\|P1\|F1\|F1s\|F2a\|F2`; no P2 under `web/src` | Pass |
| Locked title + description per ADR 0110 | Pass |
| TS `ScenarioId` six-rung; default P1; `ObsScenario` removed | Pass |
| Dirty until Reset; Advance does not re-apply | Pass (projector tests lock) |
| `main.ts` passes staged config to `init`/`reset` | Pass |
| `EngineSession` stores/echoes/validates via `mask_for`; forwards to `advance_day` | Pass |
| `day_driver` richest DayLog + `mask_for` + `rich_obs_from_day_log`; no `P1Obs` | Pass |
| Mask observability / no invented `0`/`{}` | Pass (interactive tests assert) |
| HTTP/Pyodide/mock forward `obs_scenario`; mock drops P2 | Pass |
| SCN-P2 stays Out | Pass |
| No Ticket A chart rebin / age-marginal ownership | Pass (`main.ts` only config pass) |
| Delta: ruff hygiene (`!= 0`, line wraps); spy uses `day_driver.advance_day` | Pass |
| Delta: `_as_shipment_trace` duck-type + always normalize; T-071 regression | Pass |
| Tests assert real behaviour (not empty) | Pass |

## Delta reviewed (`7265668` → `f6f3acb`)

- `api/app.py`: `_as_shipment_trace` + always `_hydrate_shipments` after
  `ensure_demo_shipments` — correct fix for dual class identity under xdist.
- `tests/test_t071_demo_hydrate_edges.py`: duck-type helper + dedicated regression.
- `tests/test_t089_studio_obs_scenarios.py`: format/SIM hygiene; forward-spy
  binds `real` from `day_driver` while patching `session.advance_day`.

# T-127 Review

**Verdict:** APPROVED

## Acceptance criteria

| AC | Status | Notes |
|----|--------|-------|
| AC-layout — Cockpit Grid 3-row shell | PASS | `StudioLayout.tsx` cockpit-grid; always-on Primary/Secondary/Run/Economics/Events; tuning dock tablist + Future chip; all 14 chart ids preserved |
| AC-rust-tradeoff — `tradeoff_forecast` RPC | PASS | `tradeoff.rs` + session dispatch; candidates sweep + joint_hist; ADR 0130 bank resampling |
| AC-rust-events — `events` RPC | PASS | Masked `richest_log` window; `{ days: [...] }` envelope |
| AC-calendar — `previousOrderDayFromSchedule` | PASS | Symmetric to next-order helper |
| AC-obs-mask — `obsMask.ts` | PASS | Rust parity table + applyMask null semantics |
| AC-wire — adapter surface | PASS | `tradeoffForecast` / `events` on adapter + mock + wasm smoke |
| AC-tradeoff-ui — DecisionRail charts | PASS | Option A curve + Option B histogram; drag lookup only |
| AC-events-ui — EventsPane | PASS | Day cards, P0 waste hidden, truth-gated stockout |
| AC-merge — `studioLogic.ts` | PASS | Fetch tradeoff on advance/act/obs catch-up; events on episode/obs change |
| AC-verify — integration gate | PASS | `npm test` 341/341; Python CI parity green on merge tip |

## ADR 0130

Particle-bank sampling for display forecasts only; Autopilot `act` / `rollout_order` unchanged.

## Residual / out of scope

Primary/Secondary sub-day interpolation and truth overlays remain deferred per spec audit.

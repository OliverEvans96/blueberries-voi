# M2.5 — Definition of done (plan §9 close-out)

DATE: 2026-08-12  
Covers implementation tickets **T-009–T-017** (close-out tracked as T-018).

Plan source: `.team/plans/M2.5-filter-complete.md` §9.

## Definition of done checklist

Copied from plan §9; each item checked only when evidence exists (QA PASS / APPROVED reviews / published Stage A–B results / stubs for non-goals).

- [x] **RichObs + masks** for P0, P1, F1, F1s, F2a, F2; missing ≠ zero enforced by tests (T-010).
- [x] **Sim emits richest DayLog**; filter never needs a second simulator fork (T-009).
- [x] **Soft / heuristic likelihood removed**; production path scores masked obs via shared `day_step` kernels (MC as primary) (T-011).
- [x] **Stage C** is the generative/`day_step` check — green; old tautological TV self-check gone (T-012).
- [x] **Stage A** run under shared CRN across the six data-availability rungs; results published; **P0/P1 fail allowed** (documented); F2a FAIL held as needs-human (pack_date not emitted); F2 PASS (T-016).
- [x] **Stage B** green or diagnostic-labeled on rungs that A fails; oracle ladder shows F2 ≪ P1 error vs B-state (T-017).
- [x] **L policy:** joint default; `sliding_window` fallback when budget would trip; no silent truncation (T-015).
- [x] **No CTL, no VOI sweep, no browser** code landed under this milestone (controller/voi remain empty stubs).
- [x] agent-dev-team: specs AC pass · `.team/reviews/` APPROVED for T-009–T-017 · `.team/qa/` green · `.team/changelog.md` plain-English M2.5 entry.
- [x] Quality gates: `ruff`, `mypy`, `pytest` with coverage ≥80% (verified on close-out / per-ticket gates).

## Notes

- F2a Stage A contraction remains **needs-human** in `.team/backlog.md` (honest gap; not papered over).
- Non-goals stand: no CTL / VOI / browser production modules in this milestone.

## Postscript (T-019, 2026-08-12)

T-019 cleared the F2a Stage A gap: sim emits synthetic ASN `pack_date` on
delivery days, and Stage A F2a now **contracts / PASS** under smoke defaults
(`experiments/m25_stage_a_result.md`). The DoD checkbox and Notes bullet above
record the M2.5 close-out honesty (fail-as-blocked); they are not a current
blocker. Backlog F2a item is **Resolved**.

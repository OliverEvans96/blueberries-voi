# T-044 Review — Derived Abdella product + browser extras

DATE: 2026-08-12  
STATUS: APPROVED  
ROUND: 2  
ROLE: review  
BRANCH: `team/T-044/review`  
BASE: `main` (`f4a467f`)…`HEAD` (`b612dca`)  
IMPLEMENT: `b612dca0e0f7e4b426d6bc11b34d455754873927`

## Verdict

Round 2 confirms the verify-FAIL fix: M1.5 / M2 closeout guards allowlist only
`src/blueberries_voi/browser.py` (ADR 0097 reopen / T-044 slim façade) and still
reject premature `*pyodide*` / `*wasm*` / `*eng01*` / non-allowlisted `*browser*`
`.py` modules under `src/`. Prior round-1 approval of the product/extras work
stands.

## Blocking

(none)

## Non-blocking

- [`tests/test_m15_closeout.py` / `tests/test_m2_closeout.py`] `_ALLOWED_BROWSER_MODULES`
  is duplicated; fine for now — consolidate if a third closeout suite needs the
  same list.
- Scans now require `.py` suffix (drops `__pycache__` noise). Non-Python artifacts
  named `*wasm*` under `src/` would no longer trip these tests; packaging still
  belongs outside `src/` (T-046+).

## Checks (reviewer, round 2)

| Check | Result |
|-------|--------|
| Allowlist is exactly `browser.py` (rel. to package root) | Pass |
| Nested `*/browser.py` / `*pyodide*` / `*wasm*` / `*eng01*` still gated | Pass |
| `browser.py` remains slim derived-Abdella façade (no pyarrow/matplotlib) | Pass |
| Closeout tests pass at implement tip | Pass (`test_no_production_ctl_voi_browser_under_m15`, `test_no_browser_or_pyodide_packaging_modules_in_src`) |

## Round 1 (reference)

STATUS: APPROVED at `4ea305e` — product + extras + lazy parquet gating; full text
was on `team/T-044/review` @ `edef879`.

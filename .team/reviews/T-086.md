STATUS: APPROVED
ROUND: 3

Reviewed tip: `a8d5599c7fb69a67e3e14517d8f1b1f088e67f45` (`team/T-086/implement`)
Worktree: `.worktrees/T-086-review` on `team/T-086/review`
Base: `main` merge-base `c5485c2` (ticket stack); T-086 delta vs qa tip `eb541c1`

Focus (round 3): Mock day-to-act / `completedDay` DayDelta parity with
`EngineSession`; inclusive `buildStepNOrders`; UI `completed + 1` after
`applyDelta`.

## Blocking

(none)

## Non-blocking

- [`.team/qa/T-086-smoke.md`] Advance-skips table still shows exclusive-era
  vectors (`[qty]` from Mon; `[0, qty]` from Fri/Sun). Unit tests + footer note
  already encode inclusive length; refresh the table to `[0, qty]` /
  `[0, 0, qty]` so the smoke note matches GREEN vectors.
- [`web/src/calendar/nextOrderAdvance.test.ts`] Comments still say
  `Length = 1 − 0 = 1` / `[0 (Mon), 16 (Tue)]` while expects are inclusive —
  comment drift only.
- [`web/src/main.ts`] Studio bumps `vm.episode_day` to `completed + 1` after
  `applyDelta` (projector still stores completed). Correct for next
  `buildStepNOrders`; optional later: teach projector Snapshot vs DayDelta
  cursor semantics so hosts do not special-case.

## Acceptance criteria

| Criterion | Verdict |
|-----------|---------|
| Primary play advances to next order day via `step_n` (zeros then qty) | Pass — `main.ts` `onAdvance` → `buildStepNOrders` → `adapter.step_n`; chrome “Advance to next order day” |
| Optional single-day step available or documented omitted | Pass — omitted in `.team/qa/T-086-smoke.md`; `adapter.step` remains on contract |
| Weekday labels from epoch `2024-01-01` + episode day | Pass — `weekdayLabel` + play chrome `#day-label` |
| LT=1 next delivery / pipeline hint | Pass — `pipelineDeliveryHint` + `#delivery-hint` |
| Mock mode same advance semantics | Pass — Mock `state.day` = day-to-act; DayDelta `episode_day` = `completedDay`; Snapshot uses day-to-act; tests use `buildStepNOrders` + completed-day end assert |
| Smoke / unit proof skips non-order days | Pass — `T-086-smoke.md` + `nextOrderAdvance.test.ts` / `nextOrderDayUi.test.ts` |
| No OrderSchedule redefinition beyond Snapshot fields (ADR 0114) | Pass — helpers consume `order_weekdays` / `epoch` / `lead_time_days`; no `protection_days` |

## Round notes

1. Inclusive `buildStepNOrders` (`len = next − current + 1`) matches
   `EngineSession` `orders[i]` → `episode_day + i`.
2. UI cursor after batch = last DayDelta `episode_day + 1` (next act).
3. Mock clock aligned: `createInitialState` / `stepSimulation` mirror
   day-to-act vs completed DayDelta like `_advance` / `advance_day`.

## Test quality (static)

Assertions are substantive (order vectors, weekday membership, spy on
`step_n`, source contracts for chrome). No vacuous asserts spotted. Vitest /
full gates left to verifier.

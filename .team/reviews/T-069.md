STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [src/blueberries_voi/filter/belief.py:134] `effective_inventory` docstring still says “MF marginals”; age rows are arrival priors under ADR 0106 — wording only, not a wire/AC miss.
- [src/blueberries_voi/viz/fil11.py] Stage A helpers still compute contraction diagnostics; honesty docs correctly demote that to non-gate — leave as historical harness unless a later ticket retires the metric.

## Checklist (diff + AC + ADR 0106)

### Correctness
- `shelf_belief_from_particle_filter_REMOVED` still builds nested `(L, K)` from `particle_filter.age_posterior`; after T-068 that mass is filter-carried arrival/birth priors, not `mean_field_update`.
- Module/factory docs cite ADR 0106 and state arrival-prior / birth-prior exports (superseding ADR 0092 MF reading).
- Tests lock F2 Dirac and F2a birth-prior newest-lot rows, and assert export ≠ diagnostic MF rewrite.

### Contract adherence
- ENG-01 `flatten_shelf_belief` field names and flat length `L·K` unchanged; tests cover wire keys and row-major flatten.
- Stage A docs/harness under `experiments/` plus `viz/fil11.py` state F2a/F2 ages from priors and that P0/P1/F1 do not claim in-store age learning as a production gate.
- Changelog entry states RB / in-store age marginalisation was removed because in-store age learning was dropped (not “bootstrap is simpler”), cites T-068/T-069.

### Security
- N/A (docs + belief export semantics; no new trust boundaries).

### Test quality
- `tests/test_belief_arrival_priors.py` asserts observable export/wire/doc/changelog behaviour; not vacuous.
- Guard supersession in `tests/test_belief.py` renames MF-shaped test to arrival-age rows and bans stale MF factory docs.

# T-020 Review — Stage C exact joint vs mean-field

DATE: 2026-08-12
STATUS: APPROVED

## Scope reviewed

Worktree `/home/oliver/blog/workspaces/blueberries-voi-fil11c-stagec` on
`exp/fil11-stage-c-mf`. ADR 0090 + spec T-020; module
`src/blueberries_voi/filter/age_likelihood.py`; tests
`tests/test_age_likelihood.py`; experiment `experiments/fil11_stage_c_mf.py`.

## Checklist vs T-020 AC

| Criterion | Result |
| --- | --- |
| Typed APIs `log_p_sales_waste_given_ages` / `exact_joint_update` / `mean_field_update` | Pass |
| Stage 0 hand 2x2 TV < 1e-9 | Pass (pytest) |
| Shared `death_prob_survival_ratio` / `picking_weights` import identity | Pass |
| MF rows sum to 1; distinct from exact API | Pass |
| Experiment + result.md with FIL-04 recommendation | Pass |
| Figure marginal TV vs sigma at L=3 | Pass (`figures/m1/fil11_stage_c_mf_tv_vs_sigma.png`) |
| Production `_particle_filter_update` still uses MC LL (not replaced by age_likelihood) | Pass |
| ADR 0049 / 0057 STATUS still ACCEPTED | Pass |
| Recommendation only (no ⚑ flip) | Pass |

## Notes

- Likelihood is named `sequential_wor_pmf` (DP matching `allocate_sales`).
- Mean-field is coordinate ascent with mean age plug-ins, max 5 sweeps.
- Stage 3 shows joint TV growth on a long particle path; P1 base gates still
  pass and Stage 4 actions agree. Settle note should flag Stage 3 accumulation
  for Oliver when considering a board reopen.

## Verdict

APPROVED.

STATUS: APPROVED
ROUND: 2

## Summary

Round 1 blocker resolved at `de69c12`: Guided Paths is now a single header entry point
(`#guided-paths-host` in `hero-tools`), mounted via `renderGuidedPaths()` in
`studioLogic.ts` with `onGuidedPathSelect` wiring scenario + section (+ autoplay hint).
Per-section inline Guided Paths markup is absent from the focus pane. The popover
`Start here` trigger matches ADR 0129 §5 header promotion.

Diff `team/T-124/implement...HEAD` (`web/`) satisfies the ticket-scoped AC shards at
review depth: hatch CSS, obs-chip projector sync, `ChapterTabs`, `ReferenceDrawer`
(replacing deleted glossary/shortcut/VOI panels), floating `DayInspector` + `hoverLink`
signature, `StoreChartTabs` dual-mount, and merge layout (`studio-layout--two-pane`,
self-sized rail `align-self: start`, `ReferenceDrawer` once in header, chart ids
preserved, day-inspector portal idle-empty).

## Blocking

(none)

## Non-blocking

- AC-merge lists keyboard shortcuts 1–8 / arrows / `?` / `Escape` as exercised in
  `sections.controller.test.ts`, `main.stockout.test.ts`, or `studioScenarios.test.ts`;
  those files were not updated for shortcut behavior (handler remains in
  `studioLogic.ts` / `ReferenceDrawer.tsx`; `ReferenceDrawer.test.ts` covers `?`/`Escape`
  for the drawer). Verifier should confirm via full `cd web && npm test`.
- No component or wiring test asserts `#guided-paths-host` mount or `onGuidedPathSelect`
  behavior; optional follow-up for regression guard.

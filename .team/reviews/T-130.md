STATUS: APPROVED
ROUND: 2

## Blocking

(none)

## Non-blocking

- Spec interface list names `#chart-demand-host`; implementation colocates via `#demand-chart-slot` reparent of `#chart-demand` — behavior matches intent; id naming drift only.
- `controllerOrders.test.ts` does not exist; bar rendering is covered by implementation (`rect.order-bar`) and visual e2e, not a dedicated rect-dominance unit test.
- `freshnessHistogram.test.ts` aggregated-mode case asserts single series count, not distinct fill color (implementation uses one aggregate series).
- `EventsPane.test.ts` does not assert weekday headings or zero-quantity lot filtering explicitly; implementation provides both via `weekdayLabel` and `formatLotBreakdown`.
- `StudioLayout.test.ts` does not assert `data-layout="v5"` on `.cockpit-grid` (covered by `studio-visual-qa.spec.ts` test 1).

## Round 1 resolution

- **Demand DOW render (studioLogic.ts):** Fixed. `renderDemandDist` is now gated on `activeSection === "demand" && schedule` instead of dead `plotVisible("plot-demand")`. `setSection("demand")` reparents `#chart-demand` into `#demand-chart-slot` and calls `renderActiveFocusPlots()` (with rAF re-measure). Static regression added in `demandUi.test.ts`.
- **Observation hint copy (controls.ts):** Updated to reference Secondary pane scenario chips instead of retired decision rail.

## AC spot-check (diff + tests; verifier PASS on implement tip)

- Layout v5 grid areas, retired Run sidebar, Secondary chrome order — `cockpitGrid.css`, `StudioLayout.tsx`, `StudioLayout.test.ts`, e2e.
- SecondaryChrome obs/truth/tradeoff tabs with `useEffect` refresh — `SecondaryChrome.tsx`, `SecondaryChrome.tradeoff.test.ts`.
- Today pane metrics visible; sales/stockout hidden hosts — `StudioLayout.tsx`, `main.stockout.test.ts`.
- Controller order bars, PnL lines-only, histogram aggregate mode — chart modules + tests.
- Tuning: 1/σ label, α/ρ pad, rollout default, demand colocation — `controls.ts`, `controls.test.ts`, `tuningDock.css`.
- Events daily log — `EventsPane.tsx`.
- studioLogic SecondaryChrome mount, e2e selector migration — `studioLogic.ts`, `studio-visual-qa.spec.ts`.

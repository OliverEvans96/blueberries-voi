# T-128 Review

**Verdict:** APPROVED

**Reviewer:** implement-round (T-128)

## Scope checked

- ADR 0133 channel semantics, preset map, cache key — matches spec ACs.
- Rust `ObsChannels`, `mask_from_channels`, `set_obs_channels`, lazy cache keyed by channels.
- Python parity in `filter/types.py` and `EngineSession.set_obs_channels`.
- PyO3 + WASM RPC `set_obs_channels`; `set_obs_scenario` preset alias retained.
- Studio DecisionRail: POS / Waste / Deliveries toggles + preset dropdown (no six ladder chips).
- `scenarioAvailability` keyed by channels; F2 preset uses pack_date not age_at_receipt.
- Out of scope respected: no `day_step.rs` multi-lot changes.

## Notes

- F2 mask change (pack_date vs age_at_receipt) is intentional per ADR 0133 / Oliver lock.
- VOI preset ids unchanged; citeable regen still human-owned per backlog.

EOF
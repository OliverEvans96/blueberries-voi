STATUS: APPROVED

## Scope reviewed

T-141 implement tip on `team/T-141/implement`: independent per-unit gamma aging in ground
truth, Poisson-binomial spoilage likelihood and adapted filter proposal, mandatory
`GammaDecrementTable`, LGTIN/UPC diagnostic regen (24 rows), rollout cost-ranking fixture
update for ADR 0143 physics, ADR supersession closeout, and notebook 14 refresh.

## Acceptance criteria

All spec items in `.team/specs/T-141.md` are satisfied per `.team/qa/T-141.md` PASS on
Python 3.11 CI-identical gates.

Notable diffs vs pre-T-141:

- **Physics:** `apply_gamma_aging_independent` in `day_step`; shared-δ interval primitives removed from `unit_ll`.
- **Filter:** `pb_sample_deaths_by_lot` for LGTIN-aligned proposals; `filter_step_unit_with_birth_cached` threads table through session/VOI/diag.
- **Rollout:** cost-flip fixture uses `[30,15]` freshest inventory (independent aging changes economics vs T-131 fixture).
- **Diagnostics:** `count_bias = 0` on all spoilage rungs; P1→F1 store mean-f MAE improves on homogeneous fixture (`0.0225 → 0.0219`); lot mean-f within documented slack vs P1.
- **Docs:** ADR 0143 accepted; ADR 0137 marked SUPERSEDED; INDEX + changelog updated.

## Verify citation

`.team/qa/T-141.md` — PASS (603 pytest, full `cargo test -p voi_core`, ruff, mypy, regen script).

## Residual notes (non-blocking)

- Thermal-fleet `F1` store mean-f MAE is marginally above `P1` (+0.0001); AC-G4 guard tests three primary regimes only.
- Filter cost ~50–200 ms/day at N=200 (documented in notebook §5); within studio budget.

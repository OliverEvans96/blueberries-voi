STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [src/blueberries_voi/controller/toy_dp.py:299] `gap_vs_rollout` compares DP optimum to the **myopic protection base** arm (documented), not T-030 one-step rollout. Spec AC and QA explicitly allow “rollout (or base)” on the same toy instance while T-030 is parallel — fine for this ticket; swap/add the rollout arm when T-030 lands if the certificate should adjudicate CTL-02 depth directly.
- [src/blueberries_voi/controller/toy_dp.py] On the locked CI grid, `gap_vs_rollout(solve_toy_dp())` is currently `0.0` (`J* == J_base` at the empty initial state). Still a valid finite non-negative gap; the instance does not yet stress under-improvement. Enriching the grid later is out of AC scope.
- [src/blueberries_voi/controller/toy_dp.py:22-24] `tau_bins` only set state dimension (`len`); bin values are unused in transitions. Acceptable for a truncated discrete toy; continuous-τ fidelity is not required by AC.
- [src/blueberries_voi/controller/toy_dp.py:199] `lead_time_days` is accepted then discarded (LT=1 baked into the one-period pipeline). Documented; callers passing other LT values get silent no-ops.

## Checklist (spec / QA)

- Backward induction → `ToyDpResult` with non-empty `value_table` / `policy_table`; locked small grid (`demand {0,1,2}`, τ length ≤4, `max_lots=2`, horizon ≤4) — met
- Documented same-instance gap via `gap_vs_rollout` (finite, ≥0); docstring covers gap / base|rollout / same|identical|toy — met (base stand-in allowed)
- β=1 / constant-`w` trap: SW and Rung 0 share `Δτ_L`; toy publishes matching `DELTA_TAU_L` / `delta_tau_L` — met
- Pure `controller/` (no matplotlib / parquet / file writes); module at `controller/toy_dp.py`; package exports — met
- Targeted pytest `tests/test_toy_dp.py`: 12 passed (full-suite coverage gate left to verifier)

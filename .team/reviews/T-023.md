STATUS: APPROVED
ROUND: 3

## Blocking

(none)

## Non-blocking

- Round-2 format blocker cleared: `uv run ruff format --check tests/test_belief.py` → already formatted; tip `16e0029` collapses `_flat_prior_expected_survival` signature to one line.
- Round-1 float/`int` floor bias remains fixed: fractional MF means preserved; regression still asserts float hand calc ≠ floored.
- [src/blueberries_voi/filter/belief.py:146-147] Pipeline still ignores pending lag keys (qty × flat-grid \(\mathbb E_g[S]\)); freeze with T-024 if needed.
- Frozen dataclass still exposes mutable `list` fields; optional tuples later.
- Viz `OracleBelief` left in place (spec open question); out of scope.

## AC checklist

| AC | Verdict |
| --- | --- |
| `ShelfBelief` importable from `blueberries_voi.filter` | Pass |
| `shelf_belief_from_particle_filter_REMOVED` → MF `(L, K)` marginals via `age_posterior` | Pass |
| `shelf_belief_from_oracle` same public fields / Dirac nearest knot | Pass |
| `effective_inventory` = SW(from_marginals=True) + pipeline (incl. fractional MF means) | Pass |
| CTL path does not need `ResearchParticleFilter._state` | Pass |
| list/float JSON `to_export` / `from_export` | Pass |
| `pytest tests/test_belief.py` green; no new runtime deps | Pass (20); `ruff format --check` Pass |

## Round 3 notes

Merged implement tip `16e0029` into review (`40d6922`). Prior round-1/2 blockers cleared; gates clean (`ruff check` / `mypy` on belief paths; `pytest --no-cov tests/test_belief.py` → 20 passed).

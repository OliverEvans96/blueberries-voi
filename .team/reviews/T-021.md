STATUS: APPROVED
ROUND: 2

## Blocking

(none)

## Non-blocking

- [.team/changelog.md:26-29] Older T-015 bullet still reads as if production joint→sliding_window fallback is current; T-021 above supersedes. Optional clarifying edit later.
- [src/blueberries_voi/filter/rbpf.py:61-62] `_apply_backend_choice` only swaps `_backend` when `choice.backend == "mean_field"`; safe while `choose_backend` is always MF.

## AC checklist

| AC | Verdict |
| --- | --- |
| 1. `PRODUCTION_BACKEND == "mean_field"`; default RBPF → MeanFieldBackend | Pass |
| 2. P1 UNOBSERVED maps → real `mean_field_update`; simplex; TV moves | Pass |
| 3. Weights still `observation_loglik_mc` (not `sequential_wor_pmf`) | Pass |
| 4. Lot maps still `_apply_lot_map_age_update` | Pass |
| 5. `choose_backend` always `mean_field`; no silent L trunc; no joint production gate | Pass |
| 6. Bakeoff A–E retained; `full_joint` guard for that arm only | Pass |
| 7. ADR 0091 ACCEPTED; 0049→C; 0082/0089 superseded; 0057 historical; no new runtime deps | Pass |
| 8. Changelog plain-English production mean-field entry | Pass (`mean-field` in T-021 bullet) |

## Round 2 notes

- Changelog AC8 fixed: T-021 entry includes “mean-field”.
- `_rbpf_update` P1 MF branch gated on `backend_name == "mean_field"`; lot-map path unchanged for all RBPF-style arms; bakeoff A/C/E no longer run per-particle MF.
- Targeted QA batch: **34 passed, 0 failed**.

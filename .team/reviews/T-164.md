# T-164 review

Status: **APPROVED**

Reviewed `team/T-164/implement` through `e4e2b972` against `.team/specs/T-164.md` and ADR 0151.

## Summary

- `protection_sim.rs`: model-faithful window simulator, `sla_mc` / `sla_pb` ordering, `sla_order` inversion.
- Session `act`, PyO3 bindings, alpha-tune ladder, studio 0.7.4, docs page, notebook 20.
- CI green on run 33108712497 (all required jobs).

## Caveats (accepted)

- Fisher vs Wallenius picking law in `sla_pb` (documented; notebook quantifies).
- Fractional `lot_counts` rounded to integers on MC path.
- `sla_mc` shelf-separation covered via fast PB test, not slow MC regression.

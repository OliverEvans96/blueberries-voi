# T-164 review

**Verdict:** APPROVED

## AC coverage

| AC | Status | Notes |
|----|--------|-------|
| AC-protection-sim | OK | `protection_sim.rs` bank start-state + reference simulator; tradeoff bench wiring |
| AC-sla-mc | OK | MC protection-window order via `sla_mc_order_f_belief`; unit tests green after path-count fix |
| AC-sla-pb | OK | PB analytic SLA + survival index tests |
| AC-session | OK | `session.rs` act path exposes `sla_pb` / `sla_mc`; PyO3 FFI wired |
| AC-ladder | OK | `alpha_tune` + `tuned_alpha` ladder arms; Python interlock tests |
| AC-studio | OK | Policy chips + `actOpts`; `web/package.json` bumped to **0.7.4** |
| AC-nb20 | OK | `notebooks/20_ordering_policy_bakeoff.ipynb`, `service.py` metrics, viz helpers |

## ADR / docs alignment

- `docs/control/window-service-level.md` documents sla_pb / sla_mc controllers with code citations.
- Changelog entry for window SLA ordering present.

## Risks noted

- MC unit tests are sensitive to path count; `sla_mc_empty_shelf_orders_more_than_full` keeps 16 paths while other MC tests use fewer for speed.
- Notebook 20 is exploratory (not in pytest gate); bakeoff viz covered via library wiring tests only.

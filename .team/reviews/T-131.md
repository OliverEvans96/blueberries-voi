STATUS: NEEDS WORK
ROUND: 1

## Blocking

- [crates/voi_core/src/rollout.rs:78-104,304] **ADR 0134 terminal salvage regression** — `path_value_f_belief` ends with `terminal_salvage_unit_state`, which sums **Weibull** `w_long(τ)` per alive unit (`weibull_survival` via `f_to_age`). ADR 0134 §3 and spec AC require f-native `V_T = margin * effective_inventory_f_belief(lot_counts, f_marginals, f_grid, pending_sum=0)` on the **terminal** `truth_f_belief` state, with `terminal_salvage_f_belief` documenting `V_T = m * Σ_l E[f|l] * n_l`. `main` already used `effective_inventory_f_belief`; this branch regressed. Fix: at horizon end call `truth_f_belief` on final unit state, then `margin * effective_inventory_f_belief(..., 0, f_pipeline_default)` (or equivalent on the oracle belief wire); drop Weibull from the rollout hot path.

- [crates/voi_core/src/rollout.rs:387-421] **No-repeat-delivery AC not tested** — `no_repeat_delivery_over_horizon` only asserts `value.is_finite()` and exercises `enqueue`/`pop_arrival` outside the sim. Spec requires counting `deliver_units` / arrival events and proving total delivered equals `first_order` plus pipeline arrivals only (never `first_order × H`) for `H ≥ 3`, empty `pending0`, default schedule, `lead_time=1`.

- [crates/voi_core/src/rollout.rs:307-422] **Missing Rust terminal salvage unit test** — No `#[test]` locks `terminal_salvage_f_belief` on a hand-built f-grid with `margin = 2.0` against `margin * effective_inventory_f_belief(...)` per spec AC.

- [crates/voi_core/src/rollout.rs / voi.rs:480-534] **Missing Rust costs-ranking test** — Spec requires two `rollout_order` calls with identical seeds but `waste_cost` (or `stockout_penalty`) differing enough to flip the winning `first_order`. `voi.rs` only tests `candidate_case_radius`; no `waste_cost` / `RolloutCosts` ranking test in `cargo test -p voi_core rollout`.

- [tests/test_t131_f_native_rollout.py] **Canonical ticket suite absent** — Spec guard-supersession AC names `tests/test_t131_f_native_rollout.py` with: (1) `detect_crn_desync` ok/fail wiring, (2) monkeypatch guard that `EngineSession.act(policy="rollout")` does not call `bakeoff_rollout._path_value` when `_core` built and `BLUEBERRIES_VOI_BACKEND=rust`, (3) documented thin Python fallback when `_core` missing. Branch adds `tests/test_rollout_rust.py` but not the required file or AC coverage.

- [tests/test_rollout_rust.py:148-175] **Costs test asserts nothing about ranking** — `test_costs_affect_ranking_fixture` only checks `isinstance(..., int)` for low vs high `waste_cost`; spec requires **different** winning `first_order` when costs flip the best candidate.

- [tests/test_rollout_rust.py:49-68] **No-repeat-delivery Python test is vacuous** — Calls `rollout_order` and asserts `q >= 0`; does not count deliveries or assert the pipeline contract from the spec.

## Non-blocking

- **Inner forward sim structure (good):** `path_value_f_belief` correctly gates `first_order` to `h == 0`, continues with `damped_sw_order_f_belief` on `truth_f_belief`, uses `enqueue`/`pop_arrival`, delivers only when `arrival > 0`, and addresses `SpawnRng` streams per `(root_seed, run_id|rollout|p{path}, day)`.

- **Caller wiring (partial):** `session.rs`, `voi.rs`, and `alpha_tune.rs` pass `RolloutContext` with schedule, pending, day, shipments, CRN `run_id`, and budgets. `session` / `voi` hard-code `RolloutCosts::default()` (no session cost surface); `alpha_tune` threads episode costs — acceptable if intentional.

- **Rust-primary Python path:** `bakeoff_rollout.rollout_order` delegates via `_try_rust_rollout_order` when `_core` is built; `alpha_tune` has `_evaluate_rollout_python` fallback. Lacks the spec’s explicit guard tests.

- **Guard supersession (partial):** `tests/test_rollout.py` module-level T-121 skip replaced with `_ROLLOUT_COMPUTE_SKIP` redirecting to `test_rollout_rust.py`; structural/doc tests remain. Not equivalent to spec’s `test_t131_f_native_rollout.py` canonical suite.

- **test_rollout_rust.py:71-107** — `test_w_long_terminal_matches_python_on_fixture` validates **Weibull** cohort parity (`terminal_salvage_value` / `w_long_oldest_first`), which reinforces the wrong terminal model for f-native scoring once ADR 0134 terminal is fixed.

- **test_rust_act_policies.py** — Rollout belief-parity tests still `@pytest.mark.xfail` (spec open question to narrow/remove when T-131 AC met).

- **Notebooks / uv.lock** — Large notebook and lockfile churn in the diff; out of ticket AC scope but increases review surface.

## Summary

Forward-sim mechanics (pipeline, oracle continuation, SpawnRng, parameterized day profit) are largely in place and callers are threaded. **Blocking:** terminal salvage reintroduces Weibull (explicit ADR 0134 rejection and regression from `main`’s `effective_inventory_f_belief`), and several spec-mandated tests — especially `test_t131_f_native_rollout.py`, delivery-count, costs-ranking, and f-native `V_T` — are missing or vacuous. **Verdict: NEEDS WORK** before verify.

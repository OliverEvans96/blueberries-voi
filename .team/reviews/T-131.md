STATUS: APPROVED
ROUND: 2

## Round 1 blocking items — resolved at `team/rollout-forward-sim` tip

- **ADR 0134 terminal salvage:** `path_value_f_belief` ends with `truth_f_belief` → `terminal_salvage_f_belief` using `effective_inventory_f_belief` (pending=0). Weibull `terminal_salvage_unit_state` retained for PyO3 / cohort fallback helpers only.
- **`tests/test_t131_f_native_rollout.py`:** canonical suite (CRN desync, costs flip, EngineSession `_path_value` guard, slow rollout≥SW).
- **Rust delivery count:** `path_arrival_units_sum` + `no_repeat_delivery_over_horizon` asserts pipeline delivers `first_order` once, not `first_order × H`.
- **Rust costs ranking:** `rollout_costs_flip_winning_order` on table-belief fixture (matches Python).

## Notes (non-blocking)

- `session` / `voi` still use `RolloutCosts::default()`; `alpha_tune` threads episode costs — intentional per round 1.
- `test_rust_act_policies` rollout xfails not narrowed in this round.
- `policy.rs` includes T-132 protection MC helpers alongside restored `rung0_order_f_belief`; verify should confirm no regression.

## Verdict

**APPROVED** for verifier handoff on Python 3.11 CI parity.

STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [web/src/engine/projector.ts:75-86] `beliefGridFromFlat` feeds L×K (lot×age) density into `BeliefGrid` by labeling lot index as `tau_edges` and age (`tau_grid`) as `count_edges`. `beliefAgeCount` still draws age×count with truth at `(tau, n)`, so the studio heatmap axes/overlays can disagree with the cells. QA explicitly scoped chart pixels out of T-054; fix in chart contract or a proper rebin before T-057 polish.
- [web/src/engine/projector.test.ts:174-196] `setEconomics` “no adapter call” spy constructs an unused `EngineAdapter` — assertions pass vacuously. Prefer asserting via method shape / no adapter dependency (current PnL checks already carry the AC).
- [web/src/mock/adapter.ts:171-174] Deprecated `MockAdapter.setEconomics` remains; studio correctly uses the projector path. Remove once callers are gone.

## Acceptance criteria

| Criterion | Verdict |
|-----------|---------|
| `ViewModelProjector` applies Snapshot / DayDelta → existing `ViewModel` | Pass — `applySnapshot` / `applyDelta` / `buildViewModel`; vitest green |
| `setEconomics` local, no engine round-trip | Pass — projector-only; `main.ts` `onEconomicsChange` wires projector; tests check PnL |
| `MockAdapter` implements `EngineAdapter`; Snapshot / DayDelta (not ViewModel) | Pass — `init`/`reset`→Snapshot, `step`/`step_n`→DayDelta; forbidden keys covered |
| Heatmap density from flat belief × lot counts | Pass — `densityFromFlatBelief`: `lot_counts[l] * age_marginals[l*K+k]` |
| Unit tests cover projector + economics-local path | Pass — `npm test`: 13/13 |
| Fake physics may remain in MockAdapter | Pass — `generateFlatBelief` / `generate.ts` retained |
| Out of scope: no Pyodide / Http adapters | Pass — only `MockAdapter` implements `EngineAdapter` |

## Notes

Reviewed tip `21783148` (`team/T-054/implement`) on `team/T-054/review`. Spec/QA read from `team/T-054/qa` (merge into this web tip was hook-blocked; review artifact only committed here).

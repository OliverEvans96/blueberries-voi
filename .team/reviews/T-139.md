# T-139 review

Verdict: **APPROVED**

Commit: `113f3d3` (`team/T-139/implement`)

Round: 1

## Acceptance criteria

| AC | Status | Notes |
|----|--------|-------|
| AC-1 F3 in AC-12 drift guard | **PASS** | `SCENARIOS` includes F3; `DRIFT_MAX = 0.11` |
| AC-2 Root-cause regression test | **PASS** | `f3_dispersion_count_bias_root_cause_temperature_birth_center` documents temperature `f_at_receipt` + `waste_by` fallback |
| AC-3 Filter birth mass conservation | **PASS** | `filter_birth_alive_mass_matches_arrivals_under_dispersion` |
| AC-4 Session lot_counts tracking | **PASS** | `session_lot_counts_track_arrivals_minus_decay` (seed sweep ≤200) |
| AC-5 Contrast hook | **PASS** | `contrast_spoilage_weight` exported from `unit_ll` / `lib.rs`; inert at sd=0 |
| AC-6 sd=0 VOI CRN parity | **PASS** | `test_voi_crn_sd_zero_matches_t138_baseline` |
| AC-7 Coverage ≥80% | **PASS** | verify: **82%** branch-aware (`test_filter_belief.py` + full suite) |

## Design notes

- F3 fix aligns filter birth center with truth `f_at_receipt` under dispersion and disables GSIN `waste_by` when `arrival_dispersion_sd > 0` (lot-uniform assumption breaks under within-lot spread).
- Contrast hook exported and applied to filter spoilage log-evidence when `arrival_dispersion_sd > 0`; inert at sd=0 on T-138 baseline paths.

STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [web/src/charts/demandDist.ts] Client reimplements `protectionCoverageFromSchedule` from
  `order_weekdays` + `lead_time_days` rather than calling library `OrderSchedule.protection_days`
  (ADR 0114 says web consumes schedule fields and does not redefine weekday math). Formula matches
  ADR 0114 / Sun–Tue–Thu → 3/3/4 and is what the RED map requires; fine for C3.
- [web/src/main.ts] Caption hard-codes “protection 3 / 3 / 4” even if a non-default schedule is
  projected; chart labels still follow live coverage rows.
- [web/src/charts/demandDist.ts] Flat `FALLBACK_SUMMARY` when `demand_summary` is missing is
  unlabeled; mock + projector paths supply real DOW means after init.

## AC checklist (spec vs demand UI)

- DOW profile from Snapshot `demand_summary.dow_means` (length-7 bars) — met (`dowSeriesFromDemandSummary` + `renderDemandDist`).
- Protection coverage Sun/Tue/Thu 3/3/4 from schedule fields — met (`protectionCoverageFromSchedule` + chart label chrome).
- μ-only / decorative sinusoid not presented as physics — met (PMF chart replaced; `Math.sin` in `generate.ts` marked non-physics; `salesDemand` history-only).
- Mock stub profile for charts — met (T-085 MockAdapter stubs; projector wires into ViewModel).
- Web unit/smoke DOW length 7 + protection labels — met (`demandUi.test.ts`).
- No new runtime Python deps; no HF in browser path — met (guards in RED; implement touch is web-only).

## Scope reviewed

Ticket tip `bfc4a8f` (qa `d25848f` + implement) vs integration base `main`. Diff+AC+ADR only; no pytest.

STATUS: APPROVED
ROUND: 2

## Blocking

(none)

## Non-blocking

- [web/src/main.ts:444] Order sync is duplicated in `applyDelta` (pre-`renderAll`) and
  `onTick` (extra `playChromeApi.update`). Harmless belt-and-suspenders; could later
  keep a single owner if the wiring is tightened.
- [web/src/autopilotLoop.test.ts] Vitest still proves `onTick(delta)` with
  `day.order_qty`, not the `main.ts` “sync before `renderAll`” contract that caused
  round-1 CHANGES_REQUESTED. A source-contract assert on `applyDelta` would lock the
  regression; not required for this round now that the tip does sync first.

## AC checklist (diff review)

| Criterion | Verdict |
|-----------|---------|
| `autopilotLoop.ts` play/pause, await one `act`, apply delta, `max(0, intervalMs - elapsed)`, no overlapping `act` | Pass — `createAutopilotLoop` / `inFlight` / `scheduleNext` |
| Default interval 500 / 1000; user Controller override wins | Pass — `defaultIntervalMsForPolicy` + `getIntervalMs` from `controllerState` |
| Play chrome Autopilot Play / Pause; pause clears timer | Pass — `controls.ts` labels + `setAutopilotRunning`; loop `pause` clears timer |
| Advance while running: disable **or** step+pause | Pass — Advance disabled + UI hint |
| After Autopilot `act`, order slider / inputs sync to `day.order_qty` | Pass — round-2 tip sets `orderQty` from `delta.day.order_qty` **before** `renderAll` in `applyDelta`; `renderAll` → `playChromeApi.update` → `syncOrderInputs` |
| Pause on `config_dirty` and on `act` reject (error via studio reporting) | Pass — loop checks + `onConfigChange` pause; `onError` → `reportStudioAdapterError` |
| Vitest: single-flight, delay math, pause on error/dirty, order sync via `onTick` | Pass (presence/assertions in tree; verifier owns run) |
| Autopilot via `adapter.act`, not `generate.ts` `runDay(..., autopilot)` | Pass — ADR 0112 |

## Round-2 delta

Prior CHANGES_REQUESTED: order slider not refreshed after Autopilot act (sync only in
`onTick` after `renderAll` had already painted stale `orderQty`). Tip `9d5cc60` syncs
in `applyDelta` before `renderAll` and also refreshes play chrome in `onTick`. AC met.

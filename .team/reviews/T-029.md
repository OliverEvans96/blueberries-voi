STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [src/blueberries_voi/sim/episode.py:95] Closed-loop still passes `belief=None` (known from T-024/T-027). SW tuning therefore sees empty-shelf / pipeline-only `I_tilde`; on the CI grid `(0.7, 0.8, 0.9)` SW episode profits can tie and `tune_alpha_grid` returns the first candidate. Desktop grid still differentiates at extremes — belief wiring remains a later joint concern, not a T-029 AC miss.
- [tests/test_alpha_tune.py] Pre-existing RUF003 (Greek `α` in comments/strings) fails `ruff check .`; not introduced by the implement diff (`alpha_tune.py` is clean). Fix before CI if not already ignored.
- [tests/test_alpha_tune.py] Grid tests assert `best in alphas` and shared `root_seed`, not that the returned α is a strict profit argmax — acceptable given CRN/profit spy coverage.
- [src/blueberries_voi/sim/alpha_tune.py:314] Profit-claim gate checks table presence/completeness, not that values were produced by `tune_alpha_grid` (placeholder arms can be hand-filled). Matches QA harness lock; provenance can wait for T-032.

## Checklist (spec / QA)

- Per-arm grid + shared CRN: `tune_alpha_grid` / `LADDER_ALPHA_ARMS`; placeholders raise; candidates share `root_seed` — met
- Artifact path: `DEFAULT_TUNED_ALPHA_PATH=experiments/tuned_alpha.json` + module docstring — met
- Untuned ladder profit rejected: `require_tuned_alpha_table` / `assert_ladder_profit_claim_allowed` — met
- Controller purity: API in `sim.alpha_tune`; no matplotlib in `controller/` — met
- Objective: `episode_profit` / `ProfitCosts` (SIM-01=B) — met
- Targeted pytest `tests/test_alpha_tune.py`: 23 passed

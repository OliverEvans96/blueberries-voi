STATUS: APPROVED
ROUND: 1

Reviewed: `team/T-047/implement` @ `918295bddd02b92e556361d7572757ab8506beb7`
Base: `main` (ticket stack); implement delta vs QA tip is `packaging/pyodide/*` only.
Focus: packaging/pyodide worker RPC, JSON payloads, no `src` pyodide/wasm, demo budgets, ADR 0100.

## Blocking

(none)

## Non-blocking

- [packaging/pyodide/worker.js] Inline bootstrap `handle_rpc` duplicates `session_rpc.py` but omits `prepare_demo_config` / shipment injection; live `main.js` init therefore cannot supply `ShipmentTrace` over JSON. Acceptable for T-047: gated smoke is the Python mirror (`smoke.py` + pytest), and QA explicitly deferred live Pyodide download.
- [packaging/pyodide/worker.js] `SLIM_WHEEL_URL` uses a placeholder `github.com/oliver/...` Release asset; fine until T-046 Release publishing lands a real URL.
- [packaging/pyodide/worker.js:127] Per-RPC `runPython` string eval is worker-local only; main harness correctly bans `runPython` / `runPythonAsync`.

## AC checklist

- Worker outside `src/`, pins Pyodide **314.0.4**, micropip slim/Release wheel, one `EngineSession`, methods `init`/`step`/`step_n`/`reset`/`act` — **met** (`worker.js` + docs).
- RPC payloads JSON strings / cloneable; no deep `toJs` / nested PyProxy on the wire — **met** (`json.dumps` / `JSON.stringify`; tests green).
- Main-thread harness `postMessage` only — **met** (`main.js`).
- Demo-budget smoke `init` + `step` + `step_n` (≥2) with pass/fail exit — **met** (`uv run python packaging/pyodide/smoke.py` → exit 0).
- Dialed `DEMO_BUDGETS` (≤200 / H≤7 / paths≤2 / radius≤1), not production `N=2000` — **met**.
- No `*pyodide*` / `*wasm*` under `src/` — **met**.
- ADR 0100 Snapshot/DayDelta (flat belief; no economics/PnL/ghost/heatmap) on RPC results — **met** (schema validate on live RPC outputs).

## Evidence

- `uv run pytest tests/test_t047_pyodide_worker_rpc.py` → **12 passed**
- Smoke script exit 0; RPC results validate via `validate_snapshot` / `validate_day_delta`

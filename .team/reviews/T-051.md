STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [src/blueberries_voi/api/app.py:80] `Snapshot` / `DayDelta` use `extra="allow"`, so OpenAPI emits `additionalProperties: true`. Declared properties match goldens and forbidden presentation keys are not named, but the schema does not reject undocumented extras. Consider `extra="forbid"` (or ignore + strip) later if HttpAdapter clients need a closed wire shape.
- [src/blueberries_voi/api/app.py:98] Nested `day` / `applied_config` / list item schemas stay loosely typed (`dict[str, Any]` / `list[Any]`). Acceptable for T-051 key-set parity; tighter DayLog typing can wait for a later contract ticket.
- OpenAPI response models are defined only in `api/app.py`, parallel to T-045 `simulator.schema` validators — fine for this ticket; a shared source of truth would reduce drift risk if the wire shape grows.

## Summary

Ticket-scoped implement change wires FastAPI `response_model`s (`FlatBelief`, `Snapshot`, `DayDelta`, `StepNResponse`) on `init` / `step` / `step_n` / `reset` / `act`, satisfying ADR 0100 §3 and the T-051 OpenAPI RED tests that failed on the qa tip. HTTP golden / T-045 validator / forbidden-key / failure-path assertions remain intact. `uv run pytest tests/test_t051_api_contract.py` → 14 passed.

STATUS: APPROVED
ROUND: 1
DATE: 2026-08-12

## Blocking

(none)

## Non-blocking

- [`src/blueberries_voi/filter/arrival_priors.py`](../../src/blueberries_voi/filter/arrival_priors.py): `delivery_birth_age_prior` still calls `arrival_age_prior_f2a(pack_date)` without `receipt_date` / `as_of`, so the F2a mean falls back to the cold-mix mean and Stage A contracts on width (`F2A_TRANSIT_UNCERTAINTY_SD`) alone. The T-019 synthetic calendar (`epoch + day − round(τ_in)`) is unused for mean location today. Pre-existing T-013 wiring; out of T-019 emit scope — track as a follow-up if honesty requires mean = `(receipt − pack).days`.
- No unit test locks the documented identity `pack_date == date(2024,1,1) + day − timedelta(days=round(age_at_receipt))`. Presence / CRN / contraction are covered; a one-line calendar assert would harden the closed open question.
- [`.team/changelog.md`](../changelog.md) T-018 bullet still says “F2a remains blocked until pack-date metadata is emitted” next to the new T-019 “no longer blocked” entry — edit the T-018 sentence on a docs pass so client-facing history is not contradictory.
- Spec T-019 quality-gates AC remains `[ ]`; QA targeted GREEN (7 passed). Verifier owns full `ruff` / `mypy` / `pytest` ≥80%.
- [`.team/qa/T-019-tests.md`](../qa/T-019-tests.md) “Not covered” still calls the episode calendar epoch open; spec open question is closed — refresh the test-map note.

## Correctness

- Delivery (`arrivals > 0`): `pack_date` set to real `datetime.date` via fixed epoch `2024-01-01` + episode day − `max(round(τ_in), 0)`; `age_at_receipt = float(tau_in)` unchanged in spirit.
- Non-delivery: both `pack_date` and `age_at_receipt` stay `None` (FIL-08: missing ≠ zero / invented date).
- Spot-check: `uv run pytest tests/test_pack_date_emit.py tests/test_sim.py::test_daylog_receipt_metadata_delivery_vs_none -q --no-cov` → **7 passed**.

## Contract adherence

- Spec ACs for emit, FIL-08, CRN, F2a vs P0/P1 mask projection, narrower birth prior SD, and Stage A F2a `contracted=True` are met by tests + sim change.
- Out of scope held: no CTL/VOI/browser; no `F2A_TRANSIT_UNCERTAINTY_SD` change; no new RichObs fields or mask rows; no prior/mask rewrites.
- Smallest deterministic calendar rule documented at module top of `sim/__init__.py` and closed in the spec open questions.

## Security

- No secrets, credentials, or unsafe I/O. Synthetic calendar only; no ASN/EDI ingest surface.

## Test quality

- `tests/test_pack_date_emit.py`: every test asserts observable outcomes; CRN case is non-vacuous (`any(p is not None)`); FIL-08 non-delivery `None` covered; mask and Stage A contraction locked.
- Tightened `test_daylog_receipt_metadata_delivery_vs_none` requires delivery `pack_date` as real `date`, not optional.

STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [web/src/charts/beliefAgeCount.ts:65] Cell tooltip still formats mass as `(v * 100)%`; after rebin `v` is lot-mass deposit, not a unit-normalized probability. Cosmetic only; axes and truth placement are correct.
- Visual DOM/D3 pixel alignment of the top strip vs heatmap is intentionally out of vitest (no jsdom); residual risk for margin/width mismatch at runtime — left/right margins are matched in source (`44`/`12`).

## Checked (ACs)

| Criterion | Verdict |
|-----------|---------|
| `beliefGridFromFlat` → age×count `K×C`; `tau_edges` from `centersToEdges(tau_grid)` not `[0,L]` | Pass — `web/src/engine/projector.ts`; tests in `projector.test.ts` |
| `count_edges` integer-friendly `0..max(n_l, truth n, 1)` | Pass — `integerCountEdges` + truthLots arg; projector passes `liveLots` |
| Rebin deposits `lot_counts[l] * age_marginals[l*K+k]` into nearest-int count bin | Pass — deposit loop + non-integer case in tests |
| `ageMarginalFromFlat` length-K; Σm ≈ Σ lot_counts when rows normalize | Pass — export + `BeliefGrid.age_marginal` |
| Top age-marginal chart; shared age (`tau_edges`) domain | Pass — `beliefAgeMarginal.ts`; mounted above `plot-belief-lg` in `main.ts` |
| Truth overlay at `(lot.tau, lot.n)` on age×count scales | Pass — `beliefAgeCount.ts` `x(d.tau), y(d.n)`; no lot-index x |
| Belief `plotIds` + blurb (age×count / truth / marginal) | Pass — `sections.ts` |
| No Python / wire FlatBelief change; no Ticket B creep | Pass — diff is `web/` + `.team/` only; `FlatBelief` unchanged; no scenario chips / day_driver / reset-config edits |
| Vitest covers rebin + marginal; T-054 `L×K` tests superseded | Pass — `projector.test.ts` + `sections.belief.test.ts` |

## Notes

Reviewed tip `f0bc77a` (`team/T-090/implement`) on `team/T-090/review`. Diff vs `main`: architect → qa → implement. Diff + AC + ADR 0109 only; no pytest/vitest/ruff/mypy run.

STATUS: APPROVED
ROUND: 1

Ticket-scoped review of `team/T-117/implement` (`7b8fc1a`, includes merge of `main`) against `.team/specs/T-117.md` and ADR 0126. Diff vs `main` is `voi_core` observation/filter/belief flatten, one PyO3 `FilterObs` default-fill, projector vitest, and `.team/` spec/ADR/QA map. No `.github/workflows/` and no Python package production edits.

## Blocking

(none)

## Acceptance criteria

| AC | Verdict |
| --- | --- |
| F2 vs P0 `belief.age_marginals` differ after arrivals; `live_lots` identical | Met — `session.rs::f2_belief_differs_from_p0_live_lots_unchanged`; `belief_value` is `particle_bank_to_flat`; `live_lots_value` still physics. |
| F2a age mass narrower than P1 | Met — `f2a_age_mass_narrower_than_p1`; F2a Gaussian SD=0.75; unlabeled mix is a 0.5/2/4d cool-path spread. |
| P0 vs P1 after waste | Met — `p0_vs_p1_belief_differs_after_waste`; P0 mask omits `waste_tot`. |
| F1 vs P1 after uneven `sales_by` | Met — `f1_vs_p1_belief_differs_after_uneven_sales`; `sales_by` WOR score + lot-map align when lengths differ. |
| Catch-up CRN vs never-switched; P2 / B-state errors | Met — `catch_up_f2_matches_never_switched_and_not_oracle`; `mask_for` / `set_obs_scenario` reject P2 and B-state. |
| `cargo test -p voi_core` + projector density from `snapshot.belief` | Met — tests in `obs.rs`, `rbpf.rs`, `belief_flat.rs`, `session.rs`; vitest T-117 block in `projector.test.ts`. |
| No workflow YAML; prefer zero Python production | Met — `crates/voi_py/src/lib.rs` only `..Default::default()` for richer `FilterObs`. |

## Non-blocking

- Unlabeled birth mix is hardcoded traces in `filter_step`, not `EngineSession.shipments` (wasm still avoids Abdella parquet).
- `pack_date_days` is stored as `round(delivery_tau)` (I1 Gaussian mean), not `day - round(tau)` as the spec sentence; calendar transit matches the F2a tests.
- Flatten uses the first `L` particle slots; filter window is two lots, so occupied mass can sit in slot 0 with slot 1 padded uniform.

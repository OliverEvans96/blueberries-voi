STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [backends.py:228-311] `_apply_lot_map_age_update` uses excess-above-equal-share ×
  Weibull/picking affinity (and death_prob for waste) to move per-lot age
  grids. That is a factorised heuristic on top of MC particle weights, not a
  call through `picking_weights` / `allocate_sales`. It meets the leakage AC
  and avoids banned `sales_pow` / `waste_pow` / Gaussian symbols; exact
  per-lot forward remains out of scope. Prefer calling shared
  `picking_weights` on the grid if this path is revisited.
- [backends.py:375-381] When both `sales_total` and `sales_by_lot` are present,
  expected lot sales condition on `to_sell = min(sales_obs, on_hand)` — map
  scoring is not a pure open-loop predictive allocation. Same honesty class as
  T-011’s blended sales μ; fine for this gate.
- [backends.py:623-680] `BootstrapPFBackend` scores lot maps via
  `observation_loglik_mc` but does not call `_apply_lot_map_age_update`.
  Production `ResearchParticleFilter` → `FullJointBackend` → `_particle_filter_update` does. Age-leakage AC
  is covered on the production path; bakeoff bootstrap PF ages stay map-blind.
- [backends.py:250] `counts` is unused (`_ = counts`) in the lot-map age
  update — age affinity ignores on-hand; intentional for leakage control,
  but count-aware updates are left for a follow-on.

## Checklist

| Check | Result |
| --- | --- |
| `sales_by_lot` scored when present; lot-A > lot-B leakage ≤ 0.5 | Pass |
| `waste_by_lot` scored when present; analogous leakage | Pass |
| `UNOBSERVED` maps = totals-only; `{}` ≠ `UNOBSERVED` | Pass |
| F1 ρ=1 DayLog maps complete; biased-ρ absent from package | Pass |
| One ResearchParticleFilter / one `observation_loglik_mc`; no rung subclass | Pass |
| Shared kernels; no Wallenius density; no banned soft LL symbols | Pass |
| T-013 `delivery_birth_age_prior` birth block preserved | Pass |
| Tests substantive (not empty asserts) | Pass |
| Targeted pytest + ruff + mypy | Pass |

## Verification re-run

```text
uv run pytest tests/test_lot_resolved_ll.py -q --no-cov
→ 10 passed

uv run ruff check src/blueberries_voi/filter/backends.py tests/test_lot_resolved_ll.py
→ All checks passed

uv run mypy src/blueberries_voi/filter tests/test_lot_resolved_ll.py
→ Success: no issues found in 6 source files
```

## Verdict

T-014 acceptance criteria are met. Present lot maps enter the shared MC LL;
masked maps stay no-ops and empty observed maps are distinct; factorised
excess-share age updates keep cross-lot leakage under the locked bound; one
ResearchParticleFilter / one LL entrypoint remains; T-013 birth injection is untouched. No
blocking gaps.

STATUS: APPROVED
ROUND: 2

## Blocking

None.

## Non-blocking

- [tests/test_m2_multi_scenario.py:607-651] `test_rung0_arm_orders_positive_on_empty_shelf` locks source `demand_target`, `_protection_demand_fractile`, and a hand-built policy order — it does not extract the policy instance from `run_m2_multi_scenario`. Adequate for the round-1 gap; a tighter lock would call the runner construction path directly.
- [src/blueberries_voi/sim/m2_multi_scenario.py:122-129] `_protection_demand_fractile` is duplicated from `m2_gates` / `alpha_tune` (same NB×`PROTECTION_DEMAND_DAYS` formula). Fine for this ticket; consider a shared helper later.
- Prior round-1 non-blocking notes on `_run_closed_loop` drift vs `sim.episode`, L scored under B-state beliefs, and default `PRODUCTION_L=3` remain informational only.

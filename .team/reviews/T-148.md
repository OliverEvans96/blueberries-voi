# T-148 Review

**Verdict:** APPROVED

## Acceptance criteria

| AC | Status | Notes |
| --- | --- | --- |
| AC-version-guard — publishable diff ⇒ strict semver bump | Met | `tests/test_studio_release_version.py` diffs vs `origin/main` / `GITHUB_BASE_REF` and enforces `_semver_gt` on `web/package.json` |
| AC-first-bump — version `0.1.1` | Met | `web/package.json` bumped from `0.1.0` |
| AC-immutable-release — workflow_run auto `studio-v{version}` | Met | Draft workflow fetches tags, skips when present, publishes `studio-v${{ steps.pkg.outputs.version }}` on first sighting |
| AC-immutable-assets — `studio-v*` versioned tgz only | Met | Immutable steps attach `web/${{ env.PACKED }}` only; `make_latest: false`; `studio-latest` still ships both tgz assets |
| AC-docs — EMBEDDING.md + packaging/README.md | Met | Documents moving vs immutable pins, auto-cut tags, contributor bump policy |

## Notes

- Live `.github/workflows/release-studio.yml` unchanged per protocol; human sync required.
- Only `packaging/github-workflows/release-studio.yml` edited (not live workflows).
- Changelog entry added in plain English.

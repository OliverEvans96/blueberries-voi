STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [web/src/main.ts:159] Footer still says “Fake data studio” after the default path left Mock — cosmetic only; does not affect adapter selection.
- [web/src/engine/studioAdapter.ts:81-82] Unconfigured development (no API base URL, no override) falls through to **pyodide**, not a loud “configure Http” error. Matches “Mock not default”; smoke checklist already documents `VITE_ENGINE_API_BASE_URL` for the HTTP path.
- [web/src/engine/studioWiring.test.ts] Several chrome-wiring checks are source-contract regexes on `main.ts`. Adequate for this wiring ticket; not a substitute for the manual smoke checklist against a live ASGI / Pyodide host.

## Checklist (focus)

| Focus | Verdict |
|-------|---------|
| Prod default = PyodideAdapter | Pass — `resolveStudioAdapterKind` + `createStudioAdapter`; `main.ts` uses `createStudioAdapter` |
| Dev default = HttpAdapter when API configured | Pass — requires `VITE_ENGINE_API_BASE_URL` / `VITE_API_BASE_URL` |
| Mock not default | Pass — only via `VITE_ENGINE_ADAPTER=mock` or `kind: "mock"`; unconfigured non-prod → pyodide |
| Projector economics local | Pass — `onEconomicsChange` → `projector.setEconomics` only; no fetch / postMessage / adapter |
| Advance off fake physics | Pass — `adapter.step` + projector; no `stepSimulation` / `generate` import in `main.ts` |
| Smoke checklist | Pass — `.team/qa/T-057-smoke.md` documents env flags + pass/fail sections |
| Vitest | Pass — `studioWiring.test.ts` 16/16 green |

## Spec / ADR

Meets `.team/specs/T-057.md` acceptance criteria and ADR 0099 dual-runtime wiring (Pyodide=prod, HTTP=dev). Env names documented in smoke checklist (spec open question).

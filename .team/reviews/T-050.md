STATUS: APPROVED
ROUND: 2

## Blocking

- (none)

## Non-blocking

- Round-1 blocker resolved: `fastapi` / `httpx` are listed under
  `optional-dependencies.dev` as well as `[api]`; `uv.lock` marks both
  `extra == 'dev'` and `extra == 'api'`. CI’s `pip install -e ".[dev]"` path
  now pulls the ASGI stack without workflow changes.
- FastAPI still wraps `HTTPException(detail=_error_body(...))` as
  `{"detail": {ok, error}}` rather than the bare T-049 envelope at the top
  level. Tests accept either shape; tighten in T-051 / a custom exception
  handler if HttpAdapter needs the flat envelope.
- API handlers return `EngineSession` dicts without calling `validate_snapshot`
  / `validate_day_delta` server-side; AC is met via client-side validators in
  tests.
- `POST .../reset` and `.../act` use Pydantic body models (empty `{}` works); a
  body-less POST may 422 — fine for TestClient; confirm when wiring T-056.
- Starlette `TestClient` emits an httpx→httpx2 deprecation warning; harmless
  for now.
- Route coverage, Snapshot/DayDelta validation, OpenAPI `/openapi.json`,
  404/4xx JSON errors, and no matplotlib on the `api` import path remain
  correct against ADR 0102 / T-049 / T-050.

STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- Backlog deferred-semantic notes and the plain-English changelog entry are still
  integrate/close-out work (spec AC); not required on this implement tip.
- Optional late peels (`sim/m2/` package, deeper session peel, public RBPF
  accessors) correctly deferred; A–C façades are enough for this milestone.
- `filter/age_likelihood/__init__.py` rewrites `__file__` to the sequential-WOR
  leaf for AST scanners — intentional façade trick; worth a one-line comment in
  a follow-on if scanners grow more package-aware.
- `viz.m15.__all__` does not list `_write_stage_b_md`, but the name remains
  importable from `viz.m15` via re-export (AC is importability, not `__all__`).

## Checklist (diff vs `team/T-102/architect`)

- **No `tests/` edits:** confirmed (`git diff` / commit paths empty under `tests/`).
- **Behavior-frozen forks kept:** ceil stays in open-loop / `day_driver`; nearest
  `case_round` in episode / m2 / voi; dual `Policy.order` + `invoke_order`
  dispatch; m2 omits `day=` and OrderSchedule gate; voi/episode keep theirs;
  ESS resample remains `< 0.5 * N` in counts update; per-site τ grids explicit.
- **Façades:** real `def _rbpf_update` in `filter/backends.py` (+ end marker /
  `_SHARED_MC_KERNELS`); `age_likelihood` package re-exports; `model/__init__`
  + `_shared_day_step_symbol`; `viz.m15` / `viz.fil11` runners and pinned
  privates (`_arrival_prior`, `_spread`, `_write_stage_b_md`);
  `rollout_order` / `RolloutPolicy` stay on `controller/rollout.py`.
- **Waves A–C structure:** `filter/particle/*`, age_likelihood package, model
  params/physics/day_step, sim calendar/day_tick/types_log/open_loop,
  belief/protection/invoke_order, thin-wrapped drivers + rollout/viz peels.
- **`filter.__all__`:** unchanged vs architect tip.

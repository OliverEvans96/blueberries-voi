STATUS: APPROVED
ROUND: 2

## Round 1 blocking — resolved

- Rust `CandidateSearchConfig::with_overrides` and `candidate_search_from_rpc` now infer
  `StratifiedWide` when `n_candidates` or `candidate_span_cases` is set without an explicit mode;
  `candidate_case_radius` alone still forces neighbourhood.
- Python `_try_rust_rollout_order` forwards `candidate_search_mode="stratified_wide"` when wide
  mode is inferred from `n_candidates` / `candidate_span_cases`, matching the Python path.

## Acceptance criteria

- [x] `candidate_orders_v2` with `Neighborhood` and `StratifiedWide` modes (Rust + Python)
- [x] Wide mode includes `base_q`, K unique case multiples, adaptive span
- [x] `candidate_case_radius` alone → neighbourhood (unchanged)
- [x] Default remains neighbourhood; wide mode opt-in
- [x] PyO3 optional kwargs wired
- [x] Python mirror + tests; legacy neighbourhood test unchanged

## Non-blocking (unchanged)

- `DEMO_N_CANDIDATES` not yet wired to studio demo path
- Ablation script optional per spec

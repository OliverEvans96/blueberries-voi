STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- Implement changed CS4 fixture `(10.0, 8)` → `(10.0, 12)`. That is correct under the QA-locked
  tie rule (nearest; half away from zero / toward +∞ for non-negative `x`): `10/4 = 2.5` is
  halfway between 8 and 12 → 12. The prior expected `8` contradicted the locked prose and the
  other CS4/CS8 tie rows (`2→4`, `6→8`, `4→8`, `12→16`, …). No CHANGES_REQUESTED for that edit.
- Ticket tests all assert real behavior (fixture tables, docstring, purity AST, exports, Policy
  `order` surface); none are vacuous.
- Full-suite coverage / ruff / mypy left to verify (running only `tests/test_ordering.py` is green
  on assertions; package-wide `--cov-fail-under` does not apply to that slice alone).

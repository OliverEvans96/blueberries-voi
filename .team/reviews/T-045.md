STATUS: APPROVED
ROUND: 1
TICKET: T-045
IMPLEMENT_TIP: 8d80f52e4fc51f0ff332329423f26b161ff8a270
REVIEWED: 2026-08-12

## Blocking

(none)

## Non-blocking

- [`src/blueberries_voi/simulator/__init__.py`] Validators live under `blueberries_voi.simulator.schema` (spec-allowed) but are not re-exported from the package `__init__`; optional for T-051 convenience.
- [`src/blueberries_voi/simulator/__init__.py:7-8`] Package docstring still says Day fields are “open until T-045 goldens”; goldens are now frozen — docstring is stale only.
- [`tests/test_simulator_schema.py`] Rejection tests catch a wide exception union (`ValueError|TypeError|AssertionError|KeyError`); fine while helpers raise correctly, but a tighter expected type would harden the contract surface.
- [`.team/qa/T-045.md`] QA status file still says **RED**; update to GREEN is process hygiene for handoff, not a code gate.

## Verdict summary

Ticket-scoped implement tip (`59d92ea...8d80f52`) adds only `simulator/schema.py`, frozen goldens under `tests/fixtures/simulator/`, README (RBPF-on / seed 42 / DEMO_BUDGETS), and `tests/test_simulator_schema.py`. No wheel / Release / micropip / workflow work (T-046 out of scope held).

**Correctness / ADR 0098:** `validate_snapshot` / `validate_day_delta` enforce required keys (`seq`, `episode_day`, Snapshot `belief`; DayDelta `day` + `drop_oldest`), flat belief lengths `L` / `L*K` / `K`, reject nested `age_marginals`, and recursively ban presentation keys (`economics`, `pnl_*`, `ghost*`, `heatmap`, `density`, `ViewModel` / `view_model`). Live seed-42 Snapshot/DayDelta match frozen goldens byte-for-byte in this environment; JSON round-trip excludes presentation leaks.

**Tests vs AC:** All 23 `tests/test_simulator_schema.py` cases pass (`uv run pytest … --no-cov`). Fixtures assert presence, schema validation, forbidden-key absence, flat lengths, and live `EngineSession` sharing the same helpers. No vacuous assertions found.

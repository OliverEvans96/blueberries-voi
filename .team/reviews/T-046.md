STATUS: APPROVED
ROUND: 1

Ticket: T-046 (slim wheel + GitHub Release + micropip smoke)  
Reviewed tip: `459859514b39704870d45e55a530efcbbdf3e606` (`team/T-046/implement`)  
Base: `main` (ticket-scoped vs qa parent `1c4c691`)  
Worktree: `.worktrees/T-046-review` on `team/T-046/review`

## Blocking

(none)

## Non-blocking

- [scripts/build_slim_wheel.py:33-41] Fallback for a missing `build` package catches
  `FileNotFoundError`, but `python -m build` without the package raises
  `CalledProcessError`, so the documented local `python scripts/build_slim_wheel.py`
  path fails until `pip install build` (Release workflow already installs it). Catch
  `CalledProcessError` or document the prerequisite in `packaging/README.md`.
- [packaging/github-workflows/ci.yml] Live `.github/workflows/ci.yml` still matrices
  only 3.11/3.12 until a human copies the canonical files — already recorded as
  **needs-human** in `.team/backlog.md` and the T-044 deferral checklist; not a
  ticket defect.
- Wheel still ships `model/abdella.py` and `viz/` modules; hard Requires-Dist is
  clean (numpy/scipy only). Browser import graph via `browser.py` /
  `abdella_product` is correct; optional later trim of desktop-only modules from the
  slim artifact.

## Checklist (focus)

| Focus | Verdict |
|-------|---------|
| Slim core deps (ADR 0101) | Pass — `project.dependencies` = numpy+scipy; pyarrow/matplotlib behind `[data]`/`[viz]`/`[dev]` |
| `packaging/github-workflows` canonical CI/Release | Pass — ci.yml adds 3.14 without dropping ruff/mypy/pytest; release builds, smokes, uploads |
| micropip docs | Pass — `packaging/README.md` + root README Release URL pattern, not PyPI |
| Pyodide 314 pin | Pass — ADR 0101 + packaging docs + workflow `env` (`314.0.4` / `3.14.2`) |
| build/smoke scripts | Pass — scripts present; Release job wires them; METADATA smoke OK once `build` installed |
| Honest needs-human for `.github/` | Pass — no live workflow edits; backlog + checklist + packaging README |
| No gate weakening | Pass — coverage/mypy/ruff thresholds unchanged; locked-dep tests updated to slim core |
| T-045 untouched | Pass — no Snapshot/DayDelta/schema paths in T-046 implement diff |

## Acceptance criteria

All ACs in `.team/specs/T-046.md` met under the agent-safe packaging mirror contract
(QA tests adapted to accept `packaging/github-workflows/`). `uv run pytest
tests/test_t046_slim_wheel_release.py` — 12 passed.

## Test quality

Assertions are substantive (matrix versions, hard-dep sets, micropip URL pattern,
pin echo, release publish/dry-run hooks, derived Abdella load). Implementer
widened workflow discovery to the packaging mirror without hollowing asserts.

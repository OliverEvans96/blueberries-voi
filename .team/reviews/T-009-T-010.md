# Review: Wave 1 (T-009 Rich DayLog + T-010 RichObs/masks)

STATUS: APPROVED
ROUND: 2
DATE: 2026-08-12

## Verdict

Round 1 quality-gate blockers are cleared. Re-verified:

- `uv run ruff check tests/test_sim.py tests/test_rich_obs.py` — clean
- `uv run mypy tests/test_sim.py tests/test_rich_obs.py` — clean
- `uv run pytest tests/test_sim.py tests/test_rich_obs.py tests/test_filter.py -q --no-cov` — 33 passed

Behaviour and contracts for T-009 / T-010 remain correct: rich `DayLog` emit,
frozen `RichObs`, `UNOBSERVED` sentinel, ADR 0086 masks, B-state rejection, and
ENG-02 shared `day_step`. **Missing ≠ zero holds on the mask path**
(`ObsMask.apply` / `rich_obs_from_day_log`). Soft-LL coercion of `UNOBSERVED` →
`0` stays documented and scoped to T-011.

No new blocking issues found in this re-check.

## Blocking

(none)

## Round 1 blockers — resolution

- [tests/test_sim.py] Ruff E501 on `saw_non_delivery` assert — shortened message; ruff clean.
- [tests/test_sim.py] Mypy `no-untyped-def` on `_short_episode` — now `-> EpisodeLog`.
- [tests/test_sim.py] Mypy `arg-type` on untyped `kwargs` dict — CRN test passes kwargs directly.
- [tests/test_rich_obs.py] Mypy `union-attr` / `unused-ignore` on frozen check — uses
  `dataclasses.is_dataclass` / `fields` / `FrozenInstanceError` + `replace`; no ignores needed.

## Non-blocking

- Soft LL adapter `p1_obs_from_rich` / `coerce_p1_obs` zeros `UNOBSERVED` totals
  for the legacy backend (`types.py:217-241`, `particle_filter.py:71-85`). Documented as
  temporary until T-011 — allowed for this wave; do not treat as mask-path
  missing=zero.
- `Unobserved.__bool__` is `False` (`types.py:76-77`): `x or 0` / truthiness can
  still collapse sentinel and zero at call sites. Prefer `is_unobserved` in
  future consumers.
- CRN AC locks same-seed equality of scored aggregates within the suite, not a
  golden pre-change fixture. Adequate given emit-only change (no new RNG draws).
- `pack_date` remains always `None` on delivery (Abdella has no ASN calendar) —
  matches open question; F2a present field correctly stays `UNOBSERVED` when day
  metadata is `None`.
- Pre-existing `experiments/` Ruff RUF001 (ambiguous `σ`) fails `ruff check .`
  independently of this wave — out of ticket scope; do not conflate with the
  cleared E501 above.

## AC checklist

### T-009

| AC | Status | Evidence |
| --- | --- | --- |
| Per-lot `sales_by_lot` / `waste_by_lot` | Met | `sim/__init__.py:177-186`; `test_daylog_sales_waste_by_lot_maps` |
| Live lots `n`/`tau`/`lot_id` | Met | existing `LotState`; `test_daylog_lots_keep_n_tau_lot_id` |
| Receipt metadata; non-delivery `None` | Met | `age_at_receipt`/`pack_date`; `test_daylog_receipt_metadata_delivery_vs_none` |
| Totals == map sums; empty when zero | Met | emit filters zeros; `test_daylog_totals_match_by_lot_sums` |
| CRN scored aggregates stable | Met | no new streams; `test_daylog_crn_scored_aggregates_stable` |
| ENG-02 shared `day_step` | Met | `test_sim_shares_day_step`; index align with `day_step` live start-of-day |
| Quality gates green | **Pass** | ruff + mypy + pytest re-verified Round 2 |

### T-010

| AC | Status | Evidence |
| --- | --- | --- |
| Frozen `RichObs` required fields | Met | `types.py:96-107`; `test_rich_obs_is_frozen_dataclass_with_required_fields` |
| `UNOBSERVED` ≠ 0 / None / {} | Met | singleton + `is_unobserved`; sentinel tests |
| `ObsMask.apply` → sentinel never 0/{} | Met | `types.py:136-150`; apply / P0 zero-waste tests |
| `mask_for` P0..F2 per ADR 0086 | Met | `_SCENARIO_PRESENT`; parametrized mask tests |
| P0 zero waste → UNOBSERVED | Met | `test_p0_mask_turns_observed_zero_waste_into_unobserved` |
| `rich_obs_from_day_log` no invent | Met | projects then `mask.apply`; no `0.0` for missing receipt |
| `ResearchParticleFilter.step` RichObs boundary | Met | `RichObs \| P1Obs` + coerce; type-hint + runtime step test |
| SCN-B-state not a fabricating mask | Met | `mask_for("B-state")` raises; reject test |
| Quality gates green | **Pass** | ruff + mypy + pytest re-verified Round 2 |

## Protocol / ADR 0086

- Mask path: absent fields set to `UNOBSERVED` only — **pass** (hard rule).
- Soft-LL zeroing: documented temporary adapter — **allowed** until T-011.
- B-state: explicit `ValueError`, not an ObsMask — **pass**.
- Weak `lot_ids_live` omitted from P0/P1/F2a present sets — matches QA/ADR.

## Test quality

Tests assert real behaviour (identity, sentinel, scenario tables, projection,
ResearchParticleFilter boundary). No empty/assertion-free tests. Targeted suite gates are green.

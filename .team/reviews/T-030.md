STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [src/blueberries_voi/sim/episode.py:64-112] Closed-loop now builds oracle `ShelfBelief` and dispatches day-first vs belief-first `order` surfaces. Not listed in the T-030 interface sketch, but required for the paired-CRN profit AC with belief-first `DampedSurvivalWeightedPolicy` and aligned with ADR 0092. Prior T-024 / ordering / rung0 suites still green; no AC break observed.
- [src/blueberries_voi/controller/rollout.py:80-95] `w_long_oldest_first` uses Weibull survival \(S(\tau)\) rather than a discrete queue-position index. Allowed by QA (“implement chooses the position→weight map”); product \(V_T = m \sum w n\) is locked. Note that with \(S(\tau)\) the oldest-first sort in `_lots_for_salvage` does not change the sum.
- [src/blueberries_voi/controller/rollout.py:171] `n_particles` is a documented desktop budget knob but currently discarded (`del n_particles`); MF path does not consume it yet. Satisfies the optional “and/or” budget AC; wire later when particle rollouts exist.
- [src/blueberries_voi/controller/rollout.py:271-274] Empty-shelf early-out returns the base order and skips candidate evaluation (pipeline-only states). Preserves ≥-base ties; may miss one-step improvement when on-hand is empty but pending arrivals matter.
- [tests/test_rollout.py:387-418] Paired-CRN profit check is `≥` only (spec allows ties). A pure base-echo wrapper would also pass; discrimination for actual improvement is left to unit/neighbourhood tests.

## Checklist (spec / QA)

| AC | Verdict |
| --- | --- |
| One-step candidates, H, \(V_T\) documented | Pass — `rollout_order`, salvage helpers, module docs |
| Mean rollout profit ≥ base SW under CRN | Pass — `test_rollout_mean_profit_ge_base_sw_under_paired_crn` |
| Optional budgets with desktop defaults | Pass — `H`, `n_rollout_paths`, `candidate_case_radius`, `n_particles` |
| Forward path = shared `model.day_step` | Pass — import + spy |
| Sequential (no multiprocessing) | Pass — AST gate |
| CRN desync detector pass/fail | Pass — `detect_crn_desync` |
| `controller/` purity (no figure/FS writers) | Pass |
| Ticket tests | Pass — 20/`tests/test_rollout.py` |

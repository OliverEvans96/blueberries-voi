STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [web/src/main.ts] Focus-pane DOM order still places `plot-inventory` above
  `plot-controller-orders`, while `STUDIO_SECTIONS` lists orders first — operators
  see inventory then orders; reorder the focus-plot markup if stacked order should
  match `plotIds`.
- [web/src/charts/controllerOrders.ts] `controllerOrdersSeries` is an identity map;
  fine for the RED helper contract and future transforms.
- Interval auto-default on policy change (500 vs 1000) is implemented; T-100 still
  owns Autopilot play/pause consumption of `controllerState`.

## AC checklist

- [x] `controller` is 8th `STUDIO_SECTIONS` entry, label “Controller”
- [x] `SectionId` includes `"controller"`; load/save round-trip via section list
- [x] Controls: policy chips, alpha/rho, rollout budgets, intervalMs
- [x] `controllerOrders.ts` + `renderControllerOrders` / series helper
- [x] `plotIds` include `plot-controller-orders` and reuse `plot-inventory`
- [x] `main.ts` mounts orders chart when visible; control changes update studio state
- [x] Vitest coverage present (section registration + chart helper smoke) — not run here

## Notes

Diff vs qa `4f7db72` is implement-only (`b96409b`): sections, controls, chart,
main wiring, styles. Aligns with ADR 0117 Controller ownership (section 8, knobs,
order chart; Autopilot loop out of scope).

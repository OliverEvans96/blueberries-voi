STATUS: APPROVED
ROUND: 2

## Blocking

(none)

## Round 1 blockers — verified fixed

1. **Vacuous singleton alphabet** — Fixed. `_stage_c_cohorts` uses inventory large enough vs default demand; probe under defaults (`n_obs_samples=200`) yields `n_support=64`. `run_fil11_stage_c` raises `RuntimeError` when `n_support < 2`.
2. **Fail path different structure from pass** — Fixed. Both paths are paired-CRN: same seed per draw; sim always `_obs_from_day_step`; model is `_obs_from_day_step` (production) or `_obs_from_wrong_physics` (soft powers + hazard×dt). Divergence is TV of the two empiricals. ADR 0088 explicitly allows this paired-CRN TV / match-rate form.
3. **Vacuous green tests** — Fixed. `test_stage_c_passes_under_production_mc_ll`, `test_stage_c_fails_when_wrong_physics_injected`, `test_stage_c_smoke`, and `test_stage_c_wrong_physics_fails` all assert `n_support > 1` (plus pass/fail divergence vs tolerance).

## Non-blocking

- Production path is CRN-identical `day_step` vs `day_step` (TV=0 by construction) plus a finite-score wiring call to `observation_loglik_mc`. That matches the ticket charter / ADR-allowed paired-CRN interpretation; falsification rests on the wrong-physics inject path (probe: TV≈0.495, `passed=False`).
- No dedicated unit test that the degenerate-alphabet `RuntimeError` fires; production raise + `n_support > 1` asserts are enough for this ticket.
- Optional RBPF-vs-brute auxiliary still skipped — OK per AC / prior review.
- Do not require T-013/T-014; full-repo sibling RED suites remain out of scope.

## Checklist vs T-012 ACs

| AC | Verdict |
| --- | --- |
| Fails under injected wrong-physics obs model (`passed=False`, divergence above tol) | Met — same paired-CRN structure; soft/hazard×dt diverges |
| Passes under production MC LL / shared day_step within documented tolerance | Met — non-degenerate support; `passed=True`; MC LL finite wiring check |
| Soft `tv_vs_exact` not production Stage C gate | Met — not called from `run_fil11_stage_c`; marked NON-GATE in backends |
| Documented `uv run` entrypoint; figure under `figures/m2.5/`; result MD path documented | Met — README, `experiments/fil11_c.py`, regenerated result MD + PNG on disk |
| Optional RBPF-vs-brute auxiliary | Skipped / optional — OK |
| Quality gates green | Scoped T-012 tests green; sibling tickets out of scope |

## Verification notes

```bash
uv run pytest tests/test_stage_c_generative.py tests/test_fil11.py -q --no-cov
# → 14 passed, 1 skipped
```

Reviewer probe: production `n_support=64`, `divergence=0.0`, `passed=True`; wrong-physics `n_support=64`, `divergence≈0.495`, `passed=False`.

## Summary

Round 1 CHANGES_REQUESTED items are resolved. Stage C is a falsifiable paired-CRN generative check vs `day_step` with non-degenerate P1 support. APPROVED.

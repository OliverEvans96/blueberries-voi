STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [src/blueberries_voi/sim/profit.py:7] Submodule imports `DayLog` / `EpisodeLog` via `from blueberries_voi.sim import …` (parent package). Safe today because `sim/__init__.py` does not import `profit`; a future re-export of profit from `__init__` could create a circular import. Prefer `from blueberries_voi.sim import …` only if types move out of `__init__`, or keep as-is until that happens.
- [`EpisodeLog.scored`](src/blueberries_voi/sim/__init__.py:74-75) is `days[n_burn:]` and does not slice by `n_score`. `episode_profit` correctly follows that existing contract; any `n_score` truncation belongs to logging, not this helper.

## AC checklist

| AC | Verdict |
| --- | --- |
| Day helper: `margin * sales - waste_cost * waste - stockout_penalty * max(0, demand - sales)` | Pass (`day_profit` + formula tests) |
| Holding cost not subtracted (SIM-01=B) | Pass (`on_hand` unused; lean vs fat inventory test) |
| No filesystem I/O; no matplotlib | Pass (module AST guards + implementation) |
| Episode helper sums **scored** days only (SIM-03 burn-in) | Pass (`episode.scored`; burn-in / empty / `n_burn=0` tests) |
| Unit tests: zero stockout, pure waste, pure lost-sales | Pass |
| Ticket pytest green; no new runtime dependencies | Pass (14/14 `tests/test_sim_profit.py`; deps lock test) |

## Review notes

- Scope reviewed: `2c1ad9a..c7bd02c` (implement-only `src/blueberries_voi/sim/profit.py`).
- Matches ADR 0093 / spec interfaces: frozen `ProfitCosts`, `day_profit`, `episode_profit`.
- QA assertions are behavioral (formula equality, burn-in exclusion, I/O bans), not vacuous.
- Full-suite coverage / ruff / mypy left to verifier.

# T-125 Review

**Verdict:** APPROVED

## Acceptance criteria

| AC | Status | Notes |
| --- | --- | --- |
| AC-pyodide — Pyodide packaging deleted | Met | `packaging/pyodide/`, `browser.py`, slim-wheel scripts, and pyodide vitest modules absent; runtime guards pass |
| AC-api — HTTP API deleted | Met | `src/blueberries_voi/api/`, `httpAdapter.ts`, and API-only tests absent |
| AC-studio — Studio defaults to WASM | Met | `resolveStudioAdapterKind()` → `wasm`; `StudioAdapterKind` is `wasm \| mock`; Vite serves WASM worker only; `scripts/studio.sh` WASM-only |
| AC-mixed — Mixed tests migrated | Met | `test_t071_demo_hydrate_edges.py`, `test_t113_obs_scenario_caches.py`, `test_t097_act_damped_sw.py` green (PyO3 + wasm worker paths) |
| AC-closeout — Closeout guards updated | Met | ENG-01 / M2 / slice closeout tests accept WASM-only narrative; no `browser.py` allowlist |
| AC-config — pyproject extras cleanup | Met | No `api` / `browser` extras; CI draft drops pyodide pin env vars |
| AC-docs — README updated | Met | README + `.team/changelog.md` describe WASM-only browser studio |
| AC-verify — integration gate | Met | Python 3.11 ruff/mypy/pytest (≥80% cov), `cargo test`, `cd web && npm test` all green on integration branch |

## Notes

- Integration branch merges all implement shards: studio, hydrate-obs, docs, config, closeout, api/pyodide removal.
- `httpAdapter.ts` / `pyodideAdapter.ts` deleted; vitest suites now exercise `WasmAdapter` for engine-status, obs-scenario, and scenario wiring contracts.
- PyO3 session tests require `maturin develop -m crates/voi_py/Cargo.toml` (or equivalent `_core` wheel) before the pytest gate — documented in verify artifact.

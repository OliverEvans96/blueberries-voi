# T-125 Review

**Verdict:** APPROVED

**Diff base:** `3bcccd6` (architect tip)  
**Integration tip:** `434fc7b` (`team/T-125/integration-merge-implement`)

## Acceptance criteria

| AC | Status | Notes |
| --- | --- | --- |
| AC-pyodide — Pyodide packaging deleted | Met | `packaging/pyodide/`, `browser.py`, slim-wheel scripts, pyodide adapters/tests removed |
| AC-api — HTTP API deleted | Met | `src/blueberries_voi/api/`, `httpAdapter.*`, ASGI contract tests removed |
| AC-studio — Studio defaults to WASM | Met | `StudioAdapterKind` is `wasm \| mock`; `resolveStudioAdapterKind` defaults wasm; vite middleware and package scripts cleaned |
| AC-mixed — Mixed tests migrated | Met | `test_t071`, `test_t113`, `test_t097` no longer import ASGI/session_rpc paths |
| AC-closeout — Closeout guards updated | Met | M2/ENG01/slice closeout tests accept WASM-only narrative; `test_t125_runtime_guards.py` present |
| AC-config — pyproject extras cleanup | Met | No `api`/`browser` extras; `test_t044_packaging_extras.py` updated |
| AC-docs — README updated | Met | README + packaging READMEs document WASM-only studio; changelog entry for T-125 |
| AC-verify — Integration gate | Not met | See `.team/qa/T-125.md` — `npm test` still imports deleted `pyodideAdapter`; pytest CI-parity block fails without `_core` build |

## Notes

- Integration merges docs-implement (`8d97790`) and closeout-implement (`77a0d2e`, latest closeout tip over `9800558`).
- Studio shard removed adapter source files but left three vitest files (`engineStatus.test.ts`, `studioScenarios.test.ts`, `studioObsScenarioCaches.test.ts`) importing `./pyodideAdapter` — verify blocker, not architect-scope regression in deleted modules.
- Retained research Python (`filter/`, bakeoff modules) per spec out-of-scope.
- Draft CI YAML still lists `PYODIDE_VERSION` env (human sync per AC-config); agents did not edit live `.github/workflows/`.

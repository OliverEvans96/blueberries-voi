STATUS: APPROVED
ROUND: 2

## Blocking

(none)

## Non-blocking

- [src/blueberries_voi/sim/m2_ladder.py:182-206] Episode arms still use distinct `run_id`s (`m2-ladder-{arm}`). Shared `root_seed` alone does not CRN-couple exogenous streams across arms; prefer one shared `run_id` for closed-loop ladder arms so demand/spoil/arrival draws match.
- [src/blueberries_voi/sim/m2_ladder.py:210-213] Default `out_dir=None` writes `experiments/m2_ladder_result.json` into the repo cwd; several tests omit `out_dir` and will litter that path.
- [src/blueberries_voi/sim/m2_gates.py:161-164] `force` / non-`force` branches of `assert_crn_desync` remain identical; comment about inverting `ok` is still misleading.
- [tests/test_m2_gates.py::test_beta1_degeneracy] Source-string checks for `DampedSurvivalWeightedPolicy` / `effective_inventory` are a useful regression lock but weaker than call-path instrumentation; the new behavioral fixture test covers the real risk.

## Round 2 resolution

- Round-1 block cleared: `assert_beta1_degeneracy` now drives the age-aware side through real `DampedSurvivalWeightedPolicy.order` on a same-age (flat-`w`) fixture, with Rung 0 weights/`demand_target` matched via `effective_inventory` and the protection-interval NB fractile (`PROTECTION_DEMAND_DAYS`).
- New `test_beta1_degeneracy_orders_match_on_same_age_fixture` asserts integer order equality independently of the gate helper.
- Targeted pytest `tests/test_m2_gates.py` + `tests/test_m2_ladder.py`: **22 passed**.

## Checklist (spec / QA)

- Five-point ladder API `run_m2_ladder(*, alpha_table_path, root_seed, ...) -> LadderResult` in `sim.m2_ladder` — met
- Tuned-α hard gate via T-029 `assert_ladder_profit_claim_allowed` / `require_tuned_alpha_table` — met
- Artifacts under `experiments/` (not `controller/`); controller purity / no new runtime deps — met
- Production backend `mean_field` locked on result + module — met
- ENG-04 node ids `test_beta1_degeneracy`, `test_crn_desync_gate`, `test_dp_certificate_gate` present; CRN wraps `detect_crn_desync`; DP wires `gap_vs_rollout` / `solve_toy_dp`; **β=1 age-aware side uses real SW** — met

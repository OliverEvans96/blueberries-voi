STATUS: APPROVED
ROUND: 1

## Summary

Implement tip migrates the packaging Pyodide host to a **module worker** + ESM
`pyodide.mjs` under pin **314.0.4** (ADR 0111). Diff vs `team/T-092/architect`
is scoped to worker/main/adapter + static/FakeWorker contracts; no workflow
edits, no Playwright, no pin downgrade.

## Acceptance criteria

| AC | Verdict | Notes |
|----|---------|-------|
| `worker.js` bans `importScripts` / classic `pyodide.js`; ESM import of `…/v314.0.4/full/pyodide.mjs` | PASS | Top-level `import { loadPyodide } from "https://cdn.jsdelivr.net/pyodide/v314.0.4/full/pyodide.mjs"`; `importScripts` removed |
| Pin 314.0.4 + wheelUrl / RPC / DEMO_BUDGETS retained | PASS | `PYODIDE_VERSION`, `resolveWheelUrl`, RPC set, `DEMO_BUDGETS` / `EngineSession` binding unchanged beyond bootstrap |
| `pyodideAdapter.ts` → `new Worker(url, { type: "module" })` | PASS | |
| `main.js` → `{ type: "module" }` not classic | PASS | Replaced `{ type: "classic" }` |
| Vitest FakeWorker asserts `{ type: "module" }` | PASS | `FakeWorker.options` + `expect.objectContaining({ type: "module" })` |
| Contract/static tests ban `importScripts` / require `pyodide.mjs` under pin | PASS | `tests/test_t092_pyodide_module_worker.py` + Vitest packaging-worker contract |
| No new Playwright / browser E2E dep | PASS | `web/package.json` unchanged; guard test present |
| No `.github/workflows/` edits; no Pyodide downgrade | PASS | Diff has no workflow paths; pin remains 314.0.4 |

## Blocking

(none)

## Non-blocking

- [packaging/pyodide/worker.js:18] ESM import URL hardcodes `314.0.4` while `PYODIDE_INDEX` uses `PYODIDE_VERSION` — values match today; a future pin bump must update both.
- [tests/test_t092_pyodide_module_worker.py] RPC method checks are substring matches on stripped source (acceptable for this static ticket; false positives possible if comments/string noise grow).

## Scope check (expected changes)

- `packaging/pyodide/worker.js` — module ESM bootstrap
- `web/src/engine/pyodideAdapter.ts` — module Worker options
- `packaging/pyodide/main.js` — module Worker options
- Tests — FakeWorker + Python/Vitest static contracts
- Out of scope left alone — no Playwright, no pin downgrade, no workflow edits

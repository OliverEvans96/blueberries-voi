# T-133 review

**Verdict:** APPROVED

## Scope check (implementation)

T-133 core deliverables on `team/rollout-forward-sim` (`9bbf5a6`):

| AC | Status |
|----|--------|
| `DemandProfile::from_parts` in `voi_core` | Done |
| PyO3 `#[pyclass] DemandProfile` with `mu(day)` | Done |
| `evaluate_alpha_tune_outcomes_py` wires scalars + optional profile | Done |
| `protection_demand_quantile_py` typed profile (not JSON) | Done |
| `alpha_tune._core_demand_profile` + calendar skip removed | Done |
| `test_alpha_tune_calendar_rust.py` | Done (μ parity, reproducibility, calendar≠flat, rust-path monkeypatch) |
| Notebook 12 Rust/calendar wording | Done (`9bbf5a6`) |
| Verifier CI gate PASS | Done (`9bbf5a6`) |

The typed wire is clean: Python dataclass → `_core.DemandProfile` without JSON on the
`alpha_tune` hot path.

## Notes (non-blocking)

- Spec AC mentions Rust vs Python episode outcome parity; tests intentionally use
  weaker checks because f-native Rust and Python cohort paths diverge.
- JSON shims for other entry points remain out of scope per spec.

## Recommendation

Ready to integrate to parent when human approves branch landing.

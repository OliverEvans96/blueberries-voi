# T-064 Review — unique-particle MF dedup

DATE: 2026-08-12
STATUS: APPROVED
TIP: `bf45fca` (`team/T-064/implement`)

## Findings

- `_rbpf_update` groups by `(tuple(counts[i].tolist()), age_post[i].tobytes())`, runs
  `mean_field_update` once per key, broadcasts — matches ADR 0103 / spec T-064.
- Acceptance tests cover call-count == unique keys, naive identity (resample suppressed),
  all-distinct path, and F1 lot-map skip.
- No new runtime dependencies; lot-map path unchanged.

## Verdict

APPROVED — no blocking issues.

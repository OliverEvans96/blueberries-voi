# T-064 Review — unique-particle MF dedup

STATUS: APPROVED
ROUND: 1
DATE: 2026-08-12

## Scope

Diff vs `main` for `src/blueberries_voi/filter/backends.py` + `tests/test_rbpf_unique_mf.py` (plus ADR 0097 / spec). Tip: `bf45fca`.

## Blocking

None.

## Findings

- **Dedup correct:** P1/F2a totals path (`mean_field` + observed sales/waste totals + UNOBSERVED lot maps) fingerprints `(tuple(counts[i].tolist()), new_post[i].tobytes())`, runs `mean_field_update` once per unique key, broadcasts into `new_post[i]`.
- **Numeric identity:** Acceptance test compares `_rbpf_update` output to a naive per-particle MF reference with exact `assert_array_equal`; duplicate particles share identical posteriors.
- **Lot-map path untouched:** `if sales_map or waste_map` still precedes the MF branch; F1 spy test asserts `_apply_lot_map_age_update` runs and MF does not.
- **No new deps:** `pyproject.toml` / `uv.lock` unchanged in ticket diff; ADR 0097 `STATUS: ACCEPTED` (2026-08-12).
- **AC coverage:** Call-count (duplicates → unique keys; all-distinct → N), identity vs naive, lot-map skip, ADR/deps gates — all present and asserting real behavior. Targeted pytest: 7 passed.

## Non-blocking

- Distinct-fingerprint fixture asserts call count == N but not an explicit naive posterior compare; identity is already covered on the duplicate fixture (stricter). Optional follow-up only.

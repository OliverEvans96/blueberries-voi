STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [src/blueberries_voi/controller/damped_sw.py:67] `delta_tau_L` is computed and exposed for Rung 0 parity but is not an argument to `effective_inventory` / `order`; survival weighting stays inside T-023. Acceptable for this ticket; callers should not assume the policy re-applies Δτ_L itself.
- [src/blueberries_voi/controller/damped_sw.py:58] ADR 0058 prose says `0<ρ≤1`; constructor does not validate ρ (or α beyond the quantile helper). Out of AC — add guards later if callers can misconfigure.
- Tests assert the full CTL-01=C formula via a locked hand table (incl. ρ≠1), default ρ=0.8, required α, ShelfBelief-only surface / no `ResearchParticleFilter._state`, LT=1 / R+L=2 / Δτ_L docs, and package export. `uv run pytest tests/test_damped_sw_policy.py` → 26 passed.

## Verdict summary

Implement tip `500be2c` (diff `afa1482..HEAD`: `damped_sw.py` + controller export) satisfies `.team/specs/T-028.md`, ADR 0058 (CTL-01=C), and `.team/qa/T-028.md` fixtures. Correctness, contract, and test quality are adequate to approve.

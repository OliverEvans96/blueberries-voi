STATUS: APPROVED
ROUND: 1
TICKET: T-043
IMPLEMENT_TIP: dfc0260b7db3dc22edaadb6344c96615d707880a
REVIEWED: 2026-08-12

## Blocking

(none)

## Non-blocking

- [day_driver.py:24] Importing `blueberries_voi.simulator` still loads `pyarrow` transitively via `model.abdella` module-level `import pyarrow.parquet`. Simulator sources themselves have no matplotlib/pyarrow imports (AC AST gate passes). Deferred to T-044 eager-Abdella split before Pyodide; do not treat as T-043 scope creep.
- [session.py:273-296] `act(policy="rollout")` is implemented but unexercised by the QA suite (coverage miss); constant-policy path covers the AC “controller surface + budget knobs + DayDelta shape” bar. Prefer a thin smoke for rollout before T-047 worker budgets.
- [session.py:169-170] Session boots RBPF via private `_root_seed` / `_run_id` (same pattern as `sim/m2_multi_scenario`); fine for now, but a public setter would harden the façade.
- [session.py:109 vs day_driver.py:124] `EngineSession.step` rejects `np.integer`; `advance_day` accepts it. Prefer one rule at the façade edge.

## Verdict summary

Ticket-scoped implement diff (`bd1c470...dfc0260`) adds only `src/blueberries_voi/simulator/` — no T-044 packaging / Abdella extras churn.

**Correctness / contract:** `EngineSession` exposes `init` / `reset` / `step` / `step_n` / `act`; Snapshot / DayDelta carry flat belief (`lot_counts`, `age_marginals` len `L*K`, `tau_grid`, `L`, `K`); JSON round-trip tests ban economics / PnL / ghost / heatmap / density; `advance_day` mirrors the established closed-loop order → pending/arrival → `day_step` → obs → optional RBPF → belief path; `DEMO_BUDGETS` / `BROWSER_DEMO_BUDGETS` sit at ADR 0097 caps; Day chart fields documented in package docstring pending T-045 goldens.

**Tests vs AC:** All 24 `tests/test_simulator_session.py` cases pass; mypy clean on touched simulator + test paths; ruff clean on those paths. Full-suite coverage / CI gates left to verifier.

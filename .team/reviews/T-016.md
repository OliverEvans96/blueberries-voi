STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [src/blueberries_voi/viz/m15.py:273-276] `tight_control_collapsed` ORs in
  `post_tight_sd < prior_sd * (1.0 - margin)`, which can mark the control
  “collapsed” even when the tight posterior is far from the tight prior (as long
  as it is merely narrower than the cold mix). Prefer the first clause alone
  (stay near `prior_tight_sd`), or mirror FIL-11’s “little further contraction”
  sense — smoke currently looks honest via the first clause.
- [src/blueberries_voi/viz/m15.py:2-6 vs 131-134 vs 216] Cohort docs mix
  “newest birth slot” with “first scored delivery, shift left, re-anchor when
  off-window”; behavior matches the latter. Align the module/API docstring with
  the tracker.
- [src/blueberries_voi/viz/m15.py] Uses private ResearchParticleFilter/`fil11` internals
  (`_root_seed`, `_run_id`, `_state`, `_arrival_prior`, `_spread`); fine for a
  viz helper, but brittle if those move.
- [tests/test_stage_a_multirung.py] Contracts are mostly schema / signature /
  documentation hooks; shared-episode CRN and numeric contraction are verified
  by the published smoke MD + live run, not by an automated behavioral assert
  (acceptable per `.team/qa/T-016-tests.md` “Not covered”).
- Process: DoD still needs verifier + plain-English `.team/changelog.md` entry
  for T-016 if not already added for this ticket alone.

## Correctness

- One `run_episode(..., root_seed=root_seed)` is shared across rungs; each rung
  only changes `mask_for(scenario)` via `rich_obs_from_day_log` — shared-CRN
  claim holds for the simulator streams.
- Cohort-from-birth tracker follows the first scored delivery, decrements on
  later arrivals, skips same-day birth-only reads, and falls back to a live slot
  rather than oldest-slot-only — coherent with the documented metric intent
  under short `L`.
- Smoke reproduce (`root_seed=0`, library defaults) matches
  `experiments/m15_stage_a_result.md`: P0/P1/F1/F1s/F2a uncontracted; F2
  contracts to Dirac SD `0.0`. Sim confirms `pack_date` is always `None` while
  `age_at_receipt` is set — F2a **needs-human** is honest, not papered over.
- No VOI-$ / CTL surface in `viz.m15`. T-017 Stage B / oracle not implemented
  here.

## Contract adherence

- `StageARungResult` / `StageAMultiResult` / `run_m15_stage_a` match
  `.team/specs/T-016.md` (defaults: six rungs, `contraction_margin=0.05`).
- Figures: `figures/m1.5/m15_stage_a_rung_map.png` + README figure→rung/FIL-11
  map present.
- Hard limits for this ticket: T-016 surface is `viz/m15.py`, `viz/__init__.py`,
  tests, experiment MD, and `figures/m1.5/` README — does not edit
  `backends.py`, `arrival_priors.py`, Stage C harness, or `test_l_fallback.py`
  (those may be dirty from sibling tickets in the workspace).

## Security

- No secrets, unsafe deserialization, or network I/O. Figure write is local
  under `figures/m1.5/`.

## Test quality

- QA STATUS PASS (15/15) confirmed locally; unhappy paths (empty / unknown
  rung / margin boundaries) raise as specified.
- Ruff + mypy clean on `viz/m15.py` and `tests/test_stage_a_multirung.py`.
- No assertion-free tests that gate the ticket.

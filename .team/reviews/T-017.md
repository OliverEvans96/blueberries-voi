STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [src/blueberries_voi/viz/m15.py:516-521] On F2 (Dirac / near-Dirac arrival-age posterior), equal-tailed CI via `searchsorted` often collapses to `lo == hi` on one grid node while true `τ` is continuous — smoke coverage for the only Stage-A-pass rung is **0.0**. Honest under the current metric, but Stage B “green on A-pass rungs” (plan DoD / T-018) will stay red until CI uses a discrete-grid rule (e.g. mass at nearest node, HDI, or jitter). Document in the Stage B MD if left as-is.
- [src/blueberries_voi/viz/m15.py:513-514] When `true_age` is missing, truth is replaced by the posterior mean — that invents coverage. Prefer skip / NaN that rep rather than self-referential truth.
- [src/blueberries_voi/viz/m15.py:805-807] `run_m15_oracle_ladder(write_md=True)` rewrites `ORACLE_GAP_MD_PATH` (== Stage B result MD) with **empty** Stage B rows. Default `write_md=False` and the README recipe are safe; guard or merge if both writers share one path.
- [tests/test_stage_b_oracle.py] Suite is mostly schema / signature / doc-hook contracts (same pattern as T-016). No automated assert that `stage_a_pass` flips `diagnostic_only`, or that a cheap `run_m15_stage_b` / `run_m15_oracle_ladder` smoke returns finite coverage and F2 ≪ P1. Published MD + local smoke cover that; a tiny behavioral test would harden the gate.
- [src/blueberries_voi/viz/m15.py] Uses private RBPF fields (`_root_seed`, `_run_id`); fine for viz, brittle if those move.
- Process: DoD still needs verifier + plain-English `.team/changelog.md` entry for T-017 when closing the ticket.

## Correctness

- Shared CRN: Stage B and oracle ladder both key episodes off `root_seed + rep` and differ only by `mask_for(scenario)` — matches the multi-rung claim.
- A-fail / unmarked rungs set `diagnostic_only=True` via `_resolve_stage_a_pass`; MD embeds `STAGE_B_DIAGNOSTIC_ONLY_LABEL`. Matches AC (skip **or** label).
- Coverage + rank histograms are produced per rung; pass language reuses fil11 band `[0.70, 0.99]` and documents non-U / non-dome ranks. Rank flatness is narrative + figure, not a numeric field on `StageBRungResult` — allowed by “metric **or** documented visual+numeric rule.”
- B-state harness (`OracleBelief` / `b_state_mean_abs_age_error`) is identity belief ⇒ age error **0** by construction. Ladder `vs_b_state = mae - 0` is consistent with that ceiling; published smoke shows F2 ≪ P1 (ratio under `ORACLE_GAP_F2_VS_P1_MAX_RATIO=0.5`).
- Foresight oracle rejected as unknown rung; no CTL / VOI / B-clair implementation on the m15 surface.
- Live smoke (`stage_a_pass={'F2': True}`, cheap N/reps) matches published `experiments/m15_stage_b_result.md` pattern: diagnostic labels on A-fail rungs, F2 not diagnostic, gap table PASS.

## Contract adherence

- Spec interfaces present: `StageBRungResult`, `OracleGapRow`, `run_m15_stage_b(..., rungs=...)`, `run_m15_oracle_ladder(*, root_seed, compare=("P1","F2"))`.
- Artifacts: `experiments/m15_stage_b_result.md`, per-rung `figures/m1.5/m15_stage_b_*_rank.png`, `m15_oracle_ladder_gap.png`, README Stage B + oracle map.
- Out of scope respected: no SCN-B-clair, no VOI dollars, no CTL.
- Quality gates on touched library/tests: pytest Stage A+B **38 passed**, ruff clean on `m15.py`, mypy clean on `m15.py` + `test_stage_b_oracle.py`.

## Security

- No secrets, credentials, or network sinks. Writes confined to repo `figures/m1.5/` and `experiments/` paths. Rung/compare validation rejects unknown ids. No issues.

## Test quality

- 23 focused contracts: schemas, kwargs, coverage/rank/diagnostic hooks, B-state zero-error harness, F2≪P1 helper (good/bad synthetic rows), path conventions, CTL/VOI/B-clair surface bans, empty/unknown/clair compare rejection.
- No empty / no-op asserts observed.
- Gaps (non-blocking): happy-path runners not exercised under pytest; MD content checks soft-pass if only the path hook exists. Acceptable for this ticket’s stated cheap unit scope given smoke publish + QA PASS.

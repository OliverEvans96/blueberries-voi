STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- Mock `damped_sw` / `rollout` heuristic uses alpha-damped base-stock only (`rho` / horizon / particle knobs ignored) — allowed by AC; operators should not treat it as citeable control performance (already disclaimed in the `act` comment).
- Shared normalizer applies flat knobs after nested `budgets`, so flat wins on key collision — consistent with the CALLER_OPTS fold tests; document if Autopilot ever passes both intentionally.

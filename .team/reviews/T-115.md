STATUS: APPROVED
ROUND: 1

Ticket-scoped review of `team/T-115/qa..4517eab` against `.team/specs/T-115.md` and ADR 0122. Production diff is JS-only under `web/src/` (10 files). No `crates/`, `src/blueberries_voi/simulator/`, or wasm adapter edits.

## Blocking

(none)

## Acceptance criteria

| AC | Verdict |
| --- | --- |
| Default `showTruth === false` when `localStorage` key `blueberries-voi-studio-show-truth` is empty | Met — `loadShowTruth()` returns `raw === "true"` only (`web/src/showTruth.ts`). |
| `saveShowTruth` / `loadShowTruth` round-trip; stored `"true"` / `"false"` | Met. |
| Heatmap `.truth-cross` (or overlay) when lots passed; zero when `truthLots=[]` | Met — `main.ts` passes `truthLots(showTruth, vm.live_lots)` into `renderBeliefAgeCount`; existing renderer + tests. |
| Survival rug, history lot circles, arrival-prior `age_at_receipt` length 0 when gated; `.truth-*` when on | Met — `truthLots` / `historyForCharts()` (clears `lots` and `age_at_receipt`); classes `truth-circle` / `truth-cross` on survival, history, arrival rugs. |
| Inventory vs target and age composition from `belief_history` when off, `history[].lots` when on | Met — `inventorySeriesFromBelief` / `ageCompositionSeriesFromBelief` vs lots series; wired in `renderActiveFocusPlots`. |
| Projector `belief_history` same rolling window as `history`; no `showTruth` on Snapshot/DayDelta | Met — window slice + day alignment; wire types in `engine/types.ts` have no `showTruth`. |
| Play chrome `/show true state/i`, `role="switch"`, `aria-pressed`, `#app` class `studio--show-truth` iff on | Met — `mountPlayChrome` + `truthClassTarget`. |
| Belief blurb does not require “truth” when off; on-state captions may mention overlay | Met — `sections.ts` blurb; `syncTruthCaptions` when on. `sections.belief.test.ts` no longer always-matches `/truth/`. |
| No production edits under crates / Python simulator / wasm adapters | Met (diff vs architect + qa). |

## Non-blocking

- `saveShowTruth` runs in `setShowTruth` (`controls.ts`) and again in `onShowTruthChange` (`main.ts`). Harmless double write.
- `ViewModel.on_hand` / `effective_inv` still come from `live_lots`. Charts use the belief path when the toggle is off. Those VM fields are unused in chrome today; ADR 0122 consequence would matter if chrome later displays them.
- `applySnapshot` stamps the snapshot’s current `belief` onto every existing history day. Length/window tracking is correct; per-day historical beliefs only accumulate after `applyDelta`.

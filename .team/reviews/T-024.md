STATUS: APPROVED
ROUND: 1

## Blocking

(none)

## Non-blocking

- [src/blueberries_voi/sim/episode.py:54] `case_round` lives in `sim/episode.py` (and is re-exported from `sim`). T-026 owns `controller/ordering.case_round`; `controller/ordering.py` is not present yet, so this is not live duplication. Acceptable for T-024 (matches open-loop ceil-to-case). When T-026 lands, consolidate so the two publics cannot diverge (T-026 open question prefers nearest; M1/T-024 use ceil).
- [src/blueberries_voi/sim/episode.py:95] Closed-loop always passes `belief=None`. Spec/QA leave ShelfBelief / P1 filter wiring to joint freeze with T-023 or later tickets; primary ACs (Policy + CRN + `day_step` + injectable `shipments=`) are met.
- [src/blueberries_voi/sim/episode.py:50] Spec sketch shows required `pending_orders: Mapping[int, int]`; implementation allows `None` with a default. Driver always passes a dict snapshot; soft optional is fine for stubs.

## Checklist (acceptance)

- **Policy path**: Public `@runtime_checkable` `Policy` with `order(day, belief, *, pending_orders)`; `run_closed_loop_episode` under `sim/episode.py`, exported from `sim`.
- **Same `day_step`**: Imports / re-exports `model.day_step`; physics loop mirrors open-loop `run_episode` (arrivals → spawn CRN streams → `day_step` → `DayLog`).
- **CRN**: `STREAM_DEMAND` / `ALLOC` / `SPOIL` / arrival ship+sensor via `spawn_rng`; unused-stream test covered.
- **Burn/score**: `EpisodeLog(n_burn=..., n_score=...)`; scored slice via existing `EpisodeLog.scored`.
- **`shipments=` / no FS**: Required kw-only `shipments`; empty → `ValueError`; no `load_abdella_shipments` on this path (monkeypatched tests).
- **Purity**: No matplotlib / pyarrow / parquet writes in driver modules.
- **Tests**: QA suite `tests/test_closed_loop_episode.py` — 18 passed (ticket scope).

# 2026-08-29/30 — Blog post figure work → LGTIN belief-accuracy bug hunt — handoff

**Branches:** `fix/lgtin-l-dim-truncation` (PR #88, off `main`), `feat/arrival-lottery-studio` (PR #89, off `main`, pre-existing single-commit UI feature, pushed/PR'd as a byproduct of this session's git cleanup)
**Role:** ad hoc figure development + investigation for Oliver's personal blog post (`/home/oliver/job-search/afresh-blog-post/my-post/2026-08-26 Draft.md`), which turned into a real bug fix along the way.
**Status (2026-08-30):** PR #88 open, unreviewed. The follow-on plan (rho range change, Ax retune batch structure, production notebook rerun sizing) is now **finalized and committed** into both `notebooks/12_damped_sw_alpha_bayesian_optimization.ipynb` and `notebooks/article_figures.ipynb` (commits `fb3a4fa8`, `9ab759d6`, `b5e76331` — see `.team/plans/2026-08-29-lgtin-belief-accuracy-blog-post.md` §2.6 for the exact final config). **Neither notebook has actually been run yet** — an independent audit of both was requested before running for real; a first audit attempt was started and cancelled mid-run so this doc and the plan doc could be brought current first. A fresh, thorough audit prompt has been prepared (see below / handed to the next agent) rather than run inline.

---

## 0. What this session actually was

Oliver is finishing a blog post about the blueberries-voi VOI/POMDP model tonight. The ask started as "review the draft and brainstorm figures," and organically became: build real figures from real (if smoke-scale) simulation runs, discover a genuine data-accuracy bug while building one of them, fix it, and now plan a production-scale re-run with the fix in place. Most of the substantive work is in **`notebooks/article_figures.ipynb`** (new, comprehensive, mirrors the draft's section order, regenerates every figure inline with its own code) and two source-code changes.

---

## 1. Figure development arc (chronological)

1. **Initial brainstorm** against the draft's `[FIGURE: ...]` placeholders, cross-referenced against existing notebooks (13, 17, t163_phase1_freshness_realism, etc.) and existing rendered PNGs under `figures/`.
2. **Delivery-conditions section rewrite** — dispatched a sonnet subagent to shrink and update the draft's "Delivery conditions" section to reflect the actual current arrival model (corridor mixture, staged temperature + hourly wobble, random breaks), which had drifted well past what the draft described. Found (and left as a flagged, unresolved discrepancy) a corridor-mixture-weight mismatch between `web/src/arrivalModelPreview.ts` (62.7/37.3) and the calibration notebook (80/20).
3. **Gamma-process figure** — ported `web/src/charts/physicsTeaching.ts`'s `gammaFreshnessEnvelope` math to Python, verified numerically against live Studio defaults, iterated to 3 store-temperature overlays with sampled realizations and full-width envelopes per Oliver's feedback.
4. **Ladder bar chart iteration** (belief accuracy vs. profit, monotonic rung ladder — Books only → Scan waste on → Pack date → LGTIN → Temp history):
   - v1: single profit-ratio bar chart with CIs.
   - v2: grouped twin-axis (belief ratio + profit ratio) — Oliver asked for twin axes since scales differ.
   - v3: Oliver asked to split back into two separate charts instead, and to use the raw (not inverted) W1-ratio direction ("X/base case").
   - **Confound discovered**: the closed-loop chart (each rung's own `damped_sw` controller acting on its own belief) was noticeably less monotonic than nb17's original belief-only ladder. Root cause: closed-loop episodes let better-informed rungs order differently, diverging their *actual* truth trajectories, so rungs weren't being scored against a shared shelf history anymore.
   - **Fix**: reproduced nb17 Part 1's replay design — one truth trajectory per seed (shared order schedule, driven by the baseline rung's own closed-loop run), replayed via `session.step(order)` through each rung's observation mask for belief scoring, while keeping profit on the real closed loop. Restored clean monotonicity.
5. **Full 12-cell factorial scatter** (belief W1 vs. profit ratio, full `code_type × waste_scan × delivery_history` factorial via `channel_joint.all_obs_channels_product()`) — built to match `figures/channel_joint/profit_vs_mae_dist.png`'s legend grammar (color=waste, marker=delivery, size=code_type), ratio-normalized with real per-seed CIs. Iterated the x-axis metric (added `day_w1_error`, briefly also `day_crps_mean_error` and signed `mean_f_error` in an appendix) and fixed an inverted-axis mistake that briefly looked like Simpson's paradox (it wasn't — just a mis-flipped x-axis).

## 2. The LGTIN bug hunt

Looking at the factorial scatter, Oliver noticed LGTIN usually looked *worse* than UPC on belief accuracy — counter to the model's designed guarantee that LGTIN (lot-resolved scanning) can never be less informative than UPC (pooled scanning). Investigated in two parallel tracks:

**Track A — closed-loop confound (tested directly):** reran the LGTIN-vs-UPC comparison with the same shared-order-schedule replay fix used for the ladder chart (one shared order schedule per `(waste, delivery)` context, driven by that context's UPC variant). Result: the gap shrank substantially in most contexts but a real residual survived in 5/6 — confound was a real, partial contributor, not the whole story.

**Track B — code-level bug hunt (background subagent, general-purpose, high effort):** found a genuine, confirmed bug — **`belief_flat_from_unit_bank`** (`crates/voi_core/src/belief_flat.rs:32-61`) truncates the exported belief wire to the newest `L` lot segments, silently dropping older segments with no renormalization. `L` (`l_dim`) was **3** everywhere in the notebook/experiment pipeline (`profit_session_config` in `voi_profit.py`), despite Studio's own compiled default (`DEFAULT_L_DIM` in `params.rs`) already being 10. LGTIN and UPC populate the bank very differently for the same physical delivery — LGTIN pushes each sub-lot as its own segment (~3 segments/delivery), UPC merges the whole delivery into one cohort (1 segment/delivery) — so the same `L=3` budget held roughly one delivery's history under LGTIN vs. three deliveries' under UPC. LGTIN was hitting the truncation continuously; UPC almost never. Dropped lots are always the *stalest*, so LGTIN's exported belief was systematically missing its stale tail — this directly explains both the excess W1 error *and* a separately-found positive signed bias (LGTIN's belief systematically overestimated freshness, `belief_f − truth_f > 0`, while UPC sat near zero). The same truncated wire also feeds the real controller's effective-inventory calculation (`session.rs`'s `f_belief_for_policy`), so this was under-counting real on-hand inventory under LGTIN in production, not just in the diagnostic.

**Secondary, lower-confidence mechanism (documented, not fixed):** LGTIN's cross-lot sales-allocation likelihood (`loglik_sales_by_units` / `lot_shares_from_freshness` in `unit_ll.rs`) scores against a **static Multinomial** built from pre-removal picking weights, rather than the true sequential-without-replacement mechanism the simulator actually uses (`pick_units_f` in `day_step.rs`; the codebase already has the exact equivalent scorer, `sequential_kernel_path_logprob`, sitting unused nearby). This is the one stage of LGTIN's four-stage scoring pipeline with no UPC analog (cross-lot allocation is new information, not a refinement of an existing UPC term — see `docs/inference/upc-vs-lgtin.md`), so it isn't covered by the formal "LGTIN ≥ UPC" containment proof. Reasoned mechanism: the static approximation doesn't capture the true process's self-balancing depletion (a fresh lot's *share* of remaining picks declines as it depletes, independent of per-unit weight), so it predicts more extreme fresh/stale splits than actually occur, systematically penalizing particles with real inter-lot differentiation and pulling the posterior toward flatter beliefs than truth. This plausibly explains the *residual* gap that survived both the `L` fix and the replay correction, concentrated specifically in the no-delivery-history contexts. **Not fixed — flagged as real, understood, future work.**

## 3. The fix, applied and verified

- `crates/voi_core/src/params.rs`: `DEFAULT_L_DIM` 10 → 50.
- `src/blueberries_voi/experiments/voi_profit.py`: `profit_session_config`'s explicit `"L": 3` → `50` (this literal override was silently beating the Rust default everywhere in the experiment pipeline; fixing only the Rust constant would have done nothing for any of tonight's figures).
- Benchmarked `L=3,10,20,30` under both `damped_sw` and `sla_pb`: no meaningful runtime difference among 10/20/30 (all comfortably inside Studio's 500ms/1000ms autopilot budgets); `sla_pb` tail latency was actually *worse* at `L=3` (p95 ~88ms vs. ~46ms at L≥10) — no performance reason to pick a small `L`. Chose 50 for headroom given the cost is effectively free.
- Rebuilt and reran `article_figures.ipynb` at `L=50`: ladder chart returned to clean strict monotonicity (LGTIN now beats Pack date, as expected); factorial scatter flipped LGTIN to performing as well or better than UPC in every delivery-history context (no-delivery-history residual persisted, consistent with the Track-B mechanism above); signed bias reversed from a strong systematic +0.05 to +0.065 to a modest −0.026, in line with other rungs.

## 4. Controller decision

Discovered along the way that `channel_joint.run_seed_channel_joint` (the pipeline behind every closed-loop figure) hardcodes `policy="damped_sw"` via `_damped_sw_act_kw`, regardless of which alpha table entry is loaded — so despite calling it "sla_pb" in conversation for a while (loading alpha from the `"sla_pb"` table entry), every figure has actually always been `damped_sw`. Separately investigated `sla_pb` (Poisson-binomial fast-path SLA controller) vs. `sla_mc` (Monte Carlo oracle): `sla_mc` showed a genuine non-convergence problem (order jumped 16→128 units between 128→256 MC paths on a toy belief) — not trustworthy. Given that, and that `damped_sw` already matches the draft's existing controller-formula prose with zero rewrite needed, **Oliver has now explicitly settled on `damped_sw` only, no SLA controllers, for the post.**

## 5. Git worktree incident (resolved, but read this if things look weird)

The working directory is shared with other concurrent agent sessions. Early this session we discovered we'd been silently bounced off our starting branch (`feat/arrival-lottery-studio`) onto `chore/ci-artifact-retention` by another agent's `git stash` + `git checkout`, and had been doing unrelated backend work on top of someone else's CI branch without noticing. Diagnosed via `git reflog` + inspecting stash contents (`git stash show --stat`), recovered everything (some via direct re-application since paths were known, some by simply redoing the small number of `git rm`/`git mv` operations), and reorganized into 5 clean commits on a fresh branch (`fix/lgtin-l-dim-truncation`) off `main`, since the actual work has zero relation to either the CI branch or the arrival-lottery-studio UI feature. Pushed both `fix/lgtin-l-dim-truncation` (PR #88) and the never-yet-pushed `feat/arrival-lottery-studio` (PR #89, one pre-existing commit, untouched).

**This recurred a second time, later in the session, after Oliver believed the other agent was stopped:** the reflog shows two more unrequested checkouts (`feat/arrival-lottery-studio → chore/ci-artifact-retention → origin/main`, landing HEAD detached) happened live during a later turn, silently reverting the working tree's `params.rs`/`channel_joint.py`/`voi_profit.py` back to pre-fix `main` content. Caught it because a Modal calibration run came back missing the `freshness_w1` field it should have had. Restored via `git checkout fix/lgtin-l-dim-truncation`, verified correct. **If you're picking this up and something looks reverted or half-there, check `git reflog` first before assuming the code is broken — it may just be interference from another concurrent session on the same working directory.** Also hit (and worked around): a wedged SSH multiplexer from that agent's interrupted processes that hung every `git push`/`fetch` over SSH (HTTPS worked fine) until the stale `ssh: .../mux` processes were killed; and GPG commit signing failing in this environment (no interactive pinentry) — Oliver explicitly authorized `--no-gpg-sign` for this session's commits.

## 6. Key files

- `notebooks/article_figures.ipynb` — the canonical, reproducible source for every figure discussed for the post. Structure mirrors the draft's own section order. Re-executable end to end (currently ~5-6 min locally: cargo release build for the cold-chain example, ladder replay batch, factorial batch, appendix batch).
- `src/blueberries_voi/experiments/belief_accuracy.py` — new `day_w1_error` (Wasserstein-1 belief-accuracy metric, works over arbitrary `ObsChannels`, not just nb17's named presets).
- `src/blueberries_voi/experiments/channel_joint.py` — wires `day_w1_error` into `run_seed_channel_joint`'s output row.
- `crates/voi_core/src/params.rs`, `src/blueberries_voi/experiments/voi_profit.py` — the `L` fix.
- `dist/wheel/blueberries_voi_core-0.3.0-*.whl` — rebuilt wheel reflecting the fix (untracked; `experiments/modal/app.py` auto-selects the lexicographically-latest wheel in `dist/wheel/`, so this is what any Modal dispatch picks up. The prior `0.2.0` wheel there was stale since Aug 24 and was the actual cause of the earlier "unknown policy sla_pb" Modal error — Modal mounts local *source* fresh every invocation but not the compiled extension).
- `figures/demand/plot_g_daily_mean_vs_model.png` — pre-rendered (FreshRetailNet fitting pipeline lived in now-archived notebook 15; not re-run in the article notebook to avoid re-triggering a dataset download).
- Notebook cleanup: deleted 01-11, 13, 14×2, 15, 18, 20 (superseded); archived 16 and 21 under `notebooks/archive/` (kept for historical reference — rollout MOO POC, `sla_mc` convergence-issue record). Kept 12×2 (production Ax tuning, explicitly "production-necessary" per Oliver), 17, 19_build_your_own_controller, 19_channel_factorial_belief_vs_profit, both `t163_*`.

## 7. What changed after this doc was first written (2026-08-30)

The §5 concurrent-session interference did **not** recur again after the point this doc was originally committed. Three more commits landed cleanly on `fix/lgtin-l-dim-truncation`, finalizing (but not executing) the forward plan from the companion plan doc:

- `fb3a4fa8` — Studio `rho` slider widened to `[0.5, 2.0]` (`web/src/controls.ts`), wasm rebuilt.
- `9ab759d6` — notebook 12: `RHO_BOUNDS = (0.5, 2.0)`; Ax retune restructured from one 24-trial loop into **4 batch cells of 12 trials each (48 total)**, `AX_PARALLELISM = 4`, so a manual run can be stopped early between batches and each batch prints its improvement over the previous one.
- `b5e76331` — `article_figures.ipynb` moved from an 8-seed local run to a **30-seed, Modal-dispatched production run** (`N_BURN=7, N_SCORE=45`), with its Modal dispatch code rewritten to match notebook 12's own established pattern exactly (explicit-argument shard functions, self-built `uv_sync` image, chunked spawn+get) rather than the older `experiments/modal/app.py` pattern it initially borrowed from — Oliver explicitly asked for Modal-usage consistency across notebooks before any audit or execution.

Both notebooks are regenerated and syntax-checked but **not executed** — no Ax retune has actually run on Modal yet, no production article-figures run has happened. Oliver then asked for an independent sonnet-subagent audit of both notebooks' defensibility before running; that audit was launched, then cancelled partway through so these two docs could be updated to current state first (the audit was working off an earlier version of this doc/plan that didn't yet reflect the final batch/seed/concurrency numbers above). See `.team/plans/2026-08-29-lgtin-belief-accuracy-blog-post.md` §2.6 for the exact final parameter values, and the audit prompt prepared alongside this update for the next attempt.

See the companion plan doc (`.team/plans/2026-08-29-lgtin-belief-accuracy-blog-post.md`) for the decisions log and the (now finalized-but-unexecuted) next-steps plan.

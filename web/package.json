{
  "name": "blueberries-voi-simulator-mockup",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "studio:wasm": "../scripts/studio.sh --wasm",
    "studio:http": "../scripts/studio.sh --http",
    "studio:pyodide": "../scripts/studio.sh --pyodide",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke:autopilot": "vitest run scripts/smoke-autopilot-mock.ts"
  },
  "dependencies": {
    "d3": "^7.9.0"
  },
  "devDependencies": {
    "@types/d3": "^7.4.3",
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vitest": "^3.0.5"
  }
}
